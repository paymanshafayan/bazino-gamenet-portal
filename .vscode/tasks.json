{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "run-dev",
      "type": "shell",
      "command": "npm run dev",
      "problemMatcher": [],
      "detail": "Starts the real backend (server.ts via tsx) + Vite dev server for the main site. Management App (Bazino) is a separate build step (see its own package.json)."
    }
  ]
}
