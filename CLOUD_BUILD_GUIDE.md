# راهنمای جامع خروجی گرفتن آنلاین، خودکار و رایگان اپلیکیشن (بدون نیاز به سیستم قوی)

این راهنما به شما آموزش می‌دهد که چطور بدون نیاز به داشتن کامپیوتر قدرتمند، نصب نرم‌افزارهای سنگین (مانند Android Studio یا Xcode) و بدون درگیر شدن با تنظیمات پیچیده محلی، فایل‌های نهایی نصب اپلیکیشن اندروید (`APK` / `AAB`) و حتی نسخه `iOS` را به صورت کاملاً آنلاین و رایگان بسازید.

---

## گام اول: آپلود پروژه روی گیت‌هاب (GitHub)

تمامی سرویس‌های ساخت ابری (Cloud Build) برای دسترسی به کدها و کامپایل آن‌ها نیاز دارند که پروژه شما روی یک مخزن آنلاین مانند گیت‌هاب قرار داشته باشد.

1. وارد سایت [GitHub](https://github.com) شده و یک حساب کاربری بسازید.
2. پروژه صادر شده از این پنل را دانلود کرده، از حالت فشرده خارج کنید.
3. یک مخزن جدید (Repository) با نام دلخواه (مثلاً `bazino-app`) بسازید و آن را روی حالت **Private** (شخصی) یا **Public** (عمومی) قرار دهید.
4. با استفاده از نرم‌افزار [GitHub Desktop](https://desktop.github.com) یا خط فرمان، پوشه پروژه (شامل تمام فایل‌ها و به خصوص پوشه `flutter_app`) را روی مخزن گیت‌هاب خود آپلود (Push) کنید.

---

## روش اول: ساخت خودکار با سرویس Codemagic (ساده‌ترین روش - پیشنهادی)

سرویس **Codemagic** یکی از بهترین و هماهنگ‌ترین پلتفرم‌های ابری مخصوص فلاتر است که به شما امکان می‌دهد در ماه تا **۵۰۰ دقیقه زمان بیلد رایگان** داشته باشید که برای کارهای شخصی و تست بسیار فراتر از نیاز است.

### مراحل راه‌اندازی در Codemagic:
1. وارد سایت [Codemagic.io](https://codemagic.io) شوید و با حساب کاربری گیت‌هاب خود ثبت‌نام کنید.
2. روی دکمه **Add Application** کلیک کنید.
3. مخزن گیت‌هاب خود (مثلاً `bazino-app`) را انتخاب کنید.
4. نوع پروژه را روی **Flutter App** تنظیم کنید.
5. وارد بخش تنظیمات بیلد (Build Configuration) شوید:
   * **Build Platform:** گزینه‌های **Android** و در صورت داشتن حساب توسعه‌دهنده اپل گزینه **iOS** را تیک بزنید.
   * **Build Triggers:** می‌توانید تنظیم کنید که با هر بار تغییر در کدها (Push)، خروجی جدید به صورت خودکار ساخته شود.
   * **Root directory:** آدرس را روی `flutter_app` تنظیم کنید تا سیستم بداند پوشه اصلی فلاتر کجاست.
6. در بخش **Build** نسخه فلاتر مورد نظر را انتخاب کرده و فرمت خروجی اندروید را روی **APK** (برای نصب مستقیم) یا **App Bundle** (برای گوگل پلی) بگذارید.
7. روی دکمه **Start Build** کلیک کنید!
8. پس از چند دقیقه، بیلد با موفقیت به پایان می‌رسد و لینک دانلود مستقیم فایل `.apk` برای شما ایمیل شده و در داشبورد نیز قابل دانلود خواهد بود.

---

## روش دوم: ساخت با استفاده از GitHub Actions (کاملاً رایگان و نامحدود)

سرویس **GitHub Actions** قابلیت اجرای کدهای زمان‌بندی شده و بیلد خودکار را مستقیماً درون خودِ گیت‌هاب به شما می‌دهد. این روش کاملاً رایگان است.

### مراحل راه‌اندازی:
۱. در پوشه اصلی پروژه فلاتر خود (یعنی داخل `/flutter_app/`)، یک پوشه به نام `.github` و درون آن پوشه‌ای به نام `workflows` بسازید.
۲. یک فایل جدید به نام `flutter-build.yml` در مسیر زیر ایجاد کنید:
`/flutter_app/.github/workflows/flutter-build.yml`

۳. کدهای زیر را به صورت کامل درون این فایل کپی کنید:

```yaml
name: Flutter Cloud Build (APK)

on:
  push:
    branches: [ "main" ] # هر زمان که به برانچ main کد بفرستید فعال می‌شود
  workflow_dispatch: # امکان اجرای دستی از داخل گیت‌هاب

jobs:
  build:
    runs-on: ubuntu-latest # استفاده از سرور رایگان لینوکس گیت‌هاب

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    - name: Set up Java Development Kit (JDK)
      uses: actions/setup-java@v3
      with:
        distribution: 'zulu'
        java-version: '17'

    - name: Set up Flutter
      uses: subosito/flutter-action@v2
      with:
        flutter-version: '3.19.x' # یا نسخه هماهنگ با پروژه شما
        channel: 'stable'

    - name: Get dependencies
      run: |
        cd flutter_app
        flutter pub get

    - name: Build APK
      run: |
        cd flutter_app
        flutter build apk --release

    - name: Upload APK Artifact
      uses: actions/upload-artifact@v3
      with:
        name: release-apk
        path: flutter_app/build/app/outputs/flutter-apk/app-release.apk
```

۴. این فایل را ذخیره کرده و روی گیت‌هاب خود آپلود (Push) کنید.
۵. اکنون در پنل گیت‌هاب پروژه خود به تب **Actions** بروید.
۶. گردش کار **Flutter Cloud Build (APK)** را مشاهده خواهید کرد. روی آن کلیک کرده و دکمه **Run workflow** را بزنید.
۷. سرورهای ابری گیت‌هاب روشن شده و پروژه شما را کامپایل می‌کنند. پس از اتمام (حدود ۳ الی ۵ دقیقه)، فایل آماده `app-release.apk` در انتهای همان صفحه برای دانلود مستقیم قرار می‌گیرد.

---

## مزایای کلیدی روش بیلد ابری:
* 🚀 **بدون نیاز به سخت‌افزار قوی:** حتی با یک تبلت یا گوشی ساده هم می‌توانید پروژه را ویرایش کرده و خروجی بگیرید!
* 🛠️ **بدون چالش‌های نصب نرم‌افزار:** نیاز به دانلود گیگابایت‌ها ابزار اندروید استودیو و درگیری با تحریم‌ها و فیلترینگ‌های دانلود ابزارهای گوگل ندارید.
* 🔄 **یکپارچگی و بروزرسانی خودکار:** هر زمان تغییری در کدهای فلاتر خود اعمال کنید، سیستم ابری بلافاصله نسخه جدید را ساخته و آماده می‌کند.
