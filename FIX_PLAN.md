# پلن جامع رفع مشکلات — بازینو پرو

**مبنا:** `E2E_TEST_REPORT.md` (تست سرتاسری ۱۴۰۵/۰۶/۱۰ با Chromium 149 روی سرور زنده)
**وضعیت:** پیش‌نویس — منتظر تأیید کاربر. **هیچ کد محصولی هنوز تغییر نکرده است.**

---

## ۰. اصول کار

1. **بدون scope creep.** هر batch فقط موارد اعلام‌شده‌ی خودش را دست می‌زند.
2. **هر batch با مرورگر واقعی تست می‌شود** (قاعده‌ی ۱۲): اجرای هارنس `tests/e2e-browser/`،
   اسکرین‌شات تمام‌صفحه، بازبینی چشمی، و مقایسه‌ی وضعیت جدول‌های SQLite قبل/بعد.
3. **معیار پذیرش عینی.** برای هر مورد یک شرط قابل اندازه‌گیری تعریف شده (نه «چشمی درست به‌نظر می‌رسد»).
4. بعد از هر batch: به‌روزرسانی `ISSUES_FOUND.md` + `SESSION_SUMMARY.md` + `ARCHITECTURE.md` (در صورت نیاز)،
   سپس commit و push روی `arena/01a05e95-bazino-gamenet-portal`، و zip تحویلی.
5. `npm run lint` (tsc --noEmit) و `npm test` قبل از هر commit اجرا می‌شود.

### کشف مهمی که پلن را ساده می‌کند

بک‌اند تقریباً **کامل و درست** نوشته شده است. مثلاً `POST /api/cafe/order` (خط ۱۸۴۰ `server.ts`):

* قیمت را **سمت سرور** از منوی واقعی محاسبه می‌کند (به `item.price` کلاینت اعتماد نمی‌کند)
* موجودی انبار را چک و سپس کم می‌کند
* کوپن را با `validateCouponServerSide` اعتبارسنجی و `recordCouponUsage` می‌کند
* امتیاز وفاداری را با شرح درست ثبت می‌کند
* سفارش را با `status: "Pending"` برای پنل ادمین ذخیره می‌کند

همین کیفیت در `POST /api/accessories/order` (۱۹۳۲)، `POST /api/tournaments/register` (۲۰۲۵) و
`POST /api/articles/:id/comment` (۲۰۷۵) هم وجود دارد. **پس Batch B عمدتاً «سیم‌کشی فرانت‌اند» است، نه نوشتن منطق جدید.**
الگوی مرجع هم در همین ریپو موجود است: `ReservationsTab.handleReserve` (خط ۱۶۹) که درست کار می‌کند.

---

## Batch A — امنیت و صحت داده 🔴

> بلاک‌کننده‌ی انتشار. تا وقتی این دو حل نشوند، سایت نباید روی دامنه‌ی عمومی بماند.

### A1 · نشت پروفایل کاربر به بازدیدکننده‌ی ناشناس

| | |
|---|---|
| **شاهد** | `curl http://localhost:3000/api/user` بدون توکن → `{"username":"Gamer_950231","email":"…","phone":"09129502311","loyaltyPoints":452}` |
| **ریشه** | `server.ts:611` — `const activeUsername = tokenUsername \|\| (await store.getSetting("activeUsername")) \|\| "Guest"` |
| **چرا هست** | یادگار نسخه‌ی تک‌کاربره‌ی قبلی. `register` (۶۹۱)، `login` (۷۳۴) و `install/setup` (۳۷۶۳) این تنظیم سراسری را می‌نویسند. |

**تغییر پیشنهادی**

1. در `getCurrentUser()` وابستگی به `activeUsername` **کاملاً حذف شود**: بدون `req.authUsername` همیشه `Guest`.
2. سه نقطه‌ی نوشتن `setSetting("activeUsername", …)` حذف شوند (کلید دیگر خواننده‌ای ندارد).
3. یک migration کوچک هنگام بوت: اگر ردیف `activeUsername` در `settings` هست، حذف شود (پاک‌سازی نصب‌های موجود).
4. `GET /api/user` برای مهمان `200` با `{username:"Guest", …}` برگرداند (رفتار فعلی فرانت‌اند
   در `App.tsx:252` همین را انتظار دارد: `if (r.username !== 'Guest') setUser(r) else setUser(null)`)، پس UI نمی‌شکند.

**ریسک:** پایین. **اما** باید چک شود که اپ Flutter و اپ دسکتاپ به این fallback تکیه نکرده باشند —
قبل از تغییر، `flutter_app/` و `desktop-app/` را برای فراخوانی `/api/user` بدون هدر `Authorization` grep می‌کنم.
اگر تکیه کرده باشند، آن‌ها هم باید توکن بفرستند (تغییر جداگانه، در گزارش اعلام می‌شود).

**معیار پذیرش**
* `curl /api/user` بدون توکن → `username === "Guest"` و **بدون هیچ ایمیل/تلفنی**.
* با توکن معتبر → پروفایل درست همان کاربر.
* در مرورگر: بازدیدکننده‌ی ناشناس دکمه‌ی «ورود» می‌بیند، نه نام کاربر و نه بنر «ورود به پنل مدیریت».

---

### A2 · رزرو/سفارش/تراکنشِ واقعی کاربر هرگز دیده نمی‌شود

| | |
|---|---|
| **شاهد** | رزرو در `reservation_logs` ثبت شد، ولی `GET /api/reservations` سه رکورد نمونه‌ی `r1,r2,r3` برگرداند |
| **ریشه** | `server.ts:90` — `resolveSampleList()` در حالت `sample` **همیشه** داده‌ی نمونه را برمی‌گرداند و دیتابیس را نادیده می‌گیرد |

**تحلیل:** این تابع در ۱۹ نقطه استفاده شده، ولی دو نوع کاملاً متفاوت داده را یکسان رفتار می‌کند:

* **کاتالوگ** (سیستم‌ها، منوی کافه، محصولات، تورنمنت‌ها، مقالات، اسلایدرها، اتاق‌های چت) —
  اینجا نمونه منطقی است: سایت روی نصب تازه خالی به‌نظر نرسد.
* **داده‌ی تراکنشی** (رزروها، تراکنش‌های امتیاز، کوپن‌ها) —
  اینجا نمونه **مخرب** است: داده‌ی واقعی کاربر را پنهان می‌کند.

**تغییر پیشنهادی**

1. تابع دوم اضافه شود، کنار تابع فعلی:
   ```ts
   /** داده‌ی تراکنشی: دیتابیس همیشه منبع حقیقت است. نمونه فقط وقتی نمایش داده می‌شود
    *  که هنوز هیچ رکورد واقعی وجود نداشته باشد (تا صفحه‌ی نصب تازه خالی نباشد). */
   export async function resolveTransactionalList<T>(dbRows: T[], sampleRows: T[]): Promise<T[]> {
     if (dbRows.length > 0) return dbRows;
     return (await getDataSourceMode()) === "sample" ? sampleRows : [];
   }
   ```
2. سه نقطه به تابع جدید سوییچ شوند: `900` (transactions)، `909` (coupons)، `1083` (reservations).
   **بقیه‌ی ۱۶ نقطه دست نمی‌خورند.**
3. لیست رزروها per-user شود: `GET /api/reservations` فقط رزروهای `req.authUsername` را برگرداند
   (پنل ادمین لیست کامل را از مسیر ادمینِ خودش می‌گیرد؛ اگر نمی‌گیرد، پارامتر `?all=1` با `requireAdmin` اضافه می‌شود).
   ردیف‌های نمونه `username: ''` دارند، پس برای کاربر واردشده به‌درستی فیلتر می‌شوند.

**ریسک:** متوسط — ممکن است داشبورد ادمین که «۳ سانس» را از همین endpoint می‌خواند تحت تأثیر قرار بگیرد.
قبل از تغییر، مصرف‌کننده‌های `/api/reservations` را در `AdminPanelTab.tsx` و `flutter_app/` بررسی می‌کنم.

**معیار پذیرش**
* کاربر تازه رزرو می‌کند → **همان رزرو** با QR خودش در «رزروهای فعال شما» ظاهر می‌شود.
* کاربر B رزروهای کاربر A را نمی‌بیند.
* روی نصب کاملاً خالی، صفحه‌ی رزرو همچنان با داده‌ی نمونه پر است (رگرسیون نشود).

---

## Batch B — سیم‌کشی فرانت‌اند به بک‌اندِ موجود 🔴

> چهار قابلیت که کاربر «موفقیت» می‌بیند ولی هیچ اتفاقی نمی‌افتد.

### B1 · سفارش کافه

`src/components/CafeTab.tsx:142` — `handleCheckout` فقط `onAddLoyaltyPoints()` را صدا می‌زند.

**تغییر:** بازنویسی دقیقاً بر اساس الگوی `ReservationsTab.handleReserve`:

```ts
const response = await fetch('/api/cafe/order', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json', ...authHeader() },
  body: JSON.stringify({ items: cart, couponCode: appliedCoupon?.code, tableNumber: systemNumber })
});
```
* در حالت موفق: پاسخ سرور (`user`, `transactions`, `cafeItems`, `order`) به بالا پاس داده شود تا
  امتیاز و موجودی منو در UI به‌روز شوند؛ توست موفقیت شامل **شماره‌ی سفارش واقعی** (`CF-xxxx`) باشد.
* در حالت خطا: پیام واقعی سرور نمایش داده شود (مثلاً «موجودی «قهوه» کافی نیست») — الان چنین حالتی اصلاً وجود ندارد.
* فراخوانی `onAddLoyaltyPoints` **حذف** شود چون سرور خودش امتیاز را ثبت می‌کند (وگرنه امتیاز دوبار حساب می‌شود).

### B2 · خرید فروشگاه
`src/components/ShopTab.tsx:143` — همان الگو با `POST /api/accessories/order`.

### B3 · ثبت‌نام تیم تورنمنت
`src/App.tsx:397` — `handleRegisterTeam` که الان فقط `addNotification('Registered successfully')` است:

```ts
const res = await fetch('/api/tournaments/register', { method:'POST', …, body: JSON.stringify({ tournamentId, team }) });
if (res.ok) { const d = await res.json(); setTournaments(d.tournaments); }
else addNotification(await errorText(res), 'error');
```
ضمناً پیام انگلیسی `'Registered successfully'` باید چندزبانه شود (سایت ۴ زبانه است).

### B4 · کامنت بلاگ
`src/App.tsx:401` — همان کار با `POST /api/articles/:id/comment`.

**ریسک:** پایین. بک‌اند آماده و تست‌شده است؛ فقط باید مراقب **دوبار حساب شدن امتیاز** بود.

**معیار پذیرش (سنجش مستقیم روی دیتابیس)**
```
cafe_orders   ≥ 1     (بعد از یک سفارش در مرورگر)
shop_orders   ≥ 1
tournaments   ← تیم جدید داخل ستون teams
articles      ← کامنت جدید داخل ستون comments
```
به‌علاوه در پنل ادمین: «سفارشات زنده بوفه» دیگر «No active cafe orders» نگوید و «کل درآمد» عدد واقعی نشان دهد.
و: بعد از `F5` تیم ثبت‌شده و کامنت همچنان دیده شوند.

---

## Batch C — دسترس‌پذیری و ناوبری 🟠

### C1 · منوی موبایل
`src/App.tsx:622` — `<nav className="hidden md:flex …">` بدون هیچ جایگزینی زیر ۷۶۸px.

**تغییر پیشنهادی: نوار ناوبری پایین (bottom nav)** — نه همبرگری. دلیل:
اپ‌های گیم‌نت روی موبایل تک‌دستی استفاده می‌شوند، هر ۶ تب آیکون دارند (`Home, Monitor, Coffee, ShoppingBag, Trophy, Award`)،
و همان آرایه‌ی موجود در خط ۶۲۳ بدون تغییر قابل استفاده است.

* `position: fixed; bottom: 0` با `pb-[env(safe-area-inset-bottom)]` (پروژه از قبل این را رعایت می‌کند)
* `md:hidden` تا روی دسکتاپ تکراری نشود
* `<main>` یک `pb-` معادل ارتفاع نوار بگیرد تا محتوا زیر آن پنهان نشود
* در تب `admin` و حالت `hub` مخفی بماند (مثل هدر فعلی)

**معیار پذیرش:** در ۳۹۰×۸۴۴ هر ۶ تب با کلیک قابل دسترسی؛ `scrollWidth === 390` (بدون overflow)؛
آخرین آیتم هر صفحه پشت نوار پنهان نشود؛ روی ۱۴۴۰px نوار دیده نشود.

### C2 · تب‌های بی‌ورودی (بلاگ، چت، C#، Flutter)

**تصمیم لازم از شما** — دو گزینه:

* **گزینه ۱ (پیشنهاد من):** «بلاگ» و «چت» به ناوبری اصلی اضافه شوند (هر دو محتوای واقعی و پنل ادمین دارند)،
  و `CsharpCodeViewer` / `FlutterCodeViewer` که ابزار نمایشی توسعه‌دهنده‌اند از `App.tsx` حذف شوند
  (پنل ادمین بخش «مهاجرت‌های EF Core» و «پرزنتیشن» را برای همان کار دارد). سود جانبی: کاهش حجم باندل.
* **گزینه ۲:** همه بمانند و چهار ورودی اضافه شود (شلوغ‌تر شدن ناوبری).

### C3 · بسته‌شدن مودال‌ها با Escape
یک هوک مشترک `useModalDismiss(onClose)` که `Escape` و کلیک روی backdrop را مدیریت کند،
و در `ThemeSelectorModal`، `VisualHelpGuide`، `AuthModal` و دیالوگ تأیید خروج استفاده شود.
همراهش `role="dialog"` + `aria-modal="true"` + focus trap ساده (دسترس‌پذیری، هزینه‌ی ناچیز).

---

## Batch D — درستی داده و نمایش 🟡

### D1 · شرح تراکنش‌ها `null` می‌شود
`src/App.tsx:380` — امضا `(points: number)` است ولی سه کامپوننت `(points, description)` صدا می‌زنند.
**تغییر:** `(points: number, description?: string)` و ارسال `description` در body.
> نکته: بعد از Batch B، `/api/user/points` دیگر برای کافه/فروشگاه/تورنمنت صدا زده نمی‌شود
> (سرور خودش تراکنش می‌سازد)، ولی این تابع همچنان مصرف‌کننده دارد و باید درست شود.

### D2 · نوع تراکنش `Bonus`
`src/components/LoyaltyProfileTab.tsx:340` — شرط دودویی `tx.type === 'Earned' ? … : 'خرج امتیاز'`
باعث می‌شود `Bonus` با `+100 PTS` برچسب «خرج امتیاز» بگیرد.
**تغییر:** نگاشت صریح `Earned | Bonus | Redeemed` + fallback بر اساس علامت `points`، هر چهار زبان.

### D3 · محلی‌سازی منابع خارجی
| منبع | راه‌حل |
|---|---|
| `api.qrserver.com` (QR رزرو و QR دانلود اپ) | تولید QR سمت کلاینت با کتابخانه‌ی سبک (`qrcode` روی npm، ~۱۰KB gzip، بدون شبکه) — **مهم‌ترین مورد این batch**، چون QR ورود به سالن یک قابلیت اصلی است |
| `fonts.googleapis.com` | self-host کردن Vazirmatn از پکیج npm `vazirmatn` + `font-display: swap`. سود جانبی: بهبود LCP/TBT در GTmetrix |
| `cdn.jsdelivr.net` (highlight.js در پنل ادمین) | استایل به‌صورت local import |
| `api.dicebear.com` (آواتار چت) | آواتار حرف‌اول با CSS، یا SVG محلی |
| `openstreetmap.org` (نقشه) | نگه داشتن با `loading="lazy"` + تصویر جایگزین در صورت خطا (نقشه ذاتاً خارجی است) |

### D4 · اصلاحات متنی و ظاهری کوچک
* `ReservationsTab.tsx:148` — `'Lütfen önce boş bir sistem یا koltuk seçin.'` → `veya`
* بنر راهنمای بخش «پرزنتیشن» در پنل ادمین متن «تنظیمات کلید‌ها» را نشان می‌دهد → عنوان درست
* دکمه‌ی «اعمال کد» در پنل رزرو دو خط می‌شکند → `whitespace-nowrap` / تنظیم عرض
* لاگ بوت سرور: `Instead of /public/xxx.jpg, use /xxx.jpg` — چند ارجاع اشتباه به `/public/` در سورس

---

## Batch E — پاک‌سازی و مستندسازی 🟢

* `JWT_SECRET`: هشدار بوت هست، ولی پیشنهاد می‌کنم در حالت `NODE_ENV=production` **بوت را متوقف کند**
  به‌جای استفاده از fallback ناامن.
* صفحه‌ی نصب (`InstallPage`) با `useState(true)` bypass شده — یا فعال و تست شود، یا رسماً حذف.
  **نیاز به تصمیم شما.**
* `PROJECT_STATUS_REPORT.md` و `E2E_TEST_REPORT.md` در `README.md` ایندکس شوند.
* افزودن `tests/e2e-browser/README.md` و یک `npm run test:e2e`.
* ناهماهنگی‌های سند که در گزارش وضعیت پیدا شده بود: `flutter_app/README.md` پیش‌فرض،
  ارجاع `server/GameNet.*` در `ARCHITECTURE.md`، workflow قدیمی در `CLOUD_BUILD_GUIDE.md`.

---

## ترتیب، تخمین و ریسک

| Batch | محتوا | تخمین | ریسک رگرسیون |
|---|---|---|---|
| **A** | A1 نشت پروفایل · A2 داده‌ی تراکنشی | ۱ جلسه | متوسط (باید مصرف‌کننده‌های Flutter/دسکتاپ چک شوند) |
| **B** | B1–B4 سیم‌کشی چهار قابلیت | ۱ جلسه | پایین |
| **C** | C1 منوی موبایل · C2 ناوبری تب‌ها · C3 Escape | ۱ جلسه | پایین |
| **D** | D1–D4 داده، نمایش، محلی‌سازی CDN | ۱–۲ جلسه | پایین (D3 نیاز به تست بصری دقیق QR دارد) |
| **E** | پاک‌سازی و اسناد | ۰٫۵ جلسه | ناچیز |

**چرا این ترتیب:** A و B هر دو «سایت دروغ می‌گوید» را رفع می‌کنند (کاربر موفقیت می‌بیند ولی داده‌ای نیست) —
تا این‌ها حل نشوند بقیه بی‌معنی است. C دسترسی موبایل را باز می‌کند که پیش‌نیاز تست واقعی کاربران است.
D و E صیقل‌اند.

---

## آنچه از شما لازم دارم

1. **تأیید کلیت پلن** یا تغییر ترتیب batchها.
2. **تصمیم C2:** بلاگ و چت به ناوبری اضافه شوند و تب‌های کد C#/Flutter حذف شوند (گزینه‌ی ۱)، یا همه بمانند (گزینه‌ی ۲)؟
3. **تصمیم E:** صفحه‌ی نصب فعال و تست شود، یا حذف شود؟
4. **تأیید A2:** آیا موافقید که رزروها per-user فیلتر شوند؟ (یعنی کاربر فقط رزروهای خودش را ببیند)
5. اجازه‌ی شروع: طبق قاعده‌ی پروژه با پیام **«شروع کن»** batch اول را آغاز می‌کنم.

## آنچه صادقانه اعلام می‌کنم

* اپ **Flutter** و اپ **دسکتاپ Electron** در سندباکس قابل اجرا نیستند؛ اگر Batch A روی قرارداد API اثر بگذارد،
  تغییر لازم در آن‌ها را **کورکورانه اعمال نمی‌کنم** — فقط گزارش می‌دهم که کجا باید دستی تست شود.
* تست‌های من روی **سرور dev محلی** بودند، نه بیلد production روی `bazino.pro`.
  بعد از هر batch پیشنهاد می‌کنم شما یک بار روی دامنه‌ی واقعی هم بررسی کنید.
* اعداد GTmetrix تا وقتی روی دامنه‌ی واقعی اندازه‌گیری نشوند قابل ادعا نیستند؛ D3 احتمالاً کمکشان می‌کند ولی تضمینی نمی‌دهم.
