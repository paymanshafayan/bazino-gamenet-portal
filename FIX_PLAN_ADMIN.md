# پلن رفع — پنل مدیریت: نوشتن‌هایی که دیده نمی‌شوند

**وضعیت:** پیش‌نویس — منتظر «شروع کن». **هیچ کد محصولی هنوز تغییر نکرده است.**
**منشأ:** تست بصری و عملیاتیِ کاملِ پنل مدیریت (۲۲ مرحله)، ۱۴۰۵/۰۶/۱۱.
تصاویر: `visual-testing/admin-full-2026-09-02/` · هارنس: `tests/e2e-browser/e2e-admin-full.mjs`

---

## ۰. آنچه تست شد و آنچه سالم بود

| فاز | نتیجه |
|---|---|
| باز کردن هر ۱۶ بخش | ✅ ۱۶/۱۶ بدون خطای JavaScript |
| بازبینی چشمی | ۱۵ بخش با چشم دیده شد (تصاویر پیوست). بخش «پرزنتیشن» عکس گرفته نشد — پایین توضیح داده شده |
| عملیات نوشتنی | ۵ عملیات واقعی اجرا شد |
| دسترسی ادمین | ✅ ادمین تراکنش‌های هر ۴ کاربر، همه‌ی رزروها و همه‌ی کوپن‌ها را می‌بیند (تفکیک per-user فقط برای مشتری اعمال می‌شود) |

عملیات نوشتنی که **درست کار کردند**: انتشار مقاله (`published: true`)، تغییر وضعیت سفارش کافه
(`Pending → Preparing`، توست: «وضعیت سفارش CF-4609 با موفقیت بروزرسانی شد»)، و سوییچ منبع داده.

---

## ۱. 🔴 نوشتن ادمین ذخیره می‌شود ولی هیچ‌جا دیده نمی‌شود

دو عملیات با توست موفقیت تمام شدند ولی رکورد در API ظاهر نشد:

```
write · add a gaming station → toast «سیستم گیمینگ جدید با موفقیت به سرور افزوده شد»  → created: false
write · create a chat room   → toast «اتاق گفتگو ایجاد شد»                              → created: false
```

در دیتابیس **واقعاً نوشته شده‌اند**:

```
systems in DB    : [{ id: "s1", name: "تست سیستم 56896" }]
chat_rooms in DB : [{ name: "Room56896" }]
```

ولی API داده‌ی نمونه برمی‌گرداند:

```
GET /api/systems     → سیستم شماره ۱…۵  (هیچ‌کدام «تست سیستم» نیست)
GET /api/chat/rooms  → ["عمومی (General)","CS2","FIFA 26","Dota 2","Valorant"]
```

**ریشه:** `resolveSampleList()` در حالت `sample` — که پیش‌فرض است — همیشه داده‌ی نمونه را برمی‌گرداند.
همان الگویی که قبلاً برای رزرو/تراکنش (`resolveTransactionalList`) و مقاله/تورنمنت (`resolveMergedList`)
رفع شد، ولی برای این پنج موجودیت باقی مانده است:

| موجودیت | خط در `server.ts` | وضعیت |
|---|---|---|
| سیستم‌ها | 1103 | ❌ نوشتن ادمین نامرئی |
| منوی کافه | 1964 | ❌ نوشتن ادمین نامرئی |
| محصولات فروشگاه | 2088 | ❌ نوشتن ادمین نامرئی |
| اتاق‌های چت | 870 | ❌ نوشتن ادمین نامرئی |
| اسلایدرها | 2980 | ❌ نوشتن ادمین نامرئی |
| مقالات / تورنمنت‌ها | 2148 / 2097 | ✅ قبلاً با `resolveMergedList` رفع شد |

**اثر:** صاحب گیم‌نت سیستم جدید سالن را ثبت می‌کند، پیام موفقیت می‌بیند، و آن سیستم نه در سایت
قابل رزرو است، نه در فهرست خودِ پنل ادمین. عملاً پنل مدیریت در پیکربندی پیش‌فرض «فقط خواندنی» است.

> نکته‌ی منصفانه: بخش «سفارشی‌سازی کلوپ» حالتِ داده را شفاف نشان می‌دهد و توضیح می‌دهد که در
> حالت نمونه همه‌چیز از داده‌ی آماده می‌آید. پس رفتار کاملاً پنهان نیست — ولی دکمه‌ی «ثبت کلاینت»
> در همان حالت، پیام **موفقیت** می‌دهد بدون هیچ هشداری. یا باید رکورد دیده شود، یا باید صادقانه
> گفته شود «ذخیره شد ولی تا وقتی حالت داده روی «دیتابیس» نرود نمایش داده نمی‌شود».

**تغییر پیشنهادی:** هر پنج مسیر به `resolveMergedList()` سوییچ شوند (ردیف دیتابیس روی هم‌شناسه‌اش
می‌نشیند، بقیه‌ی ردیف‌های نمونه سر جایشان می‌مانند). برای اتاق‌های چت که کلیدشان `name` است،
`resolveMergedList(rooms, SAMPLE_CHAT_ROOMS, 'name')` — و چون لیست رشته است نه شیء، یک شاخه‌ی
ساده‌ی «اتحاد بدون تکرار» لازم دارد.

---

## ۲. 🔴 شناسه‌ی رکورد جدید با شناسه‌ی داده‌ی نمونه تداخل می‌کند

```ts
const nextId = "s" + ((await store.countSystems()) + 1);   // server.ts:2353
```

جدول `systems` خالی بود → `countSystems() = 0` → شناسه‌ی `s1`.
اما داده‌ی نمونه از قبل `s1` تا `s5` دارد. نتیجه‌ی واقعی تست:

```
systems in DB: [{ "id": "s1", "name": "تست سیستم 56896" }]
```

یعنی به‌محض اینکه مورد ۱ رفع شود (یا ادمین حالت داده را روی «دیتابیس» بگذارد)، رکورد جدید
**جای «سیستم شماره ۱» را می‌گیرد**. بدتر: `reservation_logs` هایی که `systemId: "s1"` دارند
ناگهان به سیستم دیگری اشاره می‌کنند و QR و صورتحساب مشتری به ایستگاه اشتباه وصل می‌شود.

همین الگو در چهار جای دیگر هم هست: `c` برای کافه (2414)، `a` برای لوازم جانبی (2513)،
`t` برای تورنمنت (2607)، و `a` برای مقاله (2662) — که **`a` برای هر دوی لوازم جانبی و مقالات
استفاده شده** و خودش یک تداخل بالقوه‌ی دیگر است.

**تغییر پیشنهادی:** شناسه با پیشوند + رشته‌ی تصادفی تولید شود (مثل الگوی موجود برای سفارش‌ها:
`"CF-" + …`)، و قبل از درج، یکتایی نسبت به **دیتابیس و داده‌ی نمونه** بررسی شود:

```ts
async function nextEntityId(prefix: string, exists: (id: string) => Promise<boolean>) {
  for (let i = 0; i < 5; i += 1) {
    const id = `${prefix}-${randomBytes(3).toString("hex")}`;
    if (!(await exists(id))) return id;
  }
  throw new Error("could not allocate a unique id");
}
```

پیشوندها یکتا شوند: `sys`, `cafe`, `acc`, `trn`, `art` — تا `a` دوپهلو نماند.

---

## ۳. 🟡 بخش «لاگ‌های دیتابیس» متن کوئری را نمایش نمی‌دهد

در اسکرین‌شات `14-dblogs.webp` چهار ردیف لاگ دیده می‌شود، اما **برچسب نوع عملیات خالی است و
متن SQL اصلاً نمایش داده نمی‌شود**. علت یک ناهماهنگی نام فیلد است:

| سرور می‌نویسد (`dataProviders.ts:31`) | رابط کاربری می‌خواند (`AdminPanelTab.tsx:4021,4030`) |
|---|---|
| `type` | `log.operation` |
| `command` | `log.query` |

هر دو `undefined` می‌شوند، پس کل قابلیت — که برای عیب‌یابی ساخته شده — عملاً بی‌فایده است.

**تغییر پیشنهادی:** رابط کاربری به نام‌های واقعی سرور اصلاح شود (`log.type` و `log.command`)،
با پشتیبانی از هر دو نام تا اگر جای دیگری شکل قدیمی را می‌فرستد نشکند.

---

## ۴. اعلام صادقانه — چیزی که نتوانستم تست کنم

* **بخش «پرزنتیشن» عکس گرفته نشد.** بخش باز می‌شود و متنش درست است (assert شد)، ولی
  `page.screenshot` روی آن سه بار تایم‌اوت خورد چون صفحه یک منبع خارجی سنگین می‌خواهد.
  در sandbox قابل عکس‌برداری نیست؛ روی محیط واقعی باید دستی دیده شود.
* **آپلود فایل تست نشد:** نصب قالب ZIP، آپلود APK و آپلود تصویر — هیچ‌کدام اجرا نشدند.
* **حذف رکورد** (دکمه‌های «حذف» و «غیرفعال‌سازی») تست نشد؛ فقط ایجاد.
* `cdn.jsdelivr.net` هنوز در بخش مهاجرت‌ها درخواست می‌شود (استایل highlight.js). رنگ‌آمیزی کد
  بدون آن هم کار می‌کند، ولی یک درخواست شکست‌خورده در هر بازدید ثبت می‌شود.

---

## ۵. معیار پذیرش

1. ادمین سیستم جدید ثبت کند → بلافاصله در فهرست «سیستم‌های فعال سالن» **و** در تب رزرو سایت دیده شود.
2. همین برای آیتم کافه، محصول فروشگاه، اتاق چت و اسلایدر.
3. شناسه‌ی رکورد جدید با هیچ شناسه‌ی نمونه‌ای برابر نباشد؛ تست خودکار این را بررسی کند.
4. رزرو قدیمی با `systemId: "s1"` همچنان به «سیستم شماره ۱» اشاره کند، نه به رکورد تازه.
5. بخش «لاگ‌های دیتابیس» متن SQL و برچسب نوع عملیات را نشان دهد.
6. `tsc` تمیز · `npm test` سبز (+ تست‌های جدید) · جاروب ادمین ۱۶/۱۶ · سناریوی مرورگر ۲۴/۲۴.

---

اگر موافقید، با **«شروع کن»** اجرا می‌کنم.
