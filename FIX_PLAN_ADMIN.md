# پلن رفع — پنل مدیریت

> ## ⚠️ بازبینی پس از تذکر کاربر (۱۴۰۵/۰۶/۱۱)
>
> کاربر تذکر داد که سایت الان روی **حالت داده‌ی نمونه (sample)** است و اگر ایرادی صرفاً ناشی از
> این حالت باشد، ایراد محسوب نمی‌شود. هر سه مورد را دوباره **در حالت `database` تست کردم**.
> نتیجه:
>
> | مورد | فقط در حالت sample؟ | داوری بازبینی‌شده |
> |---|---|---|
> | E.19 — نامرئی‌بودن رکورد تازه | ✅ بله | **حق با کاربر است.** به ۱ مورد UX کوچک تنزل یافت (بخش ۱) |
> | E.20 — تداخل شناسه | ❌ خیر | **باگ واقعی، در حالت database هم می‌شکند** (بخش ۲) |
> | E.21 — خالی‌بودن لاگ دیتابیس | ❌ خیر | **باگ واقعی، مستقل از حالت داده** (بخش ۳) |

**وضعیت:** پیش‌نویس — منتظر «شروع کن». **هیچ کد محصولی هنوز تغییر نکرده است.**
**منشأ:** تست بصری و عملیاتیِ کاملِ پنل مدیریت (۲۲ مرحله)، ۱۴۰۵/۰۶/۱۱.
تصاویر: `visual-testing/admin-full-2026-09-02/` · هارنس: `tests/e2e-browser/e2e-admin-full.mjs`

---

## ۰. آنچه تست شد و آنچه سالم بود

| فاز | نتیجه |
|---|---|
| باز کردن هر ۱۶ بخش | ✅ ۱۶/۱۶ بدون خطای JavaScript |
| بازبینی چشمی | ۱۵ بخش با چشم دیده شد (تصاویر پیوست). بخش «پرزنتیشن» عکس گرفته نشد — پایین توضیح داده شده |
| عملیات نوشتنی | ۵ عملیات واقعی اجرا شد |
| دسترسی ادمین | ✅ ادمین تراکنش‌های هر ۴ کاربر، همه‌ی رزروها و همه‌ی کوپن‌ها را می‌بیند (تفکیک per-user فقط برای مشتری اعمال می‌شود) |

عملیات نوشتنی که **درست کار کردند**: انتشار مقاله (`published: true`)، تغییر وضعیت سفارش کافه
(`Pending → Preparing`، توست: «وضعیت سفارش CF-4609 با موفقیت بروزرسانی شد»)، و سوییچ منبع داده.

---

## ۱. 🟡 (تنزل‌یافته) پیام موفقیتِ بی‌قید در حالت نمونه

**داوری اولیه‌ی من اشتباه بود.** این مورد را به‌عنوان باگ بحرانی ثبت کرده بودم، در حالی که
رفتارِ توصیف‌شده‌ی حالت نمونه است. بررسی مجدد در حالت `database`:

```
POST /api/admin/systems ×۳  →  GET /api/systems  →  [('s1','سیستم A'), ('s2','سیستم B'), ('s3','سیستم C')]
```

یعنی **در حالت دیتابیس، نوشتن ادمین بلافاصله و درست دیده می‌شود**. بخش «سفارشی‌سازی کلوپ» هم
صراحتاً می‌گوید در حالت نمونه همه‌چیز از داده‌ی آماده می‌آید. پس نامرئی‌بودن رکورد، طراحی است نه نقص.

آنچه از این مورد **باقی می‌ماند** فقط یک نکته‌ی صداقتِ رابط کاربری است: دکمه‌ی «ثبت کلاینت» در
حالت نمونه پیام «با موفقیت به سرور افزوده شد» می‌دهد، بدون اینکه بگوید تا وقتی حالت داده روی
«دیتابیس» نرود این رکورد نمایش داده نمی‌شود.

**تغییر پیشنهادی (اختیاری و کوچک):** در حالت نمونه، متن توست تکمیل شود:
«… ذخیره شد. برای نمایش، حالت داده را روی «دیتابیس» بگذارید.»
تنها یک رشته و یک شرط؛ هیچ تغییری در منطق داده لازم نیست.

> **`resolveSampleList` دست نمی‌خورد.** پیشنهاد قبلی‌ام برای سوییچ‌کردن پنج مسیر به
> `resolveMergedList` **پس گرفته می‌شود** — با تذکر کاربر روشن شد که رفتار فعلی عمدی است.

## ۲. 🔴 تداخل شناسه — **در حالت `database` هم می‌شکند**

```ts
const nextId = "s" + ((await store.countSystems()) + 1);   // server.ts:2353
```

شناسه از روی **تعداد ردیف‌ها** ساخته می‌شود، نه از یک شمارنده‌ی یکتا. پس هر بار که رکوردی حذف
شود، شناسه‌ی بعدی با یک رکورد موجود برخورد می‌کند. بازتولید واقعی روی سرور زنده، **در حالت
`database`** (نه sample):

```
mode = database, جدول systems خالی
POST ×۳                → [('s1','سیستم A'), ('s2','سیستم B'), ('s3','سیستم C')]
DELETE /api/admin/systems/s2  → [('s1','سیستم A'), ('s3','سیستم C')]
POST (سیستم چهارم)     → count=2 → id="s3" → برخورد

  {"error":"SqliteError: UNIQUE constraint failed: systems.id"}

نتیجه: [('s1','سیستم A'), ('s3','سیستم C')]   ← سیستم جدید اصلاً ساخته نشد
```

**اثر:** صاحب گیم‌نت که یک ایستگاه را حذف کرده باشد، دیگر **نمی‌تواند هیچ ایستگاه جدیدی اضافه
کند** و فقط یک خطای انگلیسیِ خام SQL می‌بیند. این هیچ ربطی به حالت داده ندارد.

همین الگو در چهار جای دیگر هم هست: کافه (2414)، لوازم جانبی (2513)، تورنمنت (2607)، مقاله (2662).
ضمناً پیشوند `a` هم‌زمان برای **لوازم جانبی و مقالات** استفاده شده است.

نکته‌ی ثانویه: اولین رکوردِ ساخته‌شده در حالت دیتابیس شناسه‌ی `s1` می‌گیرد که با `s1` داده‌ی نمونه
یکی است؛ اگر رزروی از دوره‌ی حالت نمونه با `systemId: "s1"` باقی مانده باشد، بعد از سوییچ به
ایستگاه دیگری اشاره می‌کند.

**تغییر پیشنهادی:** شناسه با پیشوند یکتا + رشته‌ی تصادفی، و بررسی یکتایی پیش از درج:

```ts
async function nextEntityId(prefix: string, exists: (id: string) => Promise<boolean>) {
  for (let i = 0; i < 5; i += 1) {
    const id = `${prefix}-${randomBytes(3).toString("hex")}`;
    if (!(await exists(id))) return id;
  }
  throw new Error("could not allocate a unique id");
}
```

پیشوندها: `sys`, `cafe`, `acc`, `trn`, `art` — تا `a` دوپهلو نماند.
به‌علاوه خطای `UNIQUE constraint` هرگز خام به کاربر نشان داده نشود.

## ۳. 🟡 لاگ‌های دیتابیس — **مستقل از حالت داده**

در اسکرین‌شات `14-dblogs.webp` چهار ردیف لاگ دیده می‌شود، اما **برچسب نوع عملیات خالی است و
متن SQL اصلاً نمایش داده نمی‌شود**. علت یک ناهماهنگی نام فیلد است:

| سرور می‌نویسد (`dataProviders.ts:31`) | رابط کاربری می‌خواند (`AdminPanelTab.tsx:4021,4030`) |
|---|---|
| `type` | `log.operation` |
| `command` | `log.query` |

پاسخ واقعی سرور (گرفته‌شده با توکن ادمین، در حالت `database`):

```json
{ "id": "log-gykyab0", "provider": "SQLite", "type": "SQL",
  "command": "ALTER TABLE active_coupons ADD COLUMN ownerUsername", "timestamp": "۱۱:۲۲:۳۶" }
```

`log.query` و `log.operation` هر دو `undefined` می‌شوند، پس کل قابلیت — که برای عیب‌یابی ساخته
شده — در **هر دو حالت داده** بی‌فایده است.

**تغییر پیشنهادی:** رابط کاربری به نام‌های واقعی سرور اصلاح شود (`log.type` و `log.command`)،
با پشتیبانی از هر دو نام تا اگر جای دیگری شکل قدیمی را می‌فرستد نشکند.

---

## ۴. اعلام صادقانه — چیزی که نتوانستم تست کنم

* **بخش «پرزنتیشن» عکس گرفته نشد.** بخش باز می‌شود و متنش درست است (assert شد)، ولی
  `page.screenshot` روی آن سه بار تایم‌اوت خورد چون صفحه یک منبع خارجی سنگین می‌خواهد.
  در sandbox قابل عکس‌برداری نیست؛ روی محیط واقعی باید دستی دیده شود.
* **آپلود فایل تست نشد:** نصب قالب ZIP، آپلود APK و آپلود تصویر — هیچ‌کدام اجرا نشدند.
* **حذف رکورد** (دکمه‌های «حذف» و «غیرفعال‌سازی») تست نشد؛ فقط ایجاد.
* `cdn.jsdelivr.net` هنوز در بخش مهاجرت‌ها درخواست می‌شود (استایل highlight.js). رنگ‌آمیزی کد
  بدون آن هم کار می‌کند، ولی یک درخواست شکست‌خورده در هر بازدید ثبت می‌شود.

---

## ۵. معیار پذیرش

1. در حالت `database`: ساخت سه رکورد → حذف رکورد وسط → ساخت رکورد جدید **بدون خطا** انجام شود
   و هر چهار شناسه یکتا بمانند. (این دقیقاً همان سناریویی است که امروز می‌شکند.)
2. هیچ شناسه‌ی تازه‌ای با شناسه‌های داده‌ی نمونه (`s1..s5`, `c1..`, `a1..`, `t1..`) برابر نباشد.
3. پیشوند لوازم جانبی و مقالات از هم جدا باشد.
4. خطای خام SQL هرگز به کاربر نشان داده نشود؛ پیام فارسی قابل‌فهم جایگزین شود.
5. بخش «لاگ‌های دیتابیس» متن SQL و برچسب نوع عملیات را نشان دهد.
6. (اختیاری) در حالت نمونه، توستِ ثبت رکورد توضیح بدهد که برای دیدن آن باید حالت داده عوض شود.
7. `tsc` تمیز · `npm test` سبز (+ تست‌های جدید) · جاروب ادمین ۱۶/۱۶ · سناریوی مرورگر ۲۴/۲۴.

---

اگر موافقید، با **«شروع کن»** اجرا می‌کنم.
