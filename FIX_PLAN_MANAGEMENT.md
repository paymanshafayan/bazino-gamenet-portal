# پلن رفع — نرم‌افزار مدیریت گیم‌نت (Management App)

**وضعیت:** پیش‌نویس — منتظر «شروع کن». **هیچ کد محصولی هنوز تغییر نکرده است.**
**دامنه:** `Management App/Bazino` — اپ React/Vite که سرور اصلی آن را روی `/management-app/` سرو می‌کند.
تصاویر: `visual-testing/management-2026-09-02/`

---

## ۰. آنچه سالم بود

| سنجه | نتیجه |
|---|---|
| `npx tsc --noEmit` | ✅ بدون خطا |
| `npx vite build` | ✅ موفق (۲۲۸۵ ماژول، ۸۰۶KB → ۲۲۴KB gzip) |
| سرو شدن روی سرور زنده | ✅ `GET /management-app/` → ۲۰۰ |
| رندر فارسی/RTL | ✅ درست، بدون شکستگی چیدمان |
| هر ۶ زبانه | ✅ باز می‌شوند بدون خطای JavaScript |
| overflow افقی | ✅ ندارد |
| `/api/state` | ✅ وضعیت واقعاً روی سرور ذخیره می‌شود |

زبانه‌ها: ایستگاه‌های بازی (۸ دستگاه) · بوفه و انبار داری · اعضا و کیف پول ·
حسابداری و نمودارها · دسترسی اپراتورها · تنظیمات و تم‌ها.

---

## ۱. 🔴 اپ با ۳٫۳ فریم بر ثانیه کار می‌کند

اندازه‌گیری با `requestAnimationFrame` روی همان مرورگر و همان ماشین:

| | FPS | DOM nodes |
|---|---|---|
| **Management App** | **۳٫۳** | ۷۵۳ |
| سایت اصلی بازینو | ۶۰٫۳ | ۳۰۲ |

عارضه‌ی جانبی که اول به چشم آمد: گرفتن یک اسکرین‌شات ساده با CDP بین **۱۳ تا ۲۸ ثانیه** طول می‌کشد،
چون کامپوزیتور فرصت نمی‌کند فریم بدهد. یعنی این فقط «کندی محسوس» نیست؛ رابط کاربری عملاً
قفل است. برای اپراتوری که کل شیفتش با این صفحه کار می‌کند، این جدی‌ترین ایراد است.

### ریشه — یک حلقه‌ی هر-ثانیه که کل اپ را دوباره می‌سازد

`src/App.tsx:145` یک تایمر هر ثانیه دارد:

```ts
const timer = setInterval(() => {
  setStations((prevStations) => prevStations.map((st) => { ... }));   // ← همیشه آرایه‌ی جدید
}, 1000);
```

`.map()` **بدون قید و شرط** یک آرایه‌ی جدید می‌سازد — حتی وقتی هیچ ایستگاهی در حال بازی نیست و
هیچ مقداری عوض نشده. مرجع جدید یعنی `stations` تغییر کرده، و این افکت (`src/App.tsx:80-109`)
که `stations` در آرایه‌ی وابستگی‌اش هست، هر ثانیه اجرا می‌شود:

```ts
useEffect(() => {
  safeSetStorage('bazino_stations', stations);
  ... ۱۱ کلید localStorage با JSON.stringify ...
  fetch('/api/state', { method:'POST', body: JSON.stringify({stations, buffetItems, customers, invoices}) });
}, [stations, buffetItems, customers, tariffs, expenses, invoices, walletTransactions, operators, ...]);
```

### اندازه‌گیری واقعی — با اپ کاملاً بی‌کار و بدون هیچ کلیکی

```
POST /api/state در ۱۰ ثانیه سکون:  10 بار،  51,950 بایت  (میانگین 5,195 بایت هر بار)
POST /api/state در ۱۵ ثانیه سکون:  15 بار
localStorage: ۱۴,۶۷۶ کاراکتر در ۱۱ کلید — هر ثانیه بازنویسی می‌شود
بلاب ذخیره‌شده در جدول settings سرور: 5,191 کاراکتر
```

یعنی در هر ثانیه، به‌ازای **هر نمونه‌ی باز از اپ**:
* کل درخت React دوباره رندر می‌شود،
* ۱۱ بار `JSON.stringify` + نوشتن **همگام** در localStorage (مسدودکننده‌ی رشته‌ی اصلی)،
* یک POST پنج‌کیلوبایتی به سرور،
* و سرور همان بلاب را در SQLite بازنویسی می‌کند.

در یک شیفت هشت‌ساعته: **۲۸٬۸۰۰ درخواست** و حدود **۱۵۰ مگابایت** ترافیک و نوشتن در دیتابیس،
فقط برای اینکه هیچ اتفاقی نیفتاده باشد.

### تغییر پیشنهادی (سه لایه، هر کدام مستقل)

1. **تایمر فقط وقتی چیزی عوض شده state را به‌روز کند.** اگر هیچ ایستگاهی `PLAYING` و بدون
   pause نیست، همان `prevStations` برگردانده شود تا React اصلاً re-render نکند:
   ```ts
   setStations(prev => {
     let changed = false;
     const next = prev.map(st => { /* ... */ changed = true; return updated; });
     return changed ? next : prev;   // ← مرجع قبلی = بدون رندر
   });
   ```
2. **ذخیره‌سازی debounce شود.** به‌جای نوشتن در هر تغییر، با تأخیر ~۲ ثانیه و فقط وقتی
   محتوا واقعاً فرق کرده (مقایسه‌ی امضای JSON با آخرین نسخه‌ی ذخیره‌شده).
3. **تفکیک وابستگی‌ها.** ذخیره‌ی `tariffs`/`operators`/`theme` نباید به تیک ثانیه‌شمار گره بخورد؛
   افکت به چند افکت کوچک‌تر با وابستگی‌های خودشان شکسته شود.

> با ۱ و ۲، در حالت بی‌کار انتظار داریم POSTها به **صفر** برسند و FPS به محدوده‌ی طبیعی برگردد.
> معیار پذیرش دقیقاً همین را می‌سنجد.

---

## ۲. 🟠 مودال‌ها با Escape بسته نمی‌شوند

پس از باز کردن «شروع تایم بازی» و زدن Escape:

```
after Escape: {"overlays":1,"texts":["شروع بازی در ایستگاه 03 - PS5 Standard تعیین زمان، مبلغ پردا…"]}
```

مودال باز می‌ماند و چون تمام‌صفحه است، **هر کلیک بعدی را می‌بلعد**. در تست خودکار همین باعث شد
هفت مرحله‌ی بعدی (افزایش بوفه، تغییر تعرفه، جابه‌جایی، تسویه، ایستگاه جدید، مدیریت تعرفه‌ها،
راهنما) پشت سر هم تایم‌اوت بخورند.

این دقیقاً همان ایرادی است که در سایت اصلی به‌عنوان **E.7** رفع شد. آنجا هوک `useModalDismiss`
ساخته شد (`src/utils/useModalDismiss.ts`): بستن با Escape + قفل اسکرول پس‌زمینه.

**تغییر پیشنهادی:** همان هوک در Management App تکرار شود (این اپ ماژول‌هایش را با سایت شریک
نیست، پس یک کپی کوچک لازم دارد) و در هر ۹ مودال استفاده شود:
`StationModal` · `CheckoutModal` · `AddBuffetServicesModal` · `ChangeTariffModal` ·
`ManageTariffsModal` · `ManageStationModal` · `TransferStationModal` · `HardwareRelayModal` ·
`WebSyncModal` · `SettingsThemesModal` · `VisualHelpGuide`.

---

## ۳. 🟡 وابستگی به فونت گوگل در زمان اجرا

```
FAILED https://fonts.googleapis.com/css2?family=Vazirmatn:wght@300;400;500;600;700;800;900&display=swap
```

`Management App/Bazino/index.html` سه تگ به `fonts.googleapis.com` و `fonts.gstatic.com` دارد.
این تنها خطای console اپ است. برای گیم‌نتی در ایران یا قبرس شمالی که اینترنتش فیلتر یا قطع باشد،
فونت هرگز نمی‌آید و صفحه با فونت سیستمی رندر می‌شود — و بدتر، بارگذاری stylesheet رندر را بلاک می‌کند.

> این همان کاری است که قبلاً برای دو فایل پرزنتیشن انجام شد. سایت اصلی عمداً از پشته‌ی فونت
> سیستمی استفاده می‌کند (در `index.html` ریشه مستند شده) — همان رویکرد اینجا هم درست است.

**تغییر پیشنهادی:** حذف سه تگ خارجی و تکیه بر پشته‌ی فونت سیستمی، دقیقاً مثل سایت اصلی.

---

## ۴. 🟡 هارنس تست هم باید اصلاح شود

`tests/e2e-browser/e2e-management.mjs` نوشته شد ولی به‌خاطر موارد ۱ و ۲ فقط ۲۰ از ۲۸ مرحله را
توانست اجرا کند. بعد از رفع آن دو، هارنس هم اصلاح می‌شود:
* بستن مودال با دکمه‌ی ✕ واقعی به‌جای Escape (تا تست به رفتار درست وابسته نماند)،
* عکس‌برداری با CDP و تایم‌اوت بلندتر تا زمانی که مورد ۱ رفع شود.

---

## ۵. معیار پذیرش

1. **در حالت بی‌کار (هیچ ایستگاهی در حال بازی)، در ۱۵ ثانیه صفر بار `POST /api/state`.**
2. با یک ایستگاه فعال، حداکثر یک POST هر چند ثانیه (نه هر ثانیه) و فقط وقتی داده عوض شده.
3. FPS اپ در حالت بی‌کار **بالای ۵۰** (الان ۳٫۳).
4. Escape هر مودال را می‌بندد و کلیک بعدی کار می‌کند.
5. صفر درخواست به `fonts.googleapis.com` / `fonts.gstatic.com`.
6. `tsc --noEmit` و `vite build` همچنان سبز.
7. هارنس `e2e-management.mjs` هر ۲۸ مرحله را سبز رد کند، با اسکرین‌شات هر مودال برای بازبینی چشمی.
8. رفتار عملیاتی عوض نشود: ثانیه‌شمار سانس‌ها دقیق بماند، آلارم پایان وقت سر جایش، و
   وضعیت پس از رفرش بازیابی شود.

---

## ۶. آنچه هنوز تست نشده

* **جریان کامل تسویه** (پایان سانس → فاکتور → ثبت در حسابداری) به‌خاطر مورد ۲ اجرا نشد.
* **همگام‌سازی وب** (`/api/sync/*`) فقط تا باز شدن مودال تست شد؛ تست اتصال واقعی انجام نشد.
* **نسخه‌ی دسکتاپ standalone** (سرور محلی + SQLite خودش) در sandbox قابل اجرا نیست.
