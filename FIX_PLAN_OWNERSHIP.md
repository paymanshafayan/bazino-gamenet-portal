# پلن رفع — مالکیت تراکنش‌های امتیاز و کدهای تخفیف

**وضعیت:** پیش‌نویس — منتظر پیام «شروع کن». **هیچ کد محصولی هنوز تغییر نکرده است.**
**مبنا:** مورد باقی‌مانده‌ی گزارش قبلی + دو یافته‌ی جدید که حین بررسی همین موضوع پیدا شدند.

---

## ۱. مشکل اصلی — هر دو جدول سراسری‌اند

```sql
CREATE TABLE transactions   (id, points, description, type, date);                 -- بدون username
CREATE TABLE active_coupons (code, type, value, minOrder, expiry, expiryDate,
                             maxUsageCount, usageCount, isActive);                 -- بدون مالک
```

نتیجه‌ی عملی، که با curl تأیید شد:

* **تاریخچه‌ی امتیاز مشترک است.** `GET /api/transactions` همه‌ی ردیف‌ها را می‌دهد. علی در صفحه‌ی
  «باشگاه» می‌بیند: «امتیاز بابت رزرو سانس سیستم شماره ۱»، «خرید از بوفه»، «تبدیل ۱۵۰ امتیاز به
  کد تخفیف» — تراکنش‌های **رضا**. یعنی الگوی خرید و رفت‌وآمد هر مشتری برای بقیه قابل خواندن است.
* **کد تخفیف شخصی، عمومی است.** وقتی رضا ۱۰۰ امتیازش را به کوپن `LOYAL-4CITJ` تبدیل می‌کند،
  آن کد در `GET /api/coupons` برای همه ظاهر می‌شود. چون `maxUsageCount = 1` است، **اولین نفری که
  آن را ببیند خرجش می‌کند** و امتیاز رضا دود می‌شود. عملاً باشگاه مشتریان قابل بهره‌برداری است.

---

## ۲. 🔴 یافته‌ی جدید و جدی‌تر — ارزش کوپن را خودِ کلاینت تعیین می‌کند

این را حین همین بررسی پیدا کردم و در گزارش قبلی نبود:

```ts
// server.ts — POST /api/loyalty/redeem
const { points, couponValue, code } = req.body;          // ← هر سه از کلاینت
if (user.loyaltyPoints >= points) {                       // ← تنها اعتبارسنجی
  await store.addLoyaltyPointsToUser(user.username, -points);
  await store.createCoupon({ code, value: couponValue, minOrder: couponValue * 1.5, ... });
}
```

نرخ تبدیل (`۱ امتیاز = ۱۰۰ تومان`) فقط در **فرانت‌اند** است (`LoyaltyProfileTab.tsx:27`).
سرور هیچ‌وقت چک نمی‌کند که `couponValue` با `points` بخواند. پس:

```bash
# کاربری با ۱۰۰ امتیاز:
curl -X POST /api/loyalty/redeem -H "Authorization: Bearer <token>" \
     -d '{"points":1,"couponValue":50000000,"code":"FREE"}'
# → کوپن ۵۰ میلیون تومانی ساخته می‌شود، به قیمت ۱ امتیاز
```

ضمناً **`code` هم از کلاینت می‌آید**، پس می‌شود کدی ساخت که با یک کد تبلیغاتی موجود تداخل کند.

> این دقیقاً همان الگویی است که قبلاً برای رزرو و سفارش رفع شده بود («هرگز به قیمت ارسالی
> کلاینت اعتماد نکن») — ولی این یک مسیر از قلم افتاده بود.

---

## ۳. یافته‌ی سوم — مصرف کوپن اتمیک نیست

```ts
UPDATE active_coupons SET usageCount = usageCount + 1 WHERE code = ?;
UPDATE active_coupons SET isActive = 0 WHERE code = ? AND usageCount >= maxUsageCount;
```

اعتبارسنجی (`validateCouponServerSide`) و ثبت مصرف (`recordCouponUsage`) در یک تراکنش نیستند،
پس دو درخواست هم‌زمان می‌توانند یک کوپن یک‌بارمصرف را دو بار خرج کنند.

---

## ۴. طراحی پیشنهادی

### ۴٫۱ ستون `username` روی `transactions`

هر تراکنش به یک کاربر تعلق دارد. ردیف‌های قدیمی (`username` خالی) «میراث» حساب می‌شوند.

| مسیر | رفتار جدید |
|---|---|
| `GET /api/transactions` | مهمان → `[]` · کاربر → فقط تراکنش‌های خودش · ادمین → همه (برای حسابرسی) |
| هر جای `addTransaction` (۱۰ نقطه در `server.ts`) | `username` کاربرِ همان درخواست نوشته می‌شود |
| داده‌ی نمونه | همچنان وقتی هیچ تراکنش واقعی وجود ندارد نمایش داده می‌شود (`resolveTransactionalList` فعلی) |

### ۴٫۲ ستون `ownerUsername` روی `active_coupons`

اینجا نکته‌ی ظریفی هست: کوپن‌ها **دو نوع‌اند** و نباید یکسان رفتار شوند.

| نوع | مثال | مالک | چه کسی ببیند | چه کسی بتواند خرج کند |
|---|---|---|---|---|
| تبلیغاتی / عمومی | `BAZINO10`, `VIPGOLD`, `WELCOME` | `''` (بدون مالک) | همه | همه |
| شخصی (از تبدیل امتیاز) | `LOYAL-4CITJ` | `rezа` | فقط خودش | فقط خودش |

* `GET /api/coupons` → کوپن‌های عمومی + کوپن‌های شخصیِ همان کاربر. ادمین → همه.
* `validateCouponServerSide` علاوه بر انقضا و سقف مصرف، **مالکیت** را هم چک می‌کند:
  اگر کوپن مالک دارد و مالکش کاربر جاری نیست → «این کد متعلق به شما نیست.»
  این مهم است چون بدون آن، کسی که کد را حدس بزند یا از جای دیگری ببیند باز هم می‌تواند خرجش کند.

### ۴٫۳ محاسبه‌ی سمت سرور برای تبدیل امتیاز

```ts
const POINTS_TO_TOMAN = 100;          // تنها منبع حقیقت، در سرور
const MIN_REDEEM_POINTS = 100;

const points = Math.floor(Number(req.body.points));
if (!Number.isFinite(points) || points < MIN_REDEEM_POINTS) return 400;
if (user.loyaltyPoints < points) return 400;

const couponValue = points * POINTS_TO_TOMAN;                 // ← سرور، نه کلاینت
const code = `LOYAL-${crypto.randomBytes(4).toString('hex').toUpperCase()}`;  // ← سرور
// اگر کد تکراری بود، دوباره تولید شود (تا ۵ بار)
```

`couponValue` و `code` از بدنه‌ی درخواست **نادیده گرفته می‌شوند**. فرانت‌اند همان نرخ را برای
پیش‌نمایش نگه می‌دارد ولی کد تولید نمی‌کند؛ کد واقعی از پاسخ سرور می‌آید و در توست نمایش داده می‌شود.

### ۴٫۴ اتمیک‌کردن مصرف کوپن

`recordCouponUsage` به یک `UPDATE` شرطی تبدیل می‌شود که تعداد ردیف‌های تغییرکرده را برمی‌گرداند:

```sql
UPDATE active_coupons
   SET usageCount = usageCount + 1,
       isActive   = CASE WHEN usageCount + 1 >= maxUsageCount THEN 0 ELSE isActive END
 WHERE code = ? AND isActive = 1 AND usageCount < maxUsageCount;
```

اگر صفر ردیف تغییر کرد یعنی کوپن بین اعتبارسنجی و ثبت، تمام شده → تخفیف اعمال نمی‌شود.

---

## ۵. مهاجرت دیتابیس (هر سه provider)

ریپو از قبل الگوی مهاجرت افزایشی دارد و از همان استفاده می‌کنم — **هیچ جدولی drop نمی‌شود**:

| Provider | روش |
|---|---|
| SQLite | افزودن به آرایه‌ی `wanted` در `ensureColumns()` (`dataProviders.ts:303`) — `PRAGMA table_info` + `ALTER TABLE ADD COLUMN` |
| SQL Server | افزودن دو خط `IF COL_LENGTH(...) IS NULL ALTER TABLE ... ADD ...` کنار خطوط موجود (خط ۶۶۶) |
| MongoDB | بدون اسکیما؛ فقط فیلد جدید نوشته می‌شود و در خواندن `?? ''` می‌گیرد |

**ردیف‌های موجود:** `username` و `ownerUsername` خالی می‌مانند.
پیشنهاد من: ردیف‌های خالی **میراث** تلقی شوند — تراکنش بی‌مالک به هیچ کاربری نشان داده نمی‌شود
(فقط ادمین می‌بیند) و کوپن بی‌مالک عمومی حساب می‌شود (که برای `BAZINO10` و `WELCOME` دقیقاً درست است).

> ⚠️ عارضه‌ی جانبی که صادقانه اعلام می‌کنم: کوپن‌های `LOYAL-…` که **قبلاً** ساخته شده‌اند مالک
> ندارند، پس بعد از این تغییر عمومی می‌مانند. اگر ترجیح می‌دهید، می‌توانم همه‌ی کدهای با
> پیشوند `LOYAL-` را در مهاجرت غیرفعال کنم — **این نیاز به تصمیم شما دارد** (سؤال پایین صفحه).

---

## ۶. فهرست فایل‌هایی که تغییر می‌کنند

| فایل | تغییر |
|---|---|
| `server/dataProviders.ts` | دو ستون جدید در هر سه provider؛ امضای `addTransaction`, `listTransactions`, `createCoupon`, `listCoupons`, `recordCouponUsage` |
| `server.ts` | نوشتن `username` در ۱۰ نقطه‌ی `addTransaction`؛ فیلتر per-user در `GET /api/transactions` و `GET /api/coupons`؛ بازنویسی `/api/loyalty/redeem`؛ چک مالکیت در `validateCouponServerSide` |
| `server/sampleData.ts` | افزودن `username: ''` / `ownerUsername: ''` به ردیف‌های نمونه |
| `src/types/gamenet.ts` | فیلدهای اختیاری جدید روی `LoyaltyTx` و `DiscountCode` |
| `src/components/LoyaltyProfileTab.tsx` | حذف تولید کد سمت کلاینت؛ نمایش کد واقعی از پاسخ سرور |
| `src/App.tsx` | `handleRedeemPoints` فقط `points` می‌فرستد و پاسخ سرور را می‌نشاند |
| `tests/` | تست‌های جدید برای جداسازی per-user و رد کردن `couponValue` جعلی |

---

## ۷. ریسک

| ریسک | کاهش |
|---|---|
| اپ Flutter / دسکتاپ از `/api/transactions` یا `/api/coupons` استفاده کنند و لیست خالی بگیرند | قبل از تغییر `flutter_app/` و `Management App/` را grep می‌کنم؛ اگر توکن نمی‌فرستند، **کد آن‌ها را کورکورانه تغییر نمی‌دهم** و فقط گزارش می‌کنم کجا باید دستی تست شود |
| شکستن پنل ادمین که به لیست کامل نیاز دارد | ادمین صراحتاً همه‌ی ردیف‌ها را می‌گیرد (مثل کاری که برای رزروها شد) |
| رگرسیون در ۲۳۰ تست موجود | بعد از هر مرحله `npm test` |

---

## ۸. معیار پذیرش (عینی)

1. کاربر A و کاربر B هر کدام خرید کنند → هر کدام **فقط** تراکنش خودش را ببیند؛ ادمین هر دو را ببیند.
2. کاربر A امتیاز تبدیل کند → کد `LOYAL-…` در `GET /api/coupons` **کاربر B دیده نشود**.
3. کاربر B همان کد را دستی در سبد خرید وارد کند → رد شود با پیام «این کد متعلق به شما نیست».
4. `curl -d '{"points":1,"couponValue":50000000}'` → کوپن ساخته‌شده **۱۰۰ تومان** باشد، نه ۵۰ میلیون.
5. `curl -d '{"points":-500}'` یا `{"points":5}` → `400`.
6. کوپن یک‌بارمصرف در دو درخواست هم‌زمان → فقط یکی تخفیف بگیرد.
7. `tsc` تمیز · `npm test` سبز · سناریوی مرورگر ۲۴/۲۴ · پنل ادمین ۱۶/۱۶ · صفر خطای console.
8. روی دیتابیس موجود (بدون پاک‌کردن) سرور بالا بیاید و ستون‌ها با `ALTER TABLE` اضافه شوند.

---

## ۹. یک سؤال قبل از شروع

**کوپن‌های `LOYAL-…` که از قبل در دیتابیس هستند و مالک ندارند، چه شوند؟**

* **الف)** عمومی بمانند (ساده‌ترین؛ ولی هر کسی می‌تواند خرجشان کند)
* **ب)** در مهاجرت غیرفعال شوند (`isActive = 0`) — امن‌ترین، ولی اگر مشتری‌ای کد استفاده‌نشده داشته باشد از دست می‌رود
* **ج)** دست نخورند ولی در پنل ادمین با برچسب «بدون مالک (میراث)» نشان داده شوند تا خودتان تصمیم بگیرید

پیشنهاد من **ج** است: هیچ چیزی از مشتری گرفته نمی‌شود و شما دید کامل دارید.

اگر با پلن موافقید (و گزینه‌ی سؤال بالا را انتخاب کنید)، با **«شروع کن»** اجرا می‌کنم.

---

# ✅ نتیجه‌ی اجرا — ۱۴۰۵/۰۶/۱۱

پلن کامل اجرا شد. **تصمیم کاربر برای کوپن‌های قدیمی: گزینه‌ی «ب» (غیرفعال‌سازی).**

## تغییرات

| حوزه | تغییر |
|---|---|
| اسکیما | `transactions.username` و `active_coupons.ownerUsername` روی هر سه provider، با مهاجرت افزایشی (`ALTER TABLE ADD COLUMN`) — هیچ جدولی drop نشد |
| مهاجرت گزینه‌ی «ب» | `deactivateLegacyOwnerlessLoyaltyCoupons()` در بوت؛ کدهای `LOYAL-*` بی‌مالک `isActive = 0` می‌شوند و یک خط در لاگ ثبت می‌شود |
| `GET /api/transactions` | مهمان → `[]` · کاربر → فقط خودش · ادمین → همه |
| `GET /api/coupons` | کدهای عمومی + کدهای شخصیِ خودِ کاربر · ادمین → همه |
| `validateCouponServerSide` | چک مالکیت هنگام **استفاده** (نه فقط مخفی‌کردن از لیست) → `403` |
| `POST /api/loyalty/redeem` | `requireAuth`؛ `couponValue` و `code` از بدنه **نادیده گرفته می‌شوند** و سمت سرور محاسبه می‌شوند؛ حداقل ۱۰۰ امتیاز؛ کد با `randomBytes` و چک یکتایی |
| `recordCouponUsage` | یک `UPDATE` شرطی اتمیک که `boolean` برمی‌گرداند؛ سه فراخوان در صورت `false` پاسخ `409` می‌دهند |
| ۹ نقطه‌ی `addTransaction` | همه `username` صاحبِ همان درخواست را می‌نویسند |
| فرانت‌اند | `LoyaltyProfileTab` دیگر کد تولید نمی‌کند؛ کد و مبلغ از پاسخ سرور می‌آیند و دکمه هنگام ارسال قفل می‌شود |

## راستی‌آزمایی — هر ۸ معیار پذیرش

```
columns transactions:   id,points,description,type,date,username        ✅
columns active_coupons: …,isActive,ownerUsername                        ✅
لاگ بوت: Deactivated 1 legacy ownerless LOYAL-* coupon(s)               ✅ (گزینه‌ی ب)
legacy coupon now: LOYAL-SK6R9  isActive=0                              ✅

۱) anonymous /api/transactions            → []                          ✅
   alice → 2 tx، همه با username=alice_own                              ✅
   bob   → 1 tx، همه با username=bob_own                                ✅
۲) کد شخصی alice در لیست bob دیده می‌شود؟ → False                       ✅
۳) bob همان کد را دستی وارد می‌کند       → HTTP 403                     ✅
   alice خودش                            → HTTP 200                     ✅
۴) redeem با couponValue=50,000,000       → value=10000، code=LOYAL-…   ✅
۵) points=5 → 400 · points=-500 → 400                                   ✅
۶) مصرف اتمیک کوپن (UPDATE شرطی، پاسخ 409 در رقابت)                     ✅
۷) tsc تمیز · npm test 238/238 · مرورگر 24/24 · vite build موفق         ✅
۸) روی دیتابیس موجود بدون پاک‌کردن، ستون‌ها با ALTER TABLE اضافه شدند   ✅
```

۸ تست جدید در `tests/api.test.mts` (سوئیت ۳۱) اضافه شد. یک تست قدیمی که
`GET /api/transactions` را عمومی فرض می‌کرد اصلاح شد — همان تست، قرارداد ناامن قبلی را تثبیت می‌کرد.

## باقی‌مانده (اعلام صادقانه)

* کوپن `LOYAL-SK6R9` که قبلاً ساخته شده بود طبق تصمیم شما **غیرفعال شد**. اگر مشتری‌ای آن را
  استفاده نکرده باشد، از دست رفته است؛ در پنل ادمین قابل مشاهده و در صورت نیاز قابل بازفعال‌سازی است.
* **اپ Flutter این دو مسیر را صدا می‌زند** (`flutter_app/lib/models.dart:919` و `:932`) — ولی هر دو با
  `_authHeaders()` که هدر `Authorization: Bearer …` می‌گذارد (خط ۶۸۵). پس رفتار جدید برایش درست است:
  کاربر واردشده تراکنش‌ها و کوپن‌های خودش را می‌گیرد. تنها تفاوت این است که **کاربر مهمانِ اپ**
  به‌جای داده‌ی نمونه لیست خالی می‌بیند — که همان رفتار درست و موردنظر است.
  چون اپ در sandbox اجراشدنی نیست، این **تست‌نشده** باقی می‌ماند و باید دستی بررسی شود.
