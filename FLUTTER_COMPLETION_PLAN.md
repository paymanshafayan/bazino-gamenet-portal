# برنامهٔ تکمیل اپلیکیشن فلاتر بازینو — گزارش وضعیت و نقشهٔ راه

**تاریخ:** ۱۴۰۵/۰۶/۲۱ (2026-09-12) · **مرجع کد:** `flutter_app/` · **مبتنی بر:** بررسی کامل کد (۱۷ فایل دارت، ~۶,۷۰۰ خط)، تطبیق تک‌تک اندپوینت‌ها با `server.ts`، و گزارش‌های CI

---

## الف) وضعیت فعلی — چه چیزهایی هست و کار می‌کند

| بخش | وضعیت | جزئیات |
|---|---|---|
| ساختار اپ | ✅ | ۵ تب هاب: خانه / رزرو سیستم / کافه بوفه / فروشگاه / کلوپ-پروفایل + چت زنده، بلاگ، دستیار صوتی جارویس (۱,۱۷۰ خط، STT+TTS) |
| اتصال به سرور | ✅ | `api_config.dart` → `https://bazino.pro` — هر ۲۵ اندپوینتی که اپ صدا می‌زند روی سرور موجود و فعال است (تطبیق تک‌به‌تک شد) |
| چت زنده | ✅ | WebSocket واقعی `wss://bazino.pro/api/chat/ws` — همانendpoint سایت |
| رزرو + تمدید | ✅ | رزرو سیستم + کد تخفیف + تمدید سانس (`/api/reservations/extend`) |
| کافه/فروشگاه | ✅ | سفارش با سبد خرید |
| تورنمنت | ✅ | لیست + براکت + ثبت‌نام تیم (ولی بدون پرداخت — ببینید فاز ۱) |
| باشگاه وفاداری | ✅ | امتیاز، تراکنش‌ها، تبدیل امتیاز→کد تخفیف |
| بلاگ | ✅ | خواندن + کامنت |
| پیام‌ها | ✅ | صندوق ورودی `/api/messages` + علامت خوانده‌شده |
| اسلایدر موبایل | ✅ | درست تصویر عمودی `mobileImageUrl` (نسخهٔ ۳:۴) را ترجیح می‌دهد |
| CI | ✅ | `flutter-test.yml`: **analyze پاک، ۱۰ تست پاس، بیلد وب موفق (45MB)** روی Flutter 3.47.2؛ بیلد APK هم در `main.yml` روی main سبز است |
| طراحی | ✅/🟡 | ری‌دیزاین «Neon Glass» دو پاس روی اکثر صفحات؛ باقی‌مانده در فاز ۵ |

**زیرساخت سمت سرور که از قبل برای موبایل آماده است و فقط منتظر اپ است:**
- پنل آپلود APK ادمین (آپلود چانک‌شده + فعال‌سازی نسخه + لینک استور) → `/api/admin/mobile-app/upload-apk`
- دانلود APK عمومی → `/api/mobile-app/download` + QR → `/api/mobile-app/qr.png` + ویجت «دانلود اپ» روی سایت
- اسلایدر اپ با تولید خودکار نسخهٔ عمودی موبایل

---

## ب) شکاف‌ها — به ترتیب فاز

### فاز ۱ — هستهٔ پولی و رزرو (مهم‌ترین شکاف: اپ از اقتصاد جدید سایت جدا افتاده)

وب سایت الان جریان کامل «پرداخت و رزرو» دارد؛ اپ هنوز روی مسیرهای قدیمی (legacy) است:

| # | کار | شرح | اندپوینت‌های آمادهٔ سرور |
|---|---|---|---|
| ۱.۱ | 🚨 مهاجرت رزرو به checkout جدید | الان اپ از `/api/systems/reserve` قدیمی استفاده می‌کند = **رزرو رایگان و بی‌درنگ بدون بیعانه**. وب: انتخاب روش پرداخت، بیعانه، مهلت ۱۰ دقیقه‌ای، لغو و بازگشت وجه | `/api/payments/methods` · `/api/checkout/wallet` · `/api/checkout/credits` · `/api/checkout/onsite` |
| ۱.۲ | کیف پول کاربر | موجودی + گردش + درخواست شارژ — کاملاً در اپ غایب | `/api/me/wallet` |
| ۱.۳ | کردیت بازینو (BC) | پرداخت رزرو با کردیت + نمایش موجودی | `/api/checkout/credits` |
| ۱.۴ | سفارش‌های من | لیست سفارش‌های در انتظار پرداخت حضوری + لغو | `/api/me/orders` · `/api/checkout/onsite/:id/cancel` |
| ۱.۵ | رزروهای من | لیست رزروهای فعال/آینده + لغو (تمدید از قبل هست) | `/api/me/reservations` |
| ۱.۶ | ثبت‌نام تورنمنت با پرداخت | الان اپ تیم را **رایگان** ثبت می‌کند؛ وب هزینهٔ ثبت‌نام را از کیف پول/حضوری می‌گیرد | `/api/checkout/wallet` با kind=tournament |

### فاز ۲ — حساب کاربری کامل

| # | کار | اندپوینت‌های آماده |
|---|---|---|
| ۲.۱ | ویرایش پروفایل (نام نمایشی، ایمیل، تلفن) | `/api/me/profile` |
| ۲.۲ | آواتار (آپلود/حذف) | `/api/me/avatar` |
| ۲.۳ | تغییر رمز عبور | `/api/me/password` |
| ۲.۴ | تیکت پشتیبانی واقعی (گفتگو، وضعیت، بستن) — الان اپ فقط پیام یک‌طرفهٔ `support/request` قدیمی دارد | `/api/me/tickets` · `.../:id` · `.../:id/reply` · `.../:id/close` |
| ۲.۵ | تأیید شماره موبایل با OTP (مثل وب) — برای عضویت در کمپین‌های پیامکی و اعتماد حساب | `/api/auth/otp/...` (همان جریان وب) |
| ۲.۶ | کارت عضویت دیجیتال + QR ورود (وعدهٔ سند معرفی محصول: «اسکن QR برای شروع فوری بازی») | مدل Loyalty موجود؛ QR باید ساخته شود |

### فاز ۳ — اعلان‌ها

| # | کار | شرح |
|---|---|---|
| ۳.۱ | نوتیفیکیشن زندهٔ درون‌برنامه‌ای | اپ WS را فقط برای چت گوش می‌دهد؛ event های `notification` که سرور پخش می‌کند (پیام ادمین، وضعیت رزرو) نادیده گرفته می‌شوند — هندل + بانر درون‌بری |
| ۳.۲ | پوش واقعی (FCM) | وعدهٔ سند معرفی: «هشدار رزرو جدید/فاکتور». نیاز به firebase_messaging + کلید سرور. (برای اپ بازیکن کافی است؛ پوش اپ مدیریت موضوع جدا است) |

### فاز ۴ — انتشار (Release Readiness) — «اپ قابل نصب واقعی»

| # | کار | شرح |
|---|---|---|
| ۴.۱ | 🚨 تغییر `applicationId` | الان `com.example.bazino_app` (جای‌نگهدار فلاتر) — قبل از هر انتشار عمومی **حیاتی**؛ پیشنهاد: `pro.bazinopro.app` (اندروید + iOS یکسان) |
| ۴.۲ | 🚨 امضای release | `build.gradle.kts` الان release را با کلید **debug** امضا می‌کند (TODO در کد) → ساخت keystore + نگهداری در Secrets گیت‌هاب |
| ۴.۳ | برندینگ/نسخه | نام اندروید «Bazino Arena» ↔ برند «BAZINO PRO» ناهماهنگ؛ نسخه `1.0.0+1` نیاز به استراتژی ورژن‌گذاری |
| ۴.۴ | زنجیرهٔ توزیع خودکار | CI: بیلد APK امضاشده → آپلود خودکار به `/api/admin/mobile-app/upload-apk` پروداکشن → ویجت دانلود سایت و QR فوراً فعال می‌شوند. **همهٔ اجزای سرور این زنجیره از قبل ساخته شده و بلاتکلیف مانده‌اند** |
| ۴.۵ | انتشار نسخهٔ وب/PWA | بیلد وب سبز است؛ می‌تواند روی `bazino.pro/app` سرو شود (گزینهٔ بدون استور) |

### فاز ۵ — کیفیت و پاکسازی

| # | کار |
|---|---|
| ۵.۱ | حذف `home_screen.dart` — کد مردهٔ تأییدشده (هیچ‌جا import نمی‌شود؛ hub مستقل است) |
| ۵.۲ | زبان روسی/ترکی (سایت ۴ زبان دارد؛ اپ فقط fa/en) |
| ۵.۳ | بازتولید `pubspec.lock` از pub.dev — الان هر ۷۴ پکیج به میرور چینی `pub.flutter-io.cn` اشاره می‌کنند (زنجیرهٔ تأمین + شکنندگی) |
| ۵.۴ | ~~اصلاح `flutter-test.yml` — هنوز `set +e` دارد~~ ✅ رفع شد (استپ پایانی codes.txt را چک می‌کند؛ analyze/test/build قرمز → جاب قرمز) |
| ۵.۵ | ~~تکمیل ری‌دیزاین صفحات باقی‌مانده (messages، UI جارویس)~~ ✅ **بخش UI جارویس در جلسهٔ ۱۴۰۴/۰۶/۲۱ کامل شد** (فاز ۶ زیر را ببینید)؛ باقی: تکمیل ری‌دیزاین `messages_screen.dart` — ✅ **توقف کار بصری با دستور جدید کارفرما (۱۴۰۴/۰۶/۲۱) برداشته شد**؛ مرجع: قالب هاب سایت |
| ۵.۶ | گسترش تست‌ها — ✅ بخشی انجام شد (۱۰ → ۱۶ تست: هاب کنسول + جارویس گفتگومحور)؛ باقی: قرارداد API با سرور (fromJson ها)، تست ویجت فرم‌ها |
| ۵.۷ | بررسی حجم: بیلد وب 45MB — عمدتاً ویدیوی intro (4.4MB) + google_fonts؛ بهینه‌سازی راه‌اندازی |

---

## ه‍) فاز ۶ — جارویس گفتگومحور + قالب هاب (دستور جدید کارفرما، جلسهٔ ۱۴۰۴/۰۶/۲۱) — ✅ اجرا شد

دو دستور صریح کارفرما: (۱) جارویس = دستیار هوشمند اجراکنندهٔ دستورات کاربر، باید **مثل چت‌جی‌پی‌تی** گفتگومحور شود، کامل و تست شود؛ (۲) **توقف طراحی بصری برداشته شد** — قالبی **مشابه قالب هاب سایت** (مناسب موبایل، نه کپی دقیق) برای اپ ساخته شود.

### ۶.الف — جارویس گفتگومحور (سبک ChatGPT) ✅

| کار | وضعیت |
|---|---|
| بازطراحی کامل مودال جارویس: گفتگو قهرمان صفحه، آواتار فشرده در هدر (زنده، سه پوسته)، حباب‌های پیام ChatGPT-مانند (راست‌به‌چپ‌آگاه)، نشانگر «در حال تایپ» (سه نقطهٔ متحرک)، پیشنهادهای شروع فقط در ابتدای گفتگو، نوار ورودی pill (متن + میکروفن + ارسال گرادیانی)، پشتیبانی کیبورد (viewInsets) | ✅ `jarvis_assistant.dart` |
| نشان «عملیات واقعی انجام شد» (badge طلایی) زیر پیام‌های دارای اکشن (۱۵ نوع اکشن، fa/en) | ✅ |
| **حافظهٔ گفتگو:** اپ آخرین ۱۲ نوبت را با هر دستور می‌فرستد؛ سرور آن را به LLM می‌دهد (Gemini/Ollama/OpenAI-compatible) تا «همونو دوباره» کار کند — بدون session سمت سرور؛ سخت‌گیرانه sanitize (role سفیدلیست، سقف ۱۲×۶۰۰ کاراکتر، تزریق system ممکن نیست) | ✅ `server.ts` + اپ |
| **رفع ۳ باگ واقعی در مغز تشخیصی سرور:** ①«برو به کافه/چت» به‌جای ناوبری، سفارش پیتزا/پیام چت ثبت می‌کرد؛ ②«رزرومو لغو کن» و «توی فروشگاه هدست پیدا کن» (ترتیب کلمات) به chitchat می‌رفتند؛ ③خرید/جستجوی فروشگاه کلِ جمله را با نام کالا تطبیق می‌داد → «یک ماوس گیمینگ بخر» **بی‌صدا کیبورد می‌خرید**؛ حالا token-matching با نرمال‌سازی نیم‌فاصله/ماوس↔موس و امتیاز واژهٔ کامل>زیررشته | ✅ |
| **تست سرور (محلی، کاربر واقعی لاگین‌شده):** رزرو✅ سفارش کافه✅ تمدید (کسر امتیاز)✅ کیف پول✅ لغو✅ تیکت ادمین✅ ناوبری✅ تغییر زبان✅ جستجو/خرید فروشگاه✅ + guest-guards ✅ — همه PASS | ✅ |
| تست ویجت (جدید): خوش‌آمد+ورودی+پیشنهادها، پنهان‌شدن پیشنهادها بعد از رشد گفتگو، سه‌نقطهٔ تایپ، badge اکشن — با mock کانال‌های STT/TTS | ✅ |

**یادآوری:** گفتگوی آزادِ لحن‌دار نیاز به تنظیم provider از پنل ادمین دارد (`jarvis_ai_providers` — Gemini/Ollama/OpenAI-compatible از قبل پشتیبانی می‌شود و زنجیره fallback دارد). بدون provider، دستورات دقیق کار می‌کنند و chitchat پاسخ ثابت می‌گیرد.

### ۶.ب — قالب هاب سایت برای اپ (بازتاب موبایلی) ✅

مرجع استخراج‌شده از کد سایت: `HubLayout.tsx` (ارب مرکزی بنفش + دکمه‌های مداری شیشه‌ای با رنگ نئونی اختصاصی هر بخش + ذرات) و `ConsoleHubView.tsx` + `REFERENCE_IMAGE_INFO.md` (HUD بدون اسکرول، پالت سایان/طلایی/بنفش).

| کار | وضعیت |
|---|---|
| تب خانه → **هاب کنسول**: اسلایدر داینامیک سرور (فشرده، با قاب جایگزین) + ردیف ابزار (چت/بلاگ/پیام‌ها) + **هاب مداری**: ارب مرکزی جارویس (بنفش نئون، لمس = باز شدن گفتگو) + ۵ دکمهٔ مداری شیشه‌ای (رزرو/کافه/فروشگاه/مسابقات/باشگاه) با رنگ‌های همان بخش‌های قالب هاب سایت — بدون اسکرول | ✅ `hub_screen.dart` |
| ویجت‌های عمومی: `HubOrbButton` (دکمهٔ مداری شیشه‌ای) + `HubParticlesPainter` (ذرات نئونی ایستا — عمداً بدون تایمر تا تست‌ها و گوشی‌های ضعیف سالم بمانند) | ✅ `theme.dart` |
| **رفع باگ ناوبری:** تب «کلوپ/پروفایل» نوار پایین به‌اشتباه صفحهٔ تورنمنت را باز می‌کرد؛ حالا باشگاه/پروفایل را باز می‌کند و هایلایت تب درست است | ✅ |
| تست: ۵ دکمهٔ مداری + ارب + برچسب‌ها رندر می‌شوند؛ تب پروفایل → LoyaltyScreen (نه TournamentScreen) | ✅ |

باقی‌ماندهٔ بصری (اختیاری، جلسات بعد): تعمیم زبان هاب به صفحات داخلی با همان ویجت‌ها، ری‌دیزاین `messages_screen.dart`، و بازبینی نهایی با اسکرین‌شات گوشی واقعی.

---

## ج) پیشنهاد ترتیب اجرا

1. **فاز ۴ اول** (۱-۲ جلسه): چون ارزان است و نتیجهٔ فوری دارد — اولین APK واقعی قابل‌نصب که از سایت دانلود می‌شود؛ از همین امروز می‌شود زنجیرهٔ «کد → CI → سایت → گوشی کاربر» را بست و بعد هر قابلیتی که اضافه می‌شود فوراً به دست کاربر می‌رسد.
2. **فاز ۱ بعد** (بزرگ‌ترین ارزش محصول): بدون این، اپ با اقتصاد واقعی گیم‌نت (بیعانه/کیف پول) هم‌گام نیست.
3. **فاز ۲** (حساب کامل) → **فاز ۳** (اعلان) → **فاز ۵** (کیفیت) به موازات.

> به‌روزرسانی ۱۴۰۴/۰۶/۲۱: فاز ۶ (جارویس گفتگومحور + قالب هاب) به‌عنوان دستور فوری کارفرما اجرا و کامل شد. ممنوعیت کار بصری برداشته شده و مرجع بصری از این پس «قالب هاب سایت» است.

---

## د) محدودیت‌های محیط اجرا (برای برنامه‌ریزی جلسات)

- سندباکس ایجنت: Flutter SDK قابل دانلود نیست (storage.googleapis.com بسته) → اجرای `flutter analyze/test/build` فقط از طریق **GitHub Actions** (که از قبل سبز و پیکربندی شده). ورک‌فلوی `flutter-test.yml` روی همهٔ برنچ‌ها (paths: flutter_app/**) اجرا می‌شود و اگر analyze/test/build شکست بخورد قرمز می‌شود.
- تست دستی روی گوشی/امولاتور: سمت کارفرما (طبق `HANDOFF.md` محلی) یا از طریق APK خروجی CI.
- بینایی تصویری ایجنت هنوز فعال نیست؛ چون قالب هاب سایت به‌صورت کد در ریپو موجود است (`HubLayout.tsx`/`ConsoleHubView.tsx`/`REFERENCE_IMAGE_INFO.md`)، طراحی از روی کدِ مرجع انجام شد و نتیجه باید با اسکرین‌شات گوشی واقعی بازبینی شود.
- هر push به برنچ arena بلافاصله روی bazino.pro دیپلوی می‌شود (تنظیم کارفرما) → قبل از هر پوش، سرور باید لوکال تست شود (تغییرات فلاتر برای پروداکشن بی‌خطرند، تغییرات server.ts نه).
