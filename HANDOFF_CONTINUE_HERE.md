# BAZINO PRO — سند انتقال کامل (Handoff) برای ادامه‌ی کار در چت جدید

> این سند طوری نوشته شده که یک نشست تازه‌ی Claude، بدون هیچ حافظه‌ای از این گفتگو، بتونه دقیقاً از همینجا ادامه بده. لطفاً این فایل رو همراه فایل zip پروژه به چت جدید بدید و بگید «این سند رو بخون و طبق بخش "کار بعدی" ادامه بده».

---

## ۱. معرفی کلی پروژه

**BAZINO PRO** یک پلتفرم مدیریت گیم‌نت با چهار بخش:

| بخش | مسیر | نقش |
|---|---|---|
| سایت/اپ مشتری | ریشه‌ی پروژه (`src/`, `server.ts`) | React 19 + Vite + **Express (Node.js) — بک‌اند رسمی و تنها بک‌اند production** |
| نرم‌افزار مدیریت داخلی | `Management App/Bazino` | React جدا — نرم‌افزار صندوق/مدیریت گیم‌نت که همین الان روش کار می‌کنیم |
| اپ موبایل | `flutter_app` | Flutter — حالا به بک‌اند واقعی وصله (کار قبلی) |
| فیچر تولید کد C# | `server/GameNet.*` + کامپوننت‌های Viewer | فقط یک ویژگی نمایشی/مستندسازی، نه بک‌اند واقعی |

### تصمیمات معماری کلیدی (قبلاً گرفته و اجرا شده)
- **Node.js/Express = تنها بک‌اند رسمی.** پوشه‌ی `server/GameNet.*` (ASP.NET) صرفاً خروجی یک فیچر «نمایش کد» در خود اپ React هست، نه بک‌اند واقعی.
- سه دیتابیس (SQLite/SQL Server/MongoDB) از طریق `server/dataProviders.ts` **همگی واقعی** پیاده‌سازی شدن (نه شبیه‌سازی).
- احراز هویت **واقعی و per-user** با JWT (`jsonwebtoken`) — هر کاربر توکن مستقل داره، نه یک «کاربر فعال سراسری».
- Flutter کامل به بک‌اند واقعی وصل شده: لاگین واقعی، چت real-time (WebSocket)، رزرو/سفارش/تورنومنت واقعی، دستیار هوشمند «جارویس» با اقدام‌های واقعی روی سرور.

---

## ۲. کارهای قبلاً انجام‌شده (این بخش‌ها کامل و پایدارن، نیازی به کار مجدد ندارن)

1. بررسی کامل اولیه‌ی پروژه و شناسایی مشکلات (فایل `ISSUES_FOUND.md` در ریشه‌ی پروژه)
2. بازنویسی کامل `server/dataProviders.ts`: سه دیتابیس واقعی + هش پسورد bcrypt
3. بازنویسی کامل `server.ts`: تمام endpoint ها روی دیتابیس واقعی، هش پسورد، محاسبه‌ی سرور-محور قیمت/امتیاز/تخفیف، چک هم‌پوشانی رزرو
4. کد تخفیف با انقضای واقعی و سقف تعداد استفاده
5. برندینگ Flutter اصلاح شد (`CyberNexus` → `Bazino`)
6. بهینه‌سازی تصاویر (حذف ~۴۵ مگ فایل بلااستفاده، فشرده‌سازی بقیه) — دو فایل خراب `background.png`/`bg.png` با نسخه‌های بهینه (JPG 18KB / 276KB) جایگزین شدند
7. دستیار هوشمند «جارویس» در Flutter کامل واقعی شد (NLU با Gemini + fallback، اقدام‌های واقعی: سفارش کافه، تمدید رزرو، تماس ادمین، پیام چت)
8. احراز هویت واقعی JWT per-user در بک‌اند + اتصال کامل اپ فلاتر (login/register واقعی، همه‌ی لیست‌ها از API واقعی، چت real-time با WebSocket، دکمه‌ی logout)
9. سند معماری (`ARCHITECTURE.md`) بازنویسی و به‌روز نگه داشته شده
10. بازطراحی کامل سیستم قالب‌بندی: هر قالب فایل CSS مجزا (`src/themes/`)، پوشش تمام صفحات، نصب قالب از ZIP (فرمت: theme.json + theme.css + assets/ + theme.js اختیاری)، پوشه‌ی اختصاصی هر قالب روی سرور (`server/themeStore.ts`)، SDK کامپوننت قالب (`src/themeSdk/`)
11. منبع داده نمونه/دیتابیس (`server/sampleData.ts` + سوییچ از پنل ادمین، پیش‌فرض: نمونه)
12. قانون لوگوی سایت مادر (آدرس استاندارد `/logo.png` + محافظ CSS `brand-logo-guard`)
13. حذف کامل برندینگ Google AI Studio از کل پروژه
14. ورک‌فلو CI (`ci/build-test.workflow.yml` — تایپ‌چک، تست قالب‌ها، بیلد فرانت/بک، بوت + smoke تست)
15. بهینه‌سازی عملکرد بر اساس گزارش GTmetrix (کد اسپلیتینگ، کش immutable، lazy loading، فونت‌های link، WebP)

فایل‌های مرجع مفید در ریشه‌ی پروژه: `ARCHITECTURE.md`, `ISSUES_FOUND.md`, `SESSION_SUMMARY.md` (این‌ها تاریخچه‌ی کامل تغییرات قبلی رو دارن).

---

## ۳. کار فعلی (همینه که الان روش کار می‌کنیم)

**وضعیت: جلسات اخیر کامل شدند** — سیستم قالب‌بندی جدید، منبع داده نمونه/دیتابیس، قانون لوگو، حذف برندینگ AI Studio، CI و بهینه‌سازی GTmetrix همگی انجام و push شده‌اند. اسناد (`ARCHITECTURE.md`, `SESSION_SUMMARY.md`, `README.md`) به‌روز هستند.

پروژه‌ی اصلی (سایت + پنل ادمین) در وضعیت پایدار است. کارهای قبلی در `Management App/Bazino` (۲۸ مورد GameCenter.md) طبق تاریخچه‌ی SESSION_SUMMARY تکمیل شده‌اند.

اگر جلسه‌ی جدیدی شروع شد، اول `SESSION_SUMMARY.md` بخش‌های ۳۳ تا ۳۹ و `src/themes/README.md` را بخوان — خلاصه‌ی کامل تغییرات اخیر آنجاست.

(جدول کامل وضعیت ۲۸ مورد GameCenter.md در ادامه‌ی همین بخش آمده است؛ برای جزئیات هر مورد `SESSION_SUMMARY.md` را ببینید.)

---

### جدول کامل وضعیت ۲۸ مورد (از فایل GameCenter.md)

| # | ویژگی | وضعیت قبل از این نشست | اقدام |
|---|---|---|---|
| ۱ | مدت بازی بر اساس مبلغ پرداختی | ✅ از قبل درست بود | — |
| ۲ | صدای هشدار اتمام بازی | ✅ از قبل درست بود | — |
| ۳ | تکرار زنگ و تغییر صدا | 🔴 `repeatIntervalSeconds` تعریف شده ولی استفاده نمی‌شد | ⏳ **هنوز رفع نشده** |
| ۴ | چراغ‌های هشدار | ✅ از قبل درست بود (`StationCard.tsx`) | — |
| ۵ | تعرفه‌ی ساعات خاص روز | 🔴 فقط تزئینی، اعمال نمی‌شد | ✅ **رفع شد** (این نشست) |
| ۶ | خدمات ویژه با نام/مبلغ دلخواه | ✅ از قبل درست بود | — |
| ۷ | حسابداری موجودی بوفه | ✅ از قبل درست بود | — |
| ۸ | محاسبه‌ی سود بوفه | ✅ از قبل درست بود | — |
| ۹ | تغییر تعرفه حین بازی | 🔴 نرخ‌ها ترکیب نمی‌شد (باگ مالی) | ✅ **رفع شد** |
| ۱۰ | جابه‌جایی صورت‌حساب بین ایستگاه‌ها | ✅ از قبل درست بود | — |
| ۱۱ | باز/بسته کردن تایم مشتری | ✅ از قبل درست بود (حالت OPEN) | — |
| ۱۲ | ۲۰ تم گرافیکی | 🔴 تم‌ها تعریف شدن ولی هیچ‌جا اعمال نمی‌شن | ⏳ **هنوز رفع نشده** |
| ۱۳ | طراحی گرافیکی کلوپی | ✅ کیفی/قابل قبول | — |
| ۱۴ | کیف پول و شارژ مشتری | 🔴 باگ محاسبه + پرداخت کیف‌پول کسر نمی‌شد | ✅ **رفع شد** |
| ۱۵ | محدودیت دسترسی اپراتور | ✅ از قبل درست بود | — |
| ۱۶ | رده‌بندی و تخفیف بر اساس ساعت بازی | 🔴 تخفیف کار می‌کرد ولی رتبه هیچ‌وقت خودکار ارتقا نمی‌یافت | ✅ **رفع شد** |
| ۱۷ | تولد مشتری و تخفیف | 🔴 فقط هاردکد، هیچ‌وقت از تاریخ واقعی محاسبه نمی‌شد | 🔧 **در حال انجام — نیمه‌کاره متوقف شد** |
| ۱۸ | بدهکاری/بستانکاری کیف‌پول | 🔴 همون باگ کیف‌پول | ✅ **رفع شد** |
| ۱۹،۲۰ | پرداخت قبل/بعد بازی | ✅ از قبل درست بود | — |
| ۲۱ | رند کردن مبلغ | ✅ از قبل درست بود | — |
| ۲۲ | تفکیک نقد/کارتخوان | ✅ از قبل درست بود | — |
| ۲۳ | پشتیبان‌گیری خودکار روزانه | 🔴 فقط دستی، خودکار وجود نداشت | ⏳ **هنوز رفع نشده** |
| ۲۴ | ثبت هزینه‌ها | ✅ از قبل درست بود | — |
| ۲۵ | نمودار ستونی ماهانه | 🔴 کاملاً فیک/هاردکد | ✅ **رفع شد** |
| ۲۶ | مقایسه‌ی دو ماه | 🔴 کاملاً فیک/هاردکد | ✅ **رفع شد** |
| ۲۷ | ساعت کارکرد ایستگاه | 🔴 هیچ‌وقت روزانه ریست نمی‌شد | ⏳ **هنوز رفع نشده** |
| ۲۸ | حفظ اطلاعات هنگام قطع برق | 🔴 `/api/state` روی سرور اصلاً وجود نداره (fetch سایلنت fail می‌کنه) | ⏳ **هنوز رفع نشده** |

---

## ۴. دقیقاً کجا متوقف شدیم (خیلی مهم — این بخش رو دقیق بخون)

داشتم مورد ۱۷ (تولد مشتری) رو رفع می‌کردم. کارهایی که **تا همین لحظه انجام شده**:

1. در `Management App/Bazino/src/utils/formatters.ts` تابع `getBirthdayFlags(birthDate, now)` اضافه شد که از روی تاریخ تولد واقعی، `isBirthdayToday`/`isBirthdayThisMonth` رو صحیح محاسبه می‌کنه. ✅ کامل و آماده استفاده.
2. در `Management App/Bazino/src/App.tsx`:
   - `useMemo` به import های React اضافه شد ✅
   - `getBirthdayFlags` به import از `./utils/formatters` اضافه شد ✅
   - **ولی هنوز:** یک `customersWithBirthdayFlags` با `useMemo` ساخته نشده، و مصرف‌کننده‌ها (خط‌های زیر، شماره‌خط‌ها ممکنه بعد از ادیت‌های بعدی کمی جابه‌جا شده باشن، پس با `grep` پیدا کن) هنوز به `customers` خام اشاره می‌کنن نه نسخه‌ی hydrate شده:
     - `const birthdayCountToday = customers.filter((c) => c.isBirthdayToday).length;` ← باید از نسخه‌ی hydrate شده استفاده کنه
     - `customers={customers}` در پراپ‌های `StationModal`, `CheckoutModal`, `CustomerManagement` (۳ جا) ← باید `customersWithBirthdayFlags` بشه (نه در `setCustomers`/جاهایی که مستقیم state رو می‌نویسن — اونا باید همون `customers` خام رو نگه دارن)

### کار بعدی دقیق (برای تکمیل مورد ۱۷):
```tsx
// بلافاصله بعد از تعریف state خام customers، این رو اضافه کن:
const customersWithBirthdayFlags = useMemo(() => {
  const now = new Date();
  return customers.map((c) => ({ ...c, ...getBirthdayFlags(c.birthDate, now) }));
}, [customers]);
```
بعد همه‌جایی که `customers` برای **نمایش/محاسبه** (نه نوشتن state) استفاده می‌شه، با `customersWithBirthdayFlags` جایگزین کن. مخصوصاً:
- `birthdayCountToday`
- پراپ `customers` که به `CheckoutModal` می‌ره (برای تخفیف تولد در لحظه‌ی تسویه)
- پراپ `customers` که به `CustomerManagement` می‌ره (برای نمایش لیست/بج تولد)
- پراپ `customers` که به `StationModal` می‌ره (اگه اونجا هم تخفیف/بج نشون داده می‌شه)

**نکته:** `CheckoutModal.tsx` از قبل منطق تخفیف تولد رو درست پیاده کرده بود (`customer.isBirthdayToday` رو چک می‌کنه) — فقط چون این فیلد هیچ‌وقت واقعی محاسبه نمی‌شد، عملاً کار نمی‌کرد. با این تغییر، خودش درست کار می‌کنه، نیازی به تغییر منطق تخفیف نیست.

همچنین: در `handleCreateCustomer`/`onAddCustomer` (در `CustomerManagement.tsx` و `App.tsx`) هیچ نیازی نیست این فیلدها رو دستی ست کنی — چون حالا هر رندر از روی `birthDate` واقعی محاسبه می‌شن.

---

## ۵. باقی موارد رفع‌نشده (بعد از تکمیل مورد ۱۷، به همین ترتیب پیش برو)

### مورد ۳ — تکرار آلارم
در `App.tsx`، تایمر اصلی (`useEffect` که هر ثانیه `elapsedSeconds` رو آپدیت می‌کنه) وقتی وضعیت به `FINISHED` می‌رسه فقط یک‌بار `playAlarmSound(...)` صدا می‌زنه. باید از `soundConfig.repeatIntervalSeconds` استفاده کنه تا تا وقتی ایستگاه در وضعیت FINISHED هست (چک‌اوت نشده)، آلارم هر N ثانیه دوباره پخش بشه. با `grep -n "FINISHED" App.tsx` و `grep -n "playAlarmSound" App.tsx` محل دقیق رو پیدا کن.

### مورد ۱۲ — اعمال واقعی تم‌های گرافیکی
`currentTheme` (state) داره ولی `currentTheme.primaryColor`/`darkBg`/... هیچ‌جای دیگه‌ی اپ استفاده نمی‌شه (فقط خود `SettingsThemesModal.tsx` و شاید `Header.tsx`). راه‌حل پیشنهادی: موقع تغییر `currentTheme`، مقادیر رنگی رو به‌صورت CSS Custom Properties روی `document.documentElement.style.setProperty('--color-primary', ...)` ست کن (در یک `useEffect`)، و کلاس‌های Tailwind هاردکد (`amber-500`, `zinc-900` و...) رو در کامپوننت‌های اصلی (حداقل `StationCard`, `Header`, دکمه‌های اصلی) با ارجاع به این CSS variable ها جایگزین کن. این کار می‌تونه حجیم باشه (چون کلاس‌های hardcode زیادن) — شاید بهتر باشه scope رو با کاربر هماهنگ کنی: آیا همه‌ی اپ باید دینامیک بشه یا فقط رنگ‌های اصلی (دکمه‌ها، هدر، نوار وضعیت)؟

### مورد ۲۳ — پشتیبان‌گیری خودکار روزانه
`handleExportBackupJSON` از قبل هست (دستی). باید یک `useEffect` اضافه بشه که چک کنه اگه `backupSettings.autoDailyBackup === true` و از آخرین بک‌آپ ذخیره‌شده (یک timestamp جدید توی `backupSettings` یا `localStorage` نگه دار) بیش از ۲۴ ساعت گذشته، خودکار `handleExportBackupJSON` رو صدا بزنه (یا حداقل یک کپی JSON رو در `localStorage` با کلید تاریخ‌دار ذخیره کنه، چون دانلود خودکار فایل توسط مرورگر معمولاً بدون تعامل کاربر مسدود می‌شه — این نکته رو با کاربر در میان بذار).

### مورد ۲۷ — ریست روزانه‌ی ساعت کارکرد ایستگاه
`totalServiceHoursToday` باید هر روز صفر بشه. راه‌حل: یک `lastResetDate` (رشته‌ی YYYY-MM-DD) رو یا در سطح هر ایستگاه یا یک state سراسری نگه دار؛ در یک `useEffect` (یا همون تایمر ثانیه‌ای موجود) چک کن اگه تاریخ امروز با `lastResetDate` فرق داره، همه‌ی `station.totalServiceHoursToday` رو صفر کن و `lastResetDate` رو آپدیت کن.

### مورد ۲۸ — اتصال واقعی `/api/state`
`App.tsx` به `/api/state` (که وجود نداره) POST می‌کنه. باید:
1. یک endpoint واقعی `POST /api/state` و `GET /api/state` به **`server.ts`** (بک‌اند اصلی سایت، در ریشه‌ی پروژه) اضافه بشه که یک بلوب JSON (کل state نرم‌افزار مدیریت) رو ذخیره/بازیابی کنه (می‌تونه در دیتابیس فعلی به‌عنوان یک setting ذخیره بشه، شبیه به `getSetting`/`setSetting` که از قبل توی `dataProviders.ts` هست).
2. مطمئن شو که Management App واقعاً به آدرس درست سرور (نه فرضی) وصل می‌شه — احتمالاً باید یه `api_config` مشابه چیزی که در Flutter ساختیم اونجا هم اضافه بشه (چون الان آدرس API این اپ هم ممکنه هاردکد باشه — چک کن با `grep -rn "fetch(" "Management App/Bazino/src"`).

---

## ۶. نکات فنی مهم برای ادامه‌ی کار (درس‌های این نشست)

- **این sandbox نه Flutter SDK داره نه `node_modules` نصب‌شده.** برای چک کردن TypeScript/TSX از این روش استفاده کن (واقعاً خطاهای سینتکسی رو می‌گیره):
  ```bash
  ESBUILD=/home/claude/.npm-global/lib/node_modules/tsx/node_modules/@esbuild/linux-x64/bin/esbuild
  $ESBUILD path/to/file.tsx --jsx=automatic --bundle=false
  ```
  خروجی خالی = بدون خطای سینتکسی. برای `tsc` هم می‌شه با `--skipLibCheck` و فیلتر کردن خطاهای «ماژول پیدا نشد» (چون `node_modules` نیست) استفاده کرد.
- همیشه بعد از هر ادیت، تعادل `{}`/`()` رو با یک اسکریپت پایتون کوچیک چک کن (نمونه‌ش در تاریخچه‌ی همین ادیت‌هاست).
- کاربر فارسی‌زبانه و همیشه با جزئیات فنی دقیق و صادقانه پاسخ می‌گیره؛ اگه چیزی رو نمی‌تونی تست کنی (چون SDK نیست)، صادقانه بگو.
- کاربر ترتیب اولویت‌بندی که پیشنهاد میدی رو معمولاً قبول می‌کنه، ولی صبر کن تاییدش رو بگیره قبل از هر batch بزرگ.
- فایل‌های وضعیت (`ARCHITECTURE.md`, `ISSUES_FOUND.md`, `SESSION_SUMMARY.md`) رو بعد از هر batch به‌روزرسانی کن — کاربر به این مستندسازی مداوم عادت کرده و ارزش می‌ذاره.
- در پایان هر batch، پروژه رو zip کن و با `present_files` بده — کاربر همیشه فایل نهایی زیپ‌شده می‌خواد.
