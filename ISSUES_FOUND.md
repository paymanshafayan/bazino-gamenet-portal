# BAZINO PRO — گزارش کامل مشکلات شناسایی‌شده (بررسی اولیه، قبل از هرگونه تغییر)

این فایل تمام مشکلاتی که طی بررسی کامل پروژه‌ی ارسالی شما (`gamenet-portal.zip`) شناسایی شد را در یک‌جا فهرست می‌کند — دقیقاً همان چیزی که در پروژه‌ی اصلی وجود داشت، پیش از اینکه هیچ تغییری اعمال شود.

---

## ۱. مشکلات امنیتی

### ۱.۱ پسورد کاربران به‌صورت متن ساده (Plain Text) ذخیره و مقایسه می‌شد
در `server.ts`:
```
INSERT INTO users VALUES (?, ?, ?, ?, 100)                              -- پسورد خام ذخیره می‌شد
SELECT * FROM users WHERE ... AND password = ?                          -- مقایسه‌ی خام
```
هیچ هش‌کردنی (bcrypt/argon2) در مسیر ثبت‌نام/ورود وجود نداشت — حتی در فرآیند نصب (installer) هم پسورد ادمین خام ذخیره می‌شد.

### ۱.۲ یک حساب ادمین پیش‌فرض با پسورد ضعیف داخل ریپو کامیت شده بود
فایل `database.json` در ریشه‌ی پروژه شامل:
```json
{ "username": "admin", "password": "admin", "role": "admin" }
```

### ۱.۳ اعتماد کامل به مقادیر مالی ارسالی از کلاینت
در endpoint های رزرو، سفارش کافه و سفارش فروشگاه، مقادیر `totalPrice`, `pointsEarned`, `discountApplied` مستقیماً از `req.body` (یعنی از خود مرورگر کاربر) خوانده و بدون بازمحاسبه در سرور ذخیره/اعمال می‌شدند. یعنی از طریق DevTools مرورگر می‌شد امتیاز/تخفیف دلخواه ساخت.

### ۱.۴ اعتبارسنجی ناقص کد تخفیف
`/api/discount/validate` فقط فعال‌بودن و حداقل مبلغ سفارش را چک می‌کرد؛ تاریخ انقضا (`expiry`) اصلاً به‌صورت تاریخ واقعی مقایسه نمی‌شد (فقط یک رشته‌ی نمایشی مثل «۳۰ روز دیگر» بود) و سقف تعداد استفاده اصلاً وجود نداشت — کد تخفیف فقط بعد از یک بار مصرف با `isActive = 0` به‌طور کامل غیرفعال می‌شد.

### ۱.۵ CORS باز به‌صورت کامل
در `GameNet.WebAPI` (پروژه‌ی ASP.NET): `AllowAnyOrigin().AllowAnyHeader().AllowAnyMethod()` — قابل قبول برای dev، نامناسب برای انتشار عمومی.

---

## ۲. مشکلات معماری و صحت داده

### ۲.۱ سوییچ بین سه دیتابیس در صفحه‌ی نصب کاملاً نمایشی/فیک بود
`SQLServerDataProvider` و `MongoDBDataProvider` در `server/dataProviders.ts` واقعاً به SQL Server یا MongoDB وصل نمی‌شدند؛ فقط لاگ‌های باورپذیر (`MSSQL Connection Established`, ...) تولید می‌کردند و همه‌ی عملیات واقعی را به همان `SQLiteDataProvider` محلی محول می‌کردند. یعنی هر سه گزینه‌ی نصب عملاً روی یک فایل JSON محلی کار می‌کردند.

### ۲.۲ عدم وجود چک هم‌پوشانی رزرو (Double-Booking)
سیستم رزرو فقط یک فلگ boolean (`isReserved`) روی هر سیستم داشت؛ هیچ مفهوم بازه‌ی زمانی/اسلات برای جلوگیری از رزرو هم‌پوشان وجود نداشت.

### ۲.۳ پوشه‌ی `server/GameNet.*` (ASP.NET Core) یک بک‌اند واقعی و تست‌شده نبود
- محتوای فایل‌های `.cs` عیناً به‌صورت رشته‌های هاردکد داخل `src/data/csharpCode.ts` و `csharpMvcCode.ts` وجود داشت — منبع فیچر نمایشی «تولید کد C#» در `CsharpCodeViewer.tsx`.
- **پروژه‌ی `GameNet.MVC` اصلاً کامپایل نمی‌شد**: کنترلرها و ViewModel ها به فیلدها/متدهایی ارجاع می‌دادند که روی `GameNet.Core.Entities` واقعی وجود نداشتند (مثلاً `IsReserved` به‌جای `IsActive`، شناسه‌ی `int` به‌جای `Guid`، `discount.Type`/`MinOrder`/`Expiry` به‌جای `DiscountType`/`MinOrderAmount`/`ExpiryDate`).
- هر دو پروژه‌ی `GameNet.MVC` و `GameNet.WebAPI` از `UseInMemoryDatabase` استفاده می‌کردند (نه SQL Server) — یعنی با هر ری‌استارت، تمام داده از بین می‌رفت.
- هیچ اثری از ASP.NET Identity / احراز هویت واقعی در این پروژه نبود.

### ۲.۴ `flutter_app` هم مشابه، یک پروژه‌ی مستقل توسعه‌یافته نبود
محتوای `flutter_app/pubspec.yaml` عیناً همان رشته‌ی هاردکدشده در `src/data/flutterCode.ts` بود (خروجی فیچر «تولید کد فلاتر»)، حتی با همان نام اشتباه برند: `cyber_nexus_app` به‌جای Bazino.

### ۲.۵ وابستگی نصب‌شده‌ی بلااستفاده
پکیج `sqlite3` در `package.json` وجود داشت ولی هیچ‌کجای کد واقعاً از آن استفاده نمی‌شد (به‌جایش یک شبیه‌سازی مبتنی بر فایل JSON با پارس Regex از رشته‌های SQL انجام می‌شد).

---

## ۳. مستندسازی قدیمی/متناقض

### ۳.۱ `ARCHITECTURE.md` قدیمی به‌شدت ناقص و گمراه‌کننده بود
- هیچ اشاره‌ای به `Management App/Bazino`، `flutter_app`، سیستم نصب چند-دیتابیسی، یا فیچر تولید کد C# نداشت.
- زبان‌های پشتیبانی‌شده را فقط «فارسی و انگلیسی» معرفی کرده بود (در حالی که ۴ زبان در کد پیاده شده: فارسی/انگلیسی/ترکی/روسی).
- در بخش «Planned Work»، طراحی رنگی «نئون بنفش» (`glowing neon purple`) را پیشنهاد داده بود — مستقیماً در تضاد با قانون صریح شما («هیچ رنگ بنفشی در طراحی نباید باشد»).

*(این مورد در جلسه‌ی قبل با ساخت سند معماری جدید برطرف شد.)*

---

## ۴. بهینه‌سازی و نظافت (غیرفوری)

### ۴.۱ حجم بالای تصاویر بهینه‌نشده
- `src/assets/images`: ~۴۸ مگابایت، شامل چند PNG حدود ۳.۵ مگابایتی
- `public/`: ~۱۴ مگابایت، شامل عکس‌های concept/mockup فاز طراحی (`concept.jpg`, `gaming_hub_header_mockup.jpg`, ...) که نباید در باندل نهایی باشند

### ۴.۲ فایل‌های خارج از دامنه‌ی اجرا داخل ریپو
`Bazino_Pro_Presentation.pdf/html` (~۷ مگابایت)، `zip_project.py`، `database.json` (که علاوه بر مشکل امنیتی‌اش، اصلاً نباید در مسیر deploy باشد)

### ۴.۳ کامپوننت‌های ابزار توسعه داخل اپ اصلی
`src/App.tsx` مستقیماً `CsharpCodeViewer`, `FlutterCodeViewer`, `PresentationTab`, `InstallPage` را ایمپورت می‌کند — این‌ها ابزارهای دمو/توسعه هستند، نه چیزی که کاربر نهایی گیم‌نت باید ببیند.

---

## نکته‌ی مهم: مواردی که بعد از بررسی، مشکل تشخیص داده نشدند
برای شفافیت، این‌ها هم بررسی شدند ولی با توضیح شما **مشکل محسوب نمی‌شوند** و در لیست بالا نیستند:
- دیتای سمپل در هر سه بخش (سایت، اپ مدیریت، دیتابیس تستی) — عمدی و برای فاز preview
- `layoutMode` قفل‌شده روی `'classic'` — عمدی، تا تکمیل کامل بخش hub
- استفاده از localStorage در Management App — قابل قبول در فاز فعلی preview

---

## وضعیت فعلی هرکدام (در زمان تهیه‌ی این گزارش)

| # | مشکل | وضعیت (آخرین به‌روزرسانی) |
|---|---|---|
| ۱.۱ | پسورد خام | ✅ برطرف شد (bcrypt) |
| ۱.۲ | ادمین پیش‌فرض ضعیف | ✅ برطرف شد (فقط fallback dev) |
| ۱.۳ | اعتماد به قیمت/امتیاز کلاینت | ✅ برطرف شد (محاسبه‌ی سرور-محور) |
| ۱.۴ | اعتبارسنجی ناقص کد تخفیف | ✅ برطرف شد (انقضای واقعی + سقف استفاده) |
| ۱.۵ | CORS باز | ⏳ فقط در صورت انتشار عمومی WebAPI مطرح است |
| ۲.۱ | سوییچ فیک دیتابیس | ✅ برطرف شد (هر سه provider واقعی) |
| ۲.۲ | نبود چک هم‌پوشانی رزرو | ✅ برطرف شد |
| ۲.۳ | پروژه‌ی ASP.NET ناسازگار/کامپایل‌نشدنی | ✅ به‌عنوان فیچر نمایشی پذیرفته شد |
| ۲.۴ | برندینگ اشتباه Flutter | ✅ برطرف شد |
| ۲.۵ | وابستگی بلااستفاده `sqlite3` | ✅ برطرف شد |
| ۳.۱ | سند معماری قدیمی | ✅ برطرف شد |
| ۴.۱ | حجم تصاویر | ✅ برطرف شد (GTmetrix: لوگو ۸۴۰KB→۱۵KB، پس‌زمینه ۳.۵MB→۱۸KB، WebP/JPG) |
| ۴.۲ | فایل‌های اضافی | ✅ برطرف شد (پاکسازی کامل) |
| ۴.۳ | ابزار دمو داخل اپ اصلی | ⏳ نگه‌داری‌شده (تب‌های کد/پرزنتیشن — lazy) |

---

## به‌روزرسانی‌های بعدی (پس از گزارش اولیه)

موارد جدیدی که در جلسات بعدی شناسایی و رفع شدند:

| مورد | وضعیت |
|---|---|
| موتور قالب‌بندی جدید (CSS مجزا + نصب ZIP + assets + کامپوننت) | ✅ انجام شد |
| منبع داده نمونه/دیتابیس (پیش‌فرض: نمونه) | ✅ انجام شد |
| قانون لوگوی سایت مادر (/logo.png + محافظ) | ✅ انجام شد |
| برندینگ Google AI Studio | ✅ حذف شد (صفر ارجاع) |
| ورک‌فلو CI (تایپ‌چک + تست + بیلد + boot) | ✅ انجام شد |
| عملکرد GTmetrix (کد اسپلیتینگ، کش، lazy، فونت، preconnect) | ✅ انجام شد |
| باگ بوت production باندل CJS (createRequire) | ✅ رفع شد |
| باگ تایپ‌چک CI (flutter_app در exclude) | ✅ رفع شد |
| باگ لوگو 404 در بوت production (کپی public به dist) | ✅ رفع شد |

---

## تست سرتاسری با مرورگر واقعی — ۱۴۰۵/۰۶/۱۰ (2026-09-01)

اولین باری که **کل استک واقعی** (Express + SQLite + WebSocket + Vite) در sandbox بالا آمد و
سایت مثل یک کاربر واقعی با Chromium 149 تست شد (ثبت‌نام → رزرو → کافه → فروشگاه → تورنمنت →
باشگاه → قالب → پنل ادمین). گزارش کامل با شواهد و اسکرینشات: **`E2E_TEST_REPORT.md`**
تصاویر: `visual-testing/e2e-2026-09-01/` · هارنس: `tests/e2e-browser/`

| # | مورد | شدت | وضعیت |
|---|---|---|---|
|E.1| `GET /api/user` بدون توکن، پروفایل (ایمیل+تلفن) آخرین کاربر واردشده را برمی‌گرداند — fallback به `activeUsername` | 🔴 بحرانی | ✅ رفع شد |
|E.2| رزرو کاربر در دیتابیس ثبت می‌شود ولی به‌خاطر `resolveSampleList` هرگز در «رزروهای فعال شما» دیده نمی‌شود | 🔴 بحرانی | ✅ رفع شد |
|E.3| `CafeTab`/`ShopTab` checkout هیچ‌وقت `POST /api/cafe/order` را صدا نمی‌زنند؛ `cafe_orders` و `shop_orders` خالی می‌مانند | 🔴 بحرانی | ✅ رفع شد |
|E.4| `handleRegisterTeam` و `handleAddComment` در `App.tsx` توابع stub هستند (فقط توست) | 🔴 بحرانی | ✅ رفع شد |
|E.5| تب‌های `blog`, `chat`, `csharp`, `flutter` هیچ ورودی‌ای در UI ندارند (کد مرده) | 🟠 مهم | ✅ رفع شد |
|E.6| ناوبری روی موبایل (<768px) کاملاً ناپدید می‌شود؛ جایگزین همبرگری/bottom-nav ندارد | 🟠 مهم | ✅ رفع شد |
|E.7| مودال قالب و راهنما با Escape بسته نمی‌شوند | 🟡 جزئی | ✅ رفع شد |
|E.8| `handleAddLoyaltyPoints` پارامتر `description` را دور می‌ریزد → شرح تراکنش‌ها `null` | 🟡 جزئی | ✅ رفع شد |
|E.9| نوع تراکنش `Bonus` در UI پشتیبانی نمی‌شود و «خرج امتیاز» با امتیاز مثبت نمایش داده می‌شود | 🟡 جزئی | ✅ رفع شد |
|E.10| وابستگی زمان‌اجرا به `api.qrserver.com`, `api.dicebear.com`, `fonts.googleapis.com`, `cdn.jsdelivr.net`, `openstreetmap.org` | 🟡 جزئی | ✅ رفع شد |
|E.11| رشته‌ی ترکی آلوده به فارسی در `ReservationsTab`; بنر راهنمای «پرزنتیشن» متن اشتباه؛ شکست دکمه‌ی «اعمال کد» | 🟡 جزئی | ✅ رفع شد |

**بدون مشکل:** رندر فارسی/RTL، صفر `pageerror`، صفر overflow افقی در ۳۹۰px و ۱۴۴۰px،
احراز هویت (bcrypt+JWT)، محاسبات قیمت/تخفیف/امتیاز، بازخرید امتیاز→کوپن واقعی،
موتور قالب (۵ قالب)، و هر ۱۶ بخش پنل مدیریت.

### نتیجه‌ی رفع (۱۴۰۵/۰۶/۱۰ — همان روز)

هر ۱۱ مورد رفع شد و با مرورگر واقعی دوباره تست شد. **۲۴/۲۴ مرحله‌ی سناریوی کاربر سبز**،
**۲۳۰/۲۳۰ تست خودکار سبز**، **صفر خطای console**. جزئیات و شواهد: `FIX_PLAN.md` بخش «نتیجه».

سه مورد **اضافه** که فقط در حین رفع و تست دوباره کشف شدند:

| # | مورد | شدت | وضعیت |
|---|---|---|---|
| E.12 | ویجت دانلود اپلیکیشن (`z-[85]`) روی نوار ناوبری جدید موبایل می‌افتاد و کلیک تب‌ها را می‌بلعید | 🟠 مهم | ✅ رفع شد |
| E.13 | نظر ثبت‌شده روی مقاله و تیم ثبت‌شده در تورنمنت پس از refresh زیر داده‌ی نمونه پنهان می‌شدند (`resolveSampleList` روی کاتالوگِ قابل‌ویرایش) | 🔴 بحرانی | ✅ رفع شد (`resolveMergedList`) |
| E.14 | `GET /api/admin/stats` در حالت sample سفارش‌ها را عمداً خالی برمی‌گرداند؛ داشبورد ادمین «کل درآمد ۰ تومان» نشان می‌داد حتی وقتی سفارش واقعی ثبت شده بود | 🔴 بحرانی | ✅ رفع شد |

### مالکیت تراکنش‌ها و کوپن‌ها — ۱۴۰۵/۰۶/۱۱

| # | مورد | شدت | وضعیت |
|---|---|---|---|
| E.15 | `transactions` ستون مالک نداشت؛ هر مشتری تاریخچه‌ی امتیاز و الگوی خرید بقیه را می‌دید | 🔴 بحرانی | ✅ رفع شد |
| E.16 | کد تخفیف شخصی (`LOYAL-…`) در لیست عمومی ظاهر می‌شد؛ چون یک‌بارمصرف است، اولین بیننده آن را خرج می‌کرد | 🔴 بحرانی | ✅ رفع شد |
| E.17 | `POST /api/loyalty/redeem` مقدار و کد کوپن را از بدنه‌ی درخواست می‌گرفت → با ۱ امتیاز می‌شد کوپن دلخواه ساخت | 🔴 بحرانی | ✅ رفع شد |
| E.18 | مصرف کوپن اتمیک نبود؛ دو درخواست هم‌زمان می‌توانستند یک کد یک‌بارمصرف را دو بار خرج کنند | 🟠 مهم | ✅ رفع شد |

جزئیات، طراحی و شواهد راستی‌آزمایی: `FIX_PLAN_OWNERSHIP.md`.

### تست بصری و عملیاتی کامل پنل مدیریت — ۱۴۰۵/۰۶/۱۱

هر ۱۶ بخش باز و با چشم بازبینی شد (به‌جز «پرزنتیشن» که عکس‌برداری‌اش در sandbox ممکن نشد)،
و ۵ عملیات نوشتنی واقعی اجرا شد. گزارش و پلن رفع: `FIX_PLAN_ADMIN.md`

| # | مورد | شدت | وضعیت |
|---|---|---|---|
| E.19 | ~~نوشتن ادمین در حالت sample دیده نمی‌شود~~ → **پس گرفته شد.** با تذکر کاربر و تست مجدد در حالت `database` مشخص شد رفتار عمدیِ حالت نمونه است. فقط یک نکته‌ی UX باقی ماند: توست موفقیت توضیح نمی‌دهد که برای دیدن رکورد باید حالت داده عوض شود | 🟡 جزئی | ✅ رفع شد |
| E.20 | شناسه‌ی رکورد جدید از روی **تعداد ردیف** ساخته می‌شود (`"s" + count+1`). **در حالت `database` هم می‌شکند:** پس از حذف یک رکورد، ساخت رکورد بعدی با `UNIQUE constraint failed` شکست می‌خورد و ادمین دیگر نمی‌تواند ایستگاه اضافه کند. پیشوند `a` هم بین مقالات و لوازم جانبی مشترک است | 🔴 بحرانی | ✅ رفع شد |
| E.21 | بخش «لاگ‌های دیتابیس» متن SQL و نوع عملیات را نشان نمی‌دهد (ناهماهنگی نام فیلد: `command`/`type` در سرور در برابر `query`/`operation` در UI). **مستقل از حالت داده** | 🟡 جزئی | ✅ رفع شد |

**سالم بود:** ۱۶/۱۶ بخش بدون خطای JavaScript · انتشار مقاله ✅ · تغییر وضعیت سفارش کافه ✅ ·
سوییچ منبع داده ✅ · ادمین تراکنش‌های همه‌ی کاربران و همه‌ی رزروها را می‌بیند ✅

**نتیجه‌ی رفع (همان روز):** هر سه مورد رفع و تست شدند — `tsc` تمیز · `npm test` ۲۴۱/۲۴۱ ·
جاروب ادمین ۲۲/۲۲ · سناریوی مرورگر ۲۴/۲۴. یک ایراد چهارم هم حین رفع پیدا شد: پنج فرم ادمین
شاخه‌ی خطا نداشتند و در صورت خطای سرور بی‌صدا می‌ماندند. شواهد: `FIX_PLAN_ADMIN.md`.

### تلاش برای نصب Flutter — ۱۴۰۵/۰۶/۱۱

طبق قانون ۲ هر سه پله طی شد. گزارش کامل: `FLUTTER_SETUP_ATTEMPT.md`.
نصب محلی به دو دیوار مستقل خورد (Dart SDK لینوکس و `pub.dev`، هر دو فقط از هاست‌های بسته).
راه‌حل: ورک‌فلوی `Flutter Build APK & IPA` از قبل در ریپو هست ولی فقط روی `main` و بدون
`workflow_dispatch`؛ و توکن این نشست اجازه‌ی تغییر فایل‌های ورک‌فلو را ندارد.

**آنچه بدون Flutter تست شد و سالم بود:** `tests/flutter-contract.mjs` — ۲۶ مسیر API و ۷۳ کلید
JSON از سورس دارت استخراج و روی سرور زنده آزموده شد → **۳۰/۳۰ سبز**. یعنی تغییرات امنیتی اخیر
(حذف fallback مهمان، per-user شدن تراکنش‌ها و کوپن‌ها، شناسه‌های جدید) **اپ موبایل را نشکسته‌اند**.
رندر UI، ناوبری، ویدیو، میکروفون و WebSocket همچنان **تست‌نشده** هستند.

### اولین اجرای واقعی CI فلاتر — ۱۴۰۵/۰۶/۱۱

ورک‌فلو توسط کاربر اضافه شد و اجرا شد (Flutter 3.47.2 / Dart 3.13.2). گزارش: `FLUTTER_CI_REPORT.md`.
**نشان جاب سبز است ولی دو مرحله واقعاً شکست خورده‌اند** — چون ورک‌فلویی که من دادم عمداً
`set +e` داشت تا گزارش کامل ثبت شود. پلن رفع: `FIX_PLAN_FLUTTER.md`

| # | مورد | شدت | وضعیت |
|---|---|---|---|
| E.22 | `flutter test` → ۰ پاس / ۱ شکست. `test/widget_test.dart` هنوز قالب پیش‌فرض `flutter create` است و دنبال اپ شمارنده می‌گردد؛ هیچ‌وقت چیزی از اپ بازینو را نسنجیده | 🟠 مهم | ✅ رفع شد |
| E.23 | `flutter analyze` → ۴ ایراد. پارامترهای منسوخ `localeId`/`listenFor`/`pauseFor`/`partialResults` در `jarvis_assistant.dart:224-227`؛ باید `SpeechListenOptions` شود | 🟡 جزئی | ✅ رفع شد |
| E.24 | هر ۷۴ وابستگی در `pubspec.lock` به میرور چینی `pub.flutter-io.cn` اشاره می‌کنند و صفر مورد به `pub.dev` | 🟠 مهم | ⏳ نیاز به تأیید جداگانه |
| E.25 | ورک‌فلوی CI هیچ‌وقت قرمز نمی‌شود (`set +e`)، پس شکست تست‌ها پنهان می‌ماند | 🟡 جزئی | ✅ رفع شد (استپ ششم توسط کاربر اعمال شد؛ اجرای `33650390088`) |

**سالم بود:** `flutter pub get` ✅ · `flutter build web --release` ✅ (خروجی ۴۵MB) — یعنی کل کد اپ
واقعاً کامپایل می‌شود و خطای نوع ندارد.

**نتیجه (همان روز):** E.22 و E.23 رفع و با اجرای واقعی CI تأیید شدند —
`flutter analyze` → `No issues found!` و `flutter test` → **۱۰ تست، همه سبز** (اجرای `33648613450`).
E.24 (میرور چینی در `pubspec.lock`) و E.25 (سبزِ کاذب ورک‌فلو) باز مانده‌اند؛ اولی تصمیم کاربر
می‌خواهد و دومی تغییر فایل ورک‌فلو که این نشست اجازه‌اش را ندارد.

**به‌روزرسانی E.25 (۱۴۰۵/۰۶/۱۱):** کاربر ورک‌فلوی اصلاح‌شده را اعمال کرد. اجرای `33650390088`
شش استپ دارد و استپ ششم «Fail the job if analyze or test failed» واقعاً اجرا و سبز شد.
منطق آن پیش از تحویل، با `bash -e` در هر سه حالت (همه سبز / analyze+test قرمز / فایل غایب)
تست شده بود. **اثبات end-to-end انجام شد** (با اجازه‌ی کاربر): کامیت `5caf1e6` یک تست عمداً خراب اضافه کرد →
اجرای `33652726149` **قرمز** شد؛ استپ‌های ۱ تا ۵ سبز ماندند (پس گزارش قبل از شکست commit شد) و
استپ ششم `failure` شد با annotation دقیق `flutter test=1 — see FLUTTER_CI_REPORT.md`.
`analyze` و `build web` سبز ماندند، یعنی گارد به **یک** مرحله‌ی شکست‌خورده واکنش می‌دهد نه به نویز.
سپس با `6d79e56` برگردانده شد و اجرای `33653069848` دوباره **سبز** شد (۱۰/۱۰).

**کشف جانبی (مفید برای جلسات بعد):** حتی وقتی دانلود لاگ اجرا بسته است، **annotationها از
`api.github.com` خواندنی‌اند** و متن دقیق خطا را دارند:
`gh api repos/<owner>/<repo>/check-runs/<job_id>/annotations`
در اجرای قرمز، همین مسیر مستقیماً `flutter test=1` و `10 tests passed, 1 failed.` را برگرداند.

### تست نرم‌افزار مدیریت گیم‌نت — ۱۴۰۵/۰۶/۱۱

هر ۶ زبانه با مرورگر واقعی باز و بازبینی شد. `tsc` و `vite build` سبز؛ اپ روی
`/management-app/` سرو می‌شود و `/api/state` واقعاً کار می‌کند. پلن رفع: `FIX_PLAN_MANAGEMENT.md`

| # | مورد | شدت | وضعیت |
|---|---|---|---|
| E.26 | اپ با **۳٫۳ FPS** کار می‌کند (سایت اصلی روی همان مرورگر ۶۰٫۳). تایمر هر-ثانیه بدون قید `setStations` می‌زند و آرایه‌ی جدید می‌سازد → کل درخت React + ۱۱ نوشتن همگام در localStorage + یک POST پنج‌کیلوبایتی. اندازه‌گیری‌شده: **۱۵ بار `POST /api/state` در ۱۵ ثانیه سکون کامل** (~۲۸٬۸۰۰ درخواست در هر شیفت هشت‌ساعته) | 🔴 بحرانی | 🟠 **نیمه‌رفع** |
| E.27 | مودال‌ها با Escape بسته نمی‌شوند و چون تمام‌صفحه‌اند هر کلیک بعدی را می‌بلعند (همان E.7 سایت اصلی، اینجا تکرار شده) | 🟠 مهم | ✅ رفع شد |
| E.28 | `Management App/Bazino/index.html` در زمان اجرا `fonts.googleapis.com` را صدا می‌زند — تنها خطای console اپ | 🟡 جزئی | ✅ رفع شد |

**تست‌نشده:** جریان کامل تسویه، تست اتصال واقعی همگام‌سازی وب، و نسخه‌ی دسکتاپ standalone.

**نتیجه (۱۴۰۵/۰۶/۱۱):** E.27 و E.28 کامل رفع شدند. هارنس از ۲۰/۲۸ به **۲۸/۲۸** رسید با صفر خطای
console و صفر درخواست ناموفق.

**E.26 فقط نیمه رفع شد و صادقانه اعلام می‌شود:** بار شبکه‌ای کامل حذف شد
(**۱۵ → ۰** درخواست در ۱۵ ثانیه سکون) و دو انیمیشن CSS که خاصیت paint/layout را انیمیت می‌کردند
بازنویسی شدند (انتساب اندازه‌گیری‌شده: `timerPulseWarning` روی `box-shadow` = +۲۵٫۸ فریم،
`soundWave` روی `height` = +۱۴٫۷ فریم). اما **نرخ فریم همچنان ظرف چند ثانیه از ~۳۰ به ~۲ سقوط
می‌کند.** شمارنده‌ی `JSEventListeners` مرورگر ~۶۱ عدد در ثانیه بالا می‌رود در حالی که DOM و heap
ثابت‌اند؛ منبعش `addEventListener` جاوااسکریپتی نیست (با patch تأیید شد)، مسیر صوتی هم نیست
(با خاموش‌کردن آلارم تأیید شد) و `React.memo` هم متوقفش نکرد. نیاز به جلسه‌ی پروفایلینگ اختصاصی
دارد. جزئیات کامل: `FIX_PLAN_MANAGEMENT.md`.

### تست بصری نسخه‌ی دسکتاپ — ۱۴۰۵/۰۶/۱۱

Electron در sandbox نصب‌شدنی نیست (باینری از GitHub Releases می‌آید و بسته است)، ولی پروسه‌ی
main آن کار خاصی جز «require کردن `server.cjs` درون‌پردازه + باز کردن پنجره روی
`localhost:PORT/management-app`» نمی‌کند. با `desktop-app/scripts/run-headless-desktop.cjs`
دقیقاً همان مسیر بازسازی شد: chdir به پوشه‌ی داده، `BAZINO_STATIC_ROOT`، `NODE_ENV=production`،
تولید JWT_SECRET، و اجرای همان باندل — سپس عکس‌برداری در ابعاد واقعی پنجره (۱۴۴۰×۹۰۰).

| # | مورد | شدت | وضعیت |
|---|---|---|---|
| E.29 | **نسخه‌ی دسکتاپ اصلاً بالا نمی‌آید.** `createRequire` در `server/dataProviders.ts:12` وقتی `import.meta` نباشد (یعنی در باندل CJS) به `process.cwd()` fallback می‌کند. `main.js` عمداً قبل از require کردن سرور، cwd را به پوشه‌ی داده‌ی کاربر می‌برد، پس `better-sqlite3` در `<userData>/node_modules` جست‌وجو می‌شود و پیدا نمی‌شود → `Cannot find module 'better-sqlite3'` و اپ می‌میرد. در حالت co-located تصادفاً کار می‌کند چون cwd همان ریشه‌ی پروژه است | 🔴 بحرانی | ✅ رفع شد |
| E.30 | `prepare-server-bundle.js` برای ساخت باندل به اینترنت نیاز دارد (`npm install` داخل پوشه‌ی bundle) در حالی که `node_modules` ساخته‌شده‌ی ریشه از قبل موجود است | 🟡 جزئی | ✅ رفع شد | | آدرس و پورت LAN در هدر هاردکد است (`Header.tsx:69` → «آفلاین LAN: 192.168.1.100:3000») و ربطی به پورت واقعی سرور ندارد | 🟡 جزئی | ✅ رفع شد |

**سالم بود:** سرور درون‌پردازه با **دیتابیس SQLite کاملاً مستقل** در پوشه‌ی داده بالا آمد
(`bazino.sqlite3` + WAL + `.jwt-secret` با مجوز ۶۰۰) · UI در ابعاد واقعی پنجره بدون overflow ·
**صفر خطای console و صفر درخواست ناموفق شبکه** (بیلد production دیگر فونت گوگل را صدا نمی‌زند) ·
هر ۶ زبانه و مودال «همگام وب» با ۵ تب درست رندر شدند · وضعیت co-located درست تشخیص داده شد.

**نتیجه‌ی E.29/E.30/E.31 (همان روز):** هر سه رفع شدند. نسخه‌ی دسکتاپ حالا **بدون هیچ دورزدنی**
بالا می‌آید (`Active provider initialized: SQLite` با cwd روی پوشه‌ی داده و بدون `node_modules`
آنجا)، ساخت باندل آفلاین کامل می‌شود، و هدر `آفلاین LAN: 127.0.0.1:3100` یعنی آدرس واقعی را
نشان می‌دهد. هارنس مدیریت روی **باندل production دسکتاپ** ۲۸/۲۸ سبز شد و `npm test` سایت
۲۴۱/۲۴۱. شواهد کامل: `FIX_PLAN_DESKTOP.md`.

### نیاز کاربر: فایل نصاب دسکتاپ برای دانلود از پنل — ۱۴۰۵/۰۶/۱۱

زیرساخت سرور و پنل از قبل آماده است (`/api/desktop/availability` و `/api/desktop/download/:platform`
و دکمه‌های `SettingsThemesModal.tsx`)؛ آنچه وجود ندارد **خودِ فایل نصاب** است، چون ساختش
به Electron و electron-builder روی هر سه سیستم‌عامل نیاز دارد و در sandbox ممکن نیست.

| # | مورد | شدت | وضعیت |
|---|---|---|---|
| E.32 | هیچ نصاب دسکتاپی وجود ندارد؛ دکمه‌های دانلود در پنل همیشه «(به‌زودی)» و غیرفعال‌اند | 🟠 مهم | ⏳ پلن آماده |
| E.33 | commit کردن نصاب در ریپو عملی نیست (سقف ۱۰۰MB گیت‌هاب، تورم دائمی تاریخچه، و fs موقتی Railway) — نیاز به میزبانی روی GitHub Releases و redirect از سرور | 🟠 مهم | ⏳ پلن آماده |

پلن کامل با معماری، تغییرات لازم در `server.ts` و فایل ورک‌فلوی سه‌پلتفرمی: `FIX_PLAN_INSTALLER.md`

## ۱۴۰۵/۰۶/۱۲ — اصلاح خطای installer ویندوز و لینوکس

- خطای Windows در `npm run build` ناشی از استفاده از single quote در `cd 'Management App/Bazino'` بود؛ به `npm --prefix "Management App/Bazino"` اصلاح شد.
- خطای Linux ناشی از نبود metadata لازم برای target `deb` بود؛ homepage، author email و maintainer اضافه شد.
- bundle ساز اکنون در CI وابستگی‌های production-only نصب می‌کند و فقط در صورت شکست نصب، fallback آفلاین دارد.

## ۱۴۰۵/۰۶/۱۲ — تشخیص دور سوم CI

- بعد از اصلاح metadata و build script، هر سه runner build اصلی را با موفقیت رد کردند اما در `Prepare the server bundle` متوقف شدند.
- اجرای `electron-rebuild` از داخل `server-bundle` به‌جای مسیر صریح desktop-app مبهم بود؛ CLI و نسخه Electron اکنون صریحاً resolve و اجرا می‌شوند.

## ۱۴۰۵/۰۶/۱۲ — خطای قطعی electron-rebuild در run 33723462882

- هر سه runner در `Prepare the server bundle` شکست خوردند.
- پیام مشترک: `Cannot find module '@electron/rebuild/bin.js'`.
- در `@electron/rebuild@3.7.1` مسیر درست CLI برابر `lib/cli.js` است، نه `bin.js` در ریشه‌ی package.
- اصلاح resolve به مسیر CLI واقعی انجام شد.

## ۱۴۰۵/۰۶/۱۲ — Web Sync در پنل مدیریت

- E.34: کلید `gamenet_sync_api_key` در backend وجود داشت اما UI پنل فقط تنظیمات جارویس را نشان می‌داد. رفع شد؛ کارت تولید/ذخیره/کپی کلید Web Sync به پنل اضافه شد و مقدار عمومی secret افشا نمی‌شود.

## ۱۴۰۵/۰۶/۱۲ — چهار درخواست جدید (CSP، تم، منوی زبان/GeoIP، ترجمه‌ی کامل)

| # | مورد | شدت | وضعیت |
|---|---|---|---|
| E.35 | CSP کنسول: `new Function` در کد تم/کلاینت با `script-src` سازگار نبود | 🟠 مهم | ✅ رفع — `ad6414f` |
| E.36 | تم انتخاب‌شده بعد از refresh به پیش‌فرض برمی‌گشت | 🟠 مهم | ✅ رفع — `3a7ebe3` |
| E.37 | منوی زبان نام کامل زبان‌ها را نشان می‌داد؛ زبان پیش‌فرض بر اساس کشور تعیین نمی‌شد | 🟡 متوسط | ✅ رفع — `4e78eb4` (فقط پرچم + کد؛ GeoIP: IR→fa, TR/CY→tr, RU→ru, بقیه→en؛ انتخاب دستی همیشه غالب است) |
| E.38 | ~۷۶۰ سه‌تایی `language === 'fa' ? … : …` فقط فارسی/انگلیسی داشتند؛ کاربر روسی/ترکی متن انگلیسی می‌دید | 🔴 بحرانی | ✅ رفع — بچ A/B/D `3239806`، بچ C `3d4bb51` (پنل مدیریت + راهنمای تصویری) |
| E.39 | اعداد با `toLocaleString('fa-IR')` / بدون locale در همه‌ی زبان‌ها فارسی رندر می‌شدند | 🟡 متوسط | ✅ رفع — `localeOf(language)` در `src/utils/i18n.ts` |
| E.40 | پیام‌های خطای API (`error:` در server.ts) همه فارسی بودند و مستقیم به کاربر نشان داده می‌شدند | 🟠 مهم | ✅ رفع — بچ E `9ea336e` (`server/apiMessages.ts` + هدر `X-Lang` از کلاینت + `code` ماشین‌خوان) |
| E.41 | نام تم داخلی «قالب گرید کنسولی» و timestamp لاگ‌های DB در همه‌ی زبان‌ها فارسی بود | 🟢 جزئی | ✅ رفع — نام زبان‌خنثی `Console Grid (Classic)`، ساعت با ارقام لاتین |
| E.42 | داده‌های نمونه (`sampleData.ts`: نام سیستم‌ها، منوی بوفه، مقالات، اسلایدها) فقط fa/en دارند | 🟢 جزئی | ⏳ خارج از محدوده‌ی توافق‌شده — در ru/tr متن فارسی/انگلیسی پایگاه داده نمایش داده می‌شود |
