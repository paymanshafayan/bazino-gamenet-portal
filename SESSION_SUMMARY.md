# BAZINO PRO — خلاصه‌ی تغییرات این جلسه (تکمیل‌شده)

## ۱۴۰۵/۰۶/۱۴ — کمپین اینستاگرام Invite Your Squad (Media-ID + Friend Gate)

- ingest: `POST /api/integrations/instagram/published-media` با Bearer توکن پورتال (`IG_INGEST_TOKEN` / `ig_ingest_token` seedشده؛ نه کلید sync). جدول `ig_media`.
- Friend Gate: کامنت کلیدواژه → یک PR (پیام۱+دکمه) → بعد دکمه **DM** پیام۲ با کد ۶رقمی یکتا → کامنت همان عدد توسط دوست زیر همان پست = `share_confirmed_by_friend_code` → لینک `/?ref=` + UTM اینستاگرام (+ کوپن اگر مقدار>۰). کمیسیون فقط قیف رزرو/پرداخت موجود.
- جداول `ig_media` / `ig_members` / `ig_events` در SQLite و `dbo.` و Mongo. کلیدهای `IG_SETTING_KEYS` جدا از `AFFILIATE_SETTING_KEYS`.
- متن‌ها ۴ زبان × پیام۱/۲/دوست/لینک + برچسب دکمه، همه قابل‌ویرایش در `/admin/affiliates` (`data-ig-setting`). شبیه‌ساز ادمین بدون Zernio.
- HMAC inbound `X-Zernio-Signature` روی raw body. Outbound فقط اگر env Zernio ست باشد.
- سند: `docs/payments/AFFILIATE-IG.md`.
- تست اجرا شد: unit 98/98 (سوئیت ۱۵ IG سبز)، database 38/38، providers 27/27، `tsc --noEmit` صفر خطا. API: سوئیت ۳۷ ingest+simulator سبز؛ ۱۰ شکست باقی‌مانده فقط SPA/static به‌خاطر نبودن `vite build` در این اجرا (نه منطق IG). Chromium این batch اجرا نشد.

## ۱۴۰۵/۰۶/۱۴ — بسته شدن نشست (POS ناتمام)

کاربر عکس POS Ingenico (`maximum 25.yıl`، ترمینال `S1E5R402`) فرستاد و تحقیق مستندات اتصال به نرم‌افزار مدیریت را خواست. تحقیق وب انجام نشد (نوبت‌ها بدون tool call بسته می‌شدند). به دستور کاربر: کامیت/پوش اسناد هندآف و رفتن به چت جدید. کد POS نوشته نشد. کار بعدی در `HANDOFF_PROMPT.md` §۹.

---

## چه چیزی عوض شد

### ۱. `server/dataProviders.ts` — بازنویسی کامل
- رابط تایپ‌شده‌ی `IDataStore` با متد معنادار برای هر جدول (نه رشته‌ی SQL خام)
- **SQLite واقعی** با `better-sqlite3` (فایل دیتابیس واقعی، نه JSON)
- **SQL Server واقعی** با `mssql` (اتصال واقعی، DDL واقعی، کوئری پارامتری واقعی)
- **MongoDB واقعی** با درایور رسمی `mongodb` (کالکشن/ایندکس/CRUD واقعی)
- **هش پسورد** با `bcryptjs` در هر سه دیتابیس
- متدهای `deleteX` برای همه‌ی جداول قابل‌حذف (سیستم‌ها، آیتم کافه، لوازم جانبی، تورنومنت، مقاله، اتاق چت، لاگ رزرو)
- `seedSampleData()` / `purgeSampleData()` — بارگذاری/حذف کامل تمام دیتای نمونه با یک فراخوانی

### ۲. `server.ts` — بازنویسی کامل (۱۳۱ نقطه‌ی صدازنی)
- همه‌ی endpoint ها حالا از متدهای جدید `getActiveDataProvider()` استفاده می‌کنن
- ورود/ثبت‌نام از `verifyLogin`/`createUser` استفاده می‌کنه (پسورد هش‌شده)
- **مهم:** دیتای نمونه دیگه در استارتاپ سرور به‌صورت خودکار لود نمی‌شه — فقط از طریق تیک «افزودن موارد نمونه» در صفحه‌ی نصب، یا دکمه‌ی «بارگذاری دیتای نمونه» در پنل مدیریت
- Endpoint های DELETE جدید اضافه شدن: `/api/admin/systems/:id`, `/api/admin/cafe/:id`, `/api/admin/accessories/:id`, `/api/admin/tournaments/:id`, `/api/admin/articles/:id`, `/api/admin/chat-rooms/:name`, `/api/admin/reservations/:id`

### ۳. `src/components/AdminPanelTab.tsx` — دکمه‌ی حذف اضافه شد برای:
- ✅ سیستم‌ها (کنار دکمه‌ی فعال/غیرفعال‌سازی)
- ✅ آیتم‌های کافه
- ✅ تورنومنت‌ها
- ✅ اسلایدرها (از قبل بود)

### ۴. فایل‌های دیگر
- `sqliteDb.ts` حذف شد (دیگه نیازی نیست، همه چیز مستقیم از `dataProviders.ts` میاد)
- `database.json` قدیمی (با ادمین پیش‌فرض خام) حذف شد
- `package.json`: اضافه شدن `mssql`, `mongodb`, `bcryptjs`, `better-sqlite3` + انواع تایپ‌شون؛ حذف `sqlite3` بلااستفاده
- `.gitignore`: اضافه شدن `install-config.json`, `bazino.sqlite3`, `database.json`

---

## ⚠️ گپ قبلی — الان برطرف شد ✅

پنل مدیریت حالا برای **لوازم جانبی فروشگاه، مقالات بلاگ، و اتاق‌های چت** هم بخش کامل «لیست + حذف» دارد:
- **فروشگاه لوازم جانبی**: زیر فرم افزودن، لیست کالاهای موجود با دکمه‌ی حذف اضافه شد.
- **بلاگ**: زیر فرم انتشار مقاله، لیست مقالات منتشرشده با دکمه‌ی حذف اضافه شد.
- **اتاق‌های گفتگو**: یک زیرتب کاملاً جدید («اتاق‌های گفتگوی زنده») به منوی کناری پنل مدیریت اضافه شد، شامل فرم ایجاد اتاق جدید + لیست اتاق‌های فعال با دکمه‌ی حذف.

حالا هر ۷ نوع دیتای نمونه (سیستم‌ها، آیتم کافه، لوازم جانبی، تورنومنت، مقاله، اتاق چت، رزرو) هم از صفحه‌ی نصب قابل بارگذاری‌اند و هم تک‌تک از پنل مدیریت قابل حذف.


---

---

## ۵. اولویت ۳ و ۴ — تکمیل شد

### محاسبه‌ی سرور-محور قیمت/امتیاز/تخفیف (اولویت ۳)
سه endpoint بازنویسی شدن تا دیگه هیچ‌وقت به `totalPrice`, `pointsEarned`, `discountApplied` ارسالی از کلاینت اعتماد نکنن:
- **رزرو سیستم**: قیمت از `system.hourlyRate × مدت‌زمان واقعی` محاسبه می‌شه؛ امتیاز از `Math.floor(totalPrice / 10000)`.
- **سفارش کافه**: قیمت از جمع `قیمت واقعی هر آیتم منو × تعداد` محاسبه می‌شه (نه از چیزی که کلاینت فرستاده)؛ کد تخفیف با تابع مشترک `validateCouponServerSide` اعتبارسنجی و مبلغش محاسبه می‌شه؛ همچنین موجودی هر آیتم قبل از ثبت سفارش چک می‌شه (اگر ناکافی باشه، سفارش رد می‌شه).
- **سفارش فروشگاه**: دقیقاً همون منطق، برای لوازم جانبی.
- تابع `/api/discount/validate` هم روی همین منطق مشترک بازنویسی شد تا سه‌جا کد تکراری نداشته باشیم.

### چک هم‌پوشانی رزرو (اولویت ۴)
قبل از ثبت هر رزرو، `store.hasOverlappingReservation(...)` چک می‌شه؛ اگه سیستم در همون بازه‌ی زمانی/تاریخ قبلاً رزرو شده باشه، درخواست با پیام فارسی مناسب رد می‌شه (کد وضعیت ۴۰۹).

---

## نکته‌ی جانبی که دیدم (خارج از اسکوپ این جلسه)
فرانت‌اند دو مسیر متفاوت برای محاسبه‌ی امتیاز کافه/فروشگاه داره: `ConsoleGridClassic.tsx` (بخش غیرفعال hub) از فرمول‌های متفاوتی (۱ امتیاز به ازای هر ۳۰۰۰ یا ۵۰۰۰ تومان) استفاده می‌کنه، در حالی که `CafeTab.tsx`/`ShopTab.tsx`/`ReservationsTab.tsx` (مسیر فعال classic) همه از ۱ امتیاز به ازای هر ۱۰,۰۰۰ تومان استفاده می‌کنن. من فرمول سرور رو با مسیر فعال (classic) هماهنگ کردم. اگه/وقتی بخش hub رو تکمیل کنید، بد نیست اون فرمول‌ها رو هم یکی کنید.

```bash
npm install mssql mongodb bcryptjs better-sqlite3 @types/bcryptjs @types/better-sqlite3
```
چون در این محیط sandbox دسترسی اینترنت ندارم، امکان اجرای واقعی/تست اتصال به SQL Server و MongoDB وجود نداشت — لطفاً قبل از استفاده‌ی واقعی، یک بار local تست کنید (مخصوصاً مسیر نصب با هر سه گزینه‌ی دیتابیس).

---

## ۶. اولویت ۱، ۳، ۴ (دور دوم درخواستی) — تکمیل شد

### کد تخفیف: انقضای واقعی + سقف تعداد استفاده
- فیلدهای واقعی `expiryDate` و `maxUsageCount`/`usageCount` اضافه شد (هر سه دیتابیس)
- `validateCouponServerSide` الان واقعاً انقضا و سقف استفاده رو چک می‌کنه
- `recordCouponUsage` جایگزین غیرفعال‌سازی کورکورانه شد

### برندینگ Flutter
تمام رد پای `CyberNexus` در `flutter_app/`، ژنراتور `flutterCode.ts`/`csharpMvcCode.ts`، ویوهای MVC، راهنماهای انتشار — **و حتی خود سایت زنده** (`DarkGoldHome.tsx`، یک `title` روی نقشه) به Bazino تصحیح شد.

### بهینه‌سازی تصاویر
- **۲۷ فایل کاملاً بلااستفاده حذف شد** (~۴۵ مگابایت) — asset-pack های بی‌ربط، mockup های فاز طراحی، لوگوهای تکراری
- تصاویر واقعی باقی‌مانده فشرده شدن:

| فایل | قبل | بعد |
|---|---|---|
| `public/reservations_panel.png` | ۶۱۶ کیلوبایت | ۱۱۷ کیلوبایت |
| `public/cafe_panel.png` | ۴۶۹ کیلوبایت | ۶۹ کیلوبایت |
| `public/shop_panel.png` | ۵۲۴ کیلوبایت | ۸۹ کیلوبایت |
| `public/tournaments_panel.png` | ۵۹۴ کیلوبایت | ۱۱۵ کیلوبایت |
| `public/loyalty_panel.png` | ۵۳۸ کیلوبایت | ۱۰۳ کیلوبایت |
| `public/bg.png` | ۲.۴ مگابایت | ۲۹۰ کیلوبایت |
| `src/assets/images/bazino_logo_user.png` | ۱.۱ مگابایت | ۸۴۰ کیلوبایت |

**نتیجه:** `public/` از ۱۴ مگابایت به ۸۰۰ کیلوبایت رسید. نام فایل‌ها و مسیرها عوض نشدن، پس هیچ کد دیگه‌ای نیاز به تغییر نداشت.

### ⚠️ یک باگ واقعی که نمی‌تونم خودم حلش کنم
دو فایل که واقعاً توی سایت استفاده می‌شن — `src/assets/images/background.png` (در `App.tsx`) و `src/assets/images/bg.png` (در `ConsoleHubView.tsx`) — **بایت‌های باینری‌شون خراب شده** (هدر PNG با کاراکتر جایگزین UTF‑8 جایگزین شده، احتمالاً در فرآیند zip/آپلود). این یعنی الان توی سایت واقعی این دو عکس اصلاً نمایش داده نمی‌شن. من نمی‌تونم از روی این بایت‌های خراب فایل سالم بسازم — **لطفاً نسخه‌ی اصلی این دو تصویر رو از پروژه‌ی خودتون دوباره پیدا کنید و جایگزین کنید.**

---

## جمع‌بندی نهایی این جلسه‌ی طولانی

از چک‌لیست اولیه‌ی `ISSUES_FOUND.md`، همه‌چیز به‌جز موارد زیر برطرف شد:
- ادمین پیش‌فرض ضعیف در `database.json` (فایل خودش حذف شده، ولی نصب با پسورد ضعیف هنوز فنی ممکنه)
- CORS باز در ASP.NET WebAPI (فقط اگه اون رو واقعاً دیپلوی کنید مهمه)
- فایل‌های اضافی در مسیر deploy (PDF پرزنتیشن، `zip_project.py`)
- ابزار دمو داخل اپ اصلی (`CsharpCodeViewer` و بقیه)
- دو فرمول امتیاز متفاوت بین بخش hub و classic

## قبل از اجرا لازم دارید
```bash
npm install mssql mongodb bcryptjs better-sqlite3 @types/bcryptjs @types/better-sqlite3
```
چون در این محیط sandbox دسترسی اینترنت ندارم، امکان اجرای واقعی/تست اتصال به SQL Server و MongoDB وجود نداشت — لطفاً قبل از استفاده‌ی واقعی، یک بار local تست کنید (مخصوصاً مسیر نصب با هر سه گزینه‌ی دیتابیس، و حتماً دو عکس خراب رو جایگزین کنید).

---

## ۷. دستیار هوشمند «جارویس» — از نمایش ظاهری به یک ویژگی واقعی

### چیزی که قبلاً بود (فقط برای یادآوری)
- بدون هوش مصنوعی واقعی (فقط تشخیص کلمه‌کلیدی)
- ورودی صوتی کاملاً فیک (یک جمله‌ی تصادفی از بین ۴ جمله‌ی از پیش نوشته‌شده، فارغ از چیزی که کاربر گفته)
- هیچ اقدامی (سفارش غذا، تمدید رزرو، تماس با ادمین، پیام چت) واقعاً به سرور نمی‌رسید

### چیزی که الان هست

**۱. مغز دستیار روی سرور منتقل شد (`POST /api/assistant/command`)**
- اگه `GEMINI_API_KEY` تنظیم باشه، از قابلیت **function calling واقعی Gemini** برای فهمیدن منظور کاربر استفاده می‌کنه (نه فقط `contains()`)
- اگه کلید تنظیم نباشه، به‌صورت خودکار به تشخیص کلمه‌کلیدی سقوط می‌کنه — ولی حتی اونجا هم **اقدام واقعیه**، نه فقط متن
- چهار اقدام واقعی پشتیبانی می‌شه:
  - **سفارش کافه**: آیتم منو رو توی دیتابیس واقعی پیدا می‌کنه، موجودی کم می‌کنه، سفارش واقعی توی پنل ادمین ثبت می‌شه، امتیاز واقعی اضافه می‌شه
  - **تمدید رزرو**: یک endpoint کاملاً جدید (`/api/reservations/extend`) ساختم که رزرو فعال واقعی کاربر رو پیدا می‌کنه، چک هم‌پوشانی انجام می‌ده، امتیاز واقعی کم می‌کنه، و زمان پایان رزرو رو واقعاً توی دیتابیس عوض می‌کنه
  - **تماس با ادمین**: یک پیام واقعی (`/api/support/request`) توی پنل مدیریت ثبت و broadcast می‌شه
  - **پیام چت**: پیام واقعی توی چت‌روم واقعی ثبت و برای همه پخش می‌شه

**۲. برای این‌که «تمدید رزرو» واقعی باشه، رزروها الان به کاربر متصل‌ان**
قبلاً هیچ ارتباطی بین یک رزرو و کاربری که اون رو ثبت کرده وجود نداشت. فیلد `username` به `reservation_logs` اضافه شد (هر سه دیتابیس) تا بشه فهمید «کاربر الان روی کدوم سیستمه».

**۳. ورودی صوتی واقعی شد**
پکیج `speech_to_text` اضافه شد و `JarvisStateProvider` الان واقعاً از میکروفون گوشی صدا ضبط و به متن تبدیل می‌کنه (نه یه جمله‌ی تصادفی). جالبه که مجوزهای میکروفون توی `AndroidManifest.xml` و `Info.plist` از قبل آماده بودن (با کامنت صریح «برای Jarvis Speech-To-Text») ولی هیچ‌وقت واقعاً استفاده نشده بودن.

**۴. یک باگ جانبی مهم پیدا و رفع شد**
تنها فراخوانی شبکه‌ی واقعی توی کل اپ فلاتر (`/api/app-sliders`) هاردکد شده بود روی `http://localhost:3000` — که روی گوشی واقعی اصلاً کار نمی‌کنه (چون «localhost» گوشیه، نه سرور شما). یک فایل `api_config.dart` ساختم که آدرس سرور رو یک‌جا و قابل‌تغییر نگه می‌داره (با پشتیبانی از `--dart-define=API_BASE_URL=...` موقع build).

### ⚠️ محدودیت‌هایی که صادقانه باید بدونید
1. **من نمی‌تونم کد Flutter رو کامپایل/تست کنم** — این sandbox هیچ Flutter SDK ای نداره. کد رو با دقت و بر اساس الگوهای رایج نوشتم، ولی حتماً باید با `flutter run` روی یک دستگاه/شبیه‌ساز واقعی تست بشه.
2. **بقیه‌ی اپ فلاتر (لاگین، لیست سیستم‌ها، فروشگاه) هنوز کاملاً local/mock هستن** — فقط جارویس و بارگذاری اسلایدرها به سرور واقعی وصل شدن. اگه بخواید کل اپ رو واقعی کنید، اون یک پروژه‌ی بزرگ‌تر و جداست (رونویسی کامل `AppState` برای واکشی/ارسال به API واقعی).
3. **سیستم احراز هویت فعلی بک‌اند ساده‌ست** (یک «کاربر فعال سراسری» به‌جای session/token واقعی برای هر کاربر) — این محدودیت از قبل توی کل بک‌اند بود، مخصوص جارویس نیست، ولی روی «جارویس چطور می‌فهمه کاربر کیه» هم اثر می‌ذاره.
4. `src/data/flutterCode.ts` (ژنراتور نمایشی) با این تغییرات هنوز sync نشده — چون فقط یه فیچر نمایشیه، اولویتش پایینه.

---

## ۸. مورد ۳ (احراز هویت واقعی per-user) و مورد ۲ (اتصال کامل اپ فلاتر) — تکمیل شد

### مورد ۳: توکن واقعی JWT برای هر کاربر
- پکیج `jsonwebtoken` اضافه شد؛ هر `login`/`register` یک توکن واقعی و مستقل صادر می‌کنه (۳۰ روز اعتبار)
- میدلور جدید هر درخواست رو مستقل بررسی می‌کنه (`Authorization: Bearer <token>`) — دیگه بستگی به یک «کاربر فعال سراسری» مشترک نداره
- **سازگار با گذشته**: اگه توکنی ارسال نشه (مثل سایت فعلی که هنوز توکن نمی‌فرسته)، به همون روش قدیمی (تنظیمات `activeUsername`) برمی‌گرده — یعنی سایت React فعلی بدون هیچ تغییری هنوز کار می‌کنه
- Endpoint جدید `GET /api/auth/me` برای اپ موبایل تا وضعیت لاگین رو موقع باز شدن اپ چک کنه
- تمام ۱۰ نقطه‌ای که کاربر فعلی رو صدا می‌زدن (`getCurrentUser`) به‌روزرسانی شدن تا از توکن واقعی درخواست استفاده کنن

### مورد ۲: کل اپ فلاتر به بک‌اند واقعی وصل شد
`models.dart` (تقریباً ۷۶۰ خط) به‌طور کامل بازنویسی شد:
- ورود/ثبت‌نام واقعی با پسورد واقعی (قبلاً حتی فیلد پسورد چک نمی‌شد!)، ذخیره‌ی توکن با `shared_preferences` برای باقی موندن لاگین بعد از بستن اپ
- سیستم‌ها، منوی کافه، فروشگاه، تورنومنت‌ها، مقالات، اتاق‌های چت — همه از API واقعی خونده می‌شن (نه لیست هاردکد)
- رزرو، سفارش کافه، خرید لوازم جانبی، تبدیل امتیاز، ثبت‌نام تیم تورنومنت، ثبت نظر — همه‌شون واقعاً به سرور وصل‌ان و خطای واقعی سرور رو نشون می‌دن
- چت واقعی شد: پیام واقعی ارسال می‌شه، تاریخچه‌ی واقعی لود می‌شه؛ **ربات پاسخ‌دهنده‌ی فیک حذف شد** (قبلاً یک لیست از پیام‌های ساختگی از کاربران ساختگی رو به‌جای پاسخ واقعی نشون می‌داد)
- کد تخفیف واقعی به سیستم رزرو هم اضافه شد (قبلاً فقط کافه/فروشگاه پشتیبانی می‌کرد)

### دو باگ واقعی پیش از این تغییرات که در همین مسیر پیدا و رفع شدند
- **`Colors.emerald`**: در ۶ فایل صفحه استفاده شده بود، در حالی که این رنگ اصلاً در Flutter وجود نداره (یعنی این کد از اول کامپایل نمی‌شد!). به `Colors.green` اصلاح شد.
- **`py: N`**: در ۹ جای مختلف به‌جای `vertical: N` نوشته شده بود (احتمالاً باقی‌مونده‌ی عادت Tailwind CSS) — این هم باعث خطای کامپایل می‌شد. اصلاح شد.

### صفحه‌ی صفحه لودینگ واقعی
موقع باز شدن اپ، یک لودینگ واقعی («در حال اتصال به سرور بازینو...») نشون داده می‌شه تا سشن قبلی (اگه توکن ذخیره‌شده باشه) و دیتای واقعی لود بشه — به‌جای نمایش فوری یک UI با دیتای ساختگی.

### ⚠️ باز هم صادقانه بگم چی باقی مونده
1. **هنوز نمی‌تونم Flutter رو کامپایل کنم.** با دقت زیاد نوشتم و balance براکت‌ها رو چک کردم، ولی فقط `flutter pub get && flutter run` واقعی می‌تونه تضمین بده.
2. **چت زنده real-time نیست، polling هست** — چون اپ فلاتر یک اتصال دائمی WebSocket نگه نمی‌داره (بر خلاف سایت)، هر ۴ ثانیه پیام‌های جدید رو می‌گیره. برای real-time واقعی باید پکیج `web_socket_channel` هم اضافه بشه (کار جداییه).
3. **دکمه‌ی خروج (logout) در UI اپ فلاتر اصلاً وجود نداشت** (حتی قبل از این تغییرات) — متد `logout()` واقعی توی `AppState` هست، فقط جایی توی صفحات بهش وصل نشده.
4. کد تخفیف نمونه‌ی هاردکدشده‌ی `GAMER2026` توی صفحه‌ی رزرو، چون به‌صورت پیش‌فرض توی دیتابیس واقعی وجود نداره، خطای «کد تخفیف معتبر نیست» می‌ده — رفتار صادقانه‌ست ولی شاید بخواید این کد رو حذف کنید یا واقعاً بسازیدش.

---

## ۹. دو مورد آخر — تکمیل شد

### چت واقعاً real-time شد (نه polling)
پکیج `web_socket_channel` اضافه شد. اپ فلاتر الان دقیقاً به همون endpoint وب‌سوکتی که سایت استفاده می‌کنه وصل می‌شه (`/api/chat/ws`) و پیام‌های جدید (از سایت، از کاربر دیگه‌ای روی اپ، از هرجایی) **فوری و واقعی** می‌رسن — بدون نیاز به polling هر چند ثانیه. اگه اتصال قطع بشه، خودش هر ۵ ثانیه دوباره وصل می‌شه.

### دکمه‌ی خروج (Logout) اضافه شد
توی صفحه‌ی باشگاه مشتریان، کنار نام کاربری، یک دکمه‌ی خروج واقعی اضافه شد — با یک دیالوگ تاییدیه، که واقعاً به سرور اطلاع می‌ده و توکن رو از حافظه‌ی گوشی پاک می‌کنه.

### وضعیت نهایی بعد از این دو مورد
هر ۴ محدودیتی که قبلاً لیست کرده بودم الان کامل برطرف شدن. تنها محدودیت واقعی باقی‌مانده همچنان اینه: **من نمی‌تونم Flutter رو کامپایل کنم** — قبل از استفاده‌ی واقعی حتماً باید `flutter pub get && flutter run` رو تست کنید.

---

## ۱۰. مورد ۱۷ (چک‌لیست GameCenter.md) — تولد مشتری و تخفیف — تکمیل شد

### مشکل قبلی
`getBirthdayFlags(birthDate, now)` در `utils/formatters.ts` نوشته شده بود ولی هیچ‌جا در `App.tsx` مصرف نمی‌شد؛ `customer.isBirthdayToday`/`isBirthdayThisMonth` همیشه `undefined` بودن، پس نه بج تولد در `CustomerManagement`/`StationModal` نشون داده می‌شد و نه تخفیف ۱۰٪ تولد در `CheckoutModal` هیچ‌وقت فعال می‌شد.

### تغییرات این batch (`Management App/Bazino/src/App.tsx`)
- یک `useMemo` جدید به نام `customersWithBirthdayFlags` بلافاصله بعد از افکت persistence اضافه شد که روی هر رندر تغییر `customers`، از روی `birthDate` واقعی، `isBirthdayToday`/`isBirthdayThisMonth` رو با `getBirthdayFlags` محاسبه می‌کنه.
- `birthdayCountToday` (که به `Header` پاس داده می‌شه) از `customersWithBirthdayFlags` محاسبه می‌شه.
- پراپ `customers` در سه جا (`StationModal`, `CheckoutModal`, `CustomerManagement`) به `customersWithBirthdayFlags` تغییر کرد.
- جاهایی که مستقیم state خام رو می‌نویسن (`setCustomers`, ذخیره‌سازی در `localStorage`/`/api/state`) بدون تغییر موندن — عمدی، چون فقط `birthDate` باید ذخیره بشه، نه فیلدهای مشتق‌شده.

### تست انجام‌شده
چون `node_modules` نصب نیست، کامپایل کامل ممکن نشد. با `esbuild --jsx=automatic` (باینری موجود در `tsx` پکیج گلوبال) روی `App.tsx` چک سینتکسی TSX انجام شد → **بدون خطا (exit 0)**. این فقط سینتکس رو تضمین می‌کنه؛ رفتار واقعی در مرورگر تست نشده — قبل از استفاده `npm install && npm run dev` رو اجرا کنید.

### وضعیت
✅ مورد ۱۷ کامل. کار بعدی طبق `HANDOFF_CONTINUE_HERE.md`: مورد ۳ (تکرار آلارم هشدار).

---

## ۱۱. مورد ۳ (چک‌لیست GameCenter.md) — تکرار آلارم — تکمیل شد

### مشکل قبلی
در تایمر اصلی `App.tsx`، وقتی ایستگاه به `FINISHED` می‌رسید، `playAlarmSound` فقط یک‌بار صدا زده می‌شد و چون بعد از FINISHED دیگه شرط `status === 'PLAYING'` برقرار نیست، هیچ تکراری اتفاق نمی‌افتاد — حتی اگه اپراتور دقیقه‌ها متوجه نمی‌شد.

### تغییرات این batch
- `types.ts`: فیلد `lastAlarmAt?: number` به `StationSession` اضافه شد.
- `App.tsx`: در تایمر ثانیه‌ای، یک شاخه‌ی جدید برای ایستگاه‌های `FINISHED` اضافه شد که هر `soundConfig.repeatIntervalSeconds` ثانیه (مقدار پیش‌فرض ۱۵ در `mockData.ts`) دوباره `playAlarmSound` رو صدا می‌زنه، تا وقتی چک‌اوت انجام بشه (که `activeSession` رو پاک می‌کنه و چرخه متوقف می‌شه).

### تست انجام‌شده
`esbuild --jsx=automatic` روی `App.tsx` و `types.ts` → بدون خطای سینتکسی. رفتار واقعی در مرورگر تست نشده (نبود `node_modules`).

### وضعیت
✅ مورد ۳ کامل. کار بعدی طبق `HANDOFF_CONTINUE_HERE.md`: مورد ۱۲ (اعمال واقعی تم‌های گرافیکی) — قبل از شروع scope رو با کاربر هماهنگ کنید.

---

## ۱۲. مورد ۱۲ (چک‌لیست GameCenter.md) — اعمال واقعی ۲۰ تم گرافیکی — تکمیل شد (اسکوپ: کل اپ)

### تصمیم اسکوپ
از کاربر پرسیده شد: فقط رنگ‌های اصلی، یا کل اپ کاملاً داینامیک؟ پاسخ: **کل اپ (همه‌ی کامپوننت‌ها)**.

### راه‌حل فنی
به‌جای جایگزینی دستی ۱۰۰۰+ کلاس Tailwind هاردکد (`amber-*`, `zinc-*`) در ۱۷ فایل کامپوننت، از معماری CSS-variable خود Tailwind v4 استفاده شد:
- فایل جدید: `src/utils/theme.ts` — یک اسکیل کامل Tailwind-مانند (۵۰ تا ۹۵۰) از روی `theme.primaryColor` (برای amber) و `theme.bgColor`/`theme.cardBg` (برای zinc، با انکر دقیق روی ۹۵۰/۹۰۰) تولید می‌کنه، و با `applyAppTheme()` روی `document.documentElement.style` override می‌کنه.
- `App.tsx`: `useEffect([currentTheme])` جدید که `applyAppTheme` رو صدا می‌زنه.
- نتیجه: با تغییر `currentTheme`، تمام کلاس‌های `bg-amber-500`, `text-zinc-400`, `border-zinc-800`, `shadow-amber-500` و... در تمام ۱۷ کامپوننت + `App.tsx` بدون تغییر حتی یک `className` رنگشون عوض می‌شه.

### تصمیم طراحی مهم: `yellow-*` دست‌نخورده موند
`yellow-400`/`yellow-500` در این اپ دو نقش معنایی (نه تزئینی) داره: چراغ وضعیت WARNING (۵ دقیقه مونده) در `StationCard.tsx`، و دکمه‌ی «minimize» سبک macOS در `Header.tsx`. اگه این رنگ هم override می‌شد، برای تم‌هایی مثل «نئون سایبرپانک» (primary=آبی/فیروزه‌ای)، چراغ هشدار WARNING دیگه زرد نمی‌موند و از بین می‌رفت. به‌جاش، ۶ نقطه‌ی تزئینی که گرادینت amber→yellow برای جلوه‌ی طلایی داشتن (نه معنای هشدار)، به amber→amber تغییر کردن:
`App.tsx`، `Header.tsx` (۲ جا: لوگو و بج امتیاز)، `OperatorPermissions.tsx`، `StationCard.tsx` (دکمه‌ی اصلی)، `StationModal.tsx` (دکمه‌ی اصلی).

### نکته‌ی رفتار پیش‌فرض
تم پیش‌فرض (gold-crown): `bgColor`/`cardBg` دقیقاً برابر هگز اصلی `zinc-950`/`zinc-900` هستن → ظاهر پیش‌فرض بدون تغییر می‌مونه. `primaryColor` (#EAB308) با `amber-500` اصلی Tailwind (#f59e0b) یکی نیست → حتی تم پیش‌فرض هم کمی به‌سمت طلایی واقعی‌تر شیفت می‌کنه؛ این عمدی و درسته چون خود تم این مقدار رو به‌عنوان «طلایی رسمی» تعریف کرده.

### تست انجام‌شده
`esbuild --jsx=automatic` روی **تمام فایل‌های `src`** (نه فقط تغییریافته‌ها) اجرا شد → بدون خطای سینتکسی در هیچ فایلی. **در مرورگر واقعی تست نشده** (نبود `node_modules`/شبکه در sandbox) — قبل از استفاده حتماً `npm install && npm run dev`، چند تم رو عوض کنید، و مطمئن شوید چراغ WARNING هنوز زرد می‌مونه.

### وضعیت
✅ مورد ۱۲ کامل (اسکوپ: کل اپ). کار بعدی طبق `HANDOFF_CONTINUE_HERE.md`: مورد ۲۳ (پشتیبان‌گیری خودکار روزانه).

---

## ۱۳. مورد ۲۳ (چک‌لیست GameCenter.md) — پشتیبان‌گیری خودکار روزانه — تکمیل شد

### تغییرات
- `App.tsx`: `backupSettings` حالا واقعاً در `localStorage` (`bazino_backup_settings`) persist می‌شه (قبلاً هیچ‌وقت ذخیره/بازیابی نمی‌شد).
- `buildBackupPayload()` مشترک بین export دستی و بک‌آپ خودکار.
- `handleAutoBackup()`: به‌جای دانلود خودکار فایل (که مرورگرها معمولاً بدون تعامل کاربر مسدود می‌کنن)، یک snapshot تاریخ‌دار در `localStorage` ذخیره می‌شه؛ فقط ۷ روز آخر نگه داشته می‌شه.
- `useEffect` هر ۱ دقیقه چک می‌کنه که آیا ۲۴ ساعت از `lastBackupTime` گذشته و `autoDailyBackup` فعاله.
- **باگ stale-closure رفع شد:** بدون `useRef`، تایمر دقیقه‌ای می‌تونست بعد از ۲۴ ساعت بی‌کاری، داده‌ی روز mount رو به‌جای داده‌ی واقعی روز بک‌آپ ذخیره کنه. با یک ref که هر رندر آپدیت می‌شه رفع شد.
- `SettingsThemesModal.tsx`: سوییچ فعال/غیرفعال + نمایش «آخرین پشتیبان خودکار» اضافه شد (prop از قبل تعریف شده بود ولی UI نداشت).

### تست انجام‌شده
`esbuild --jsx=automatic` روی کل `src` → بدون خطا. **رفتار واقعی تست نشده** (نبود `node_modules`).

### وضعیت
✅ مورد ۲۳ کامل. کار بعدی: مورد ۲۷ (ریست روزانه‌ی ساعت کارکرد ایستگاه).

---

## ۱۴. مورد ۲۷ (چک‌لیست GameCenter.md) — ریست روزانه‌ی ساعت کارکرد ایستگاه — تکمیل شد

### تغییرات
- `App.tsx`: state جدید `lastServiceHoursResetDate` (persist شده در `bazino_service_hours_reset_date`).
- `useEffect` هر ۱ دقیقه چک می‌کنه تاریخ امروز با تاریخ ریست آخر فرق داره یا نه؛ اگه آره، `totalServiceHoursToday` همه‌ی ایستگاه‌ها صفر می‌شه.
- از `setStations` با فرم تابعی استفاده شد، پس این افکت از اول نیازی به `stations` در dependency array نداشت — بدون ریسک stale-closure (برخلاف مورد ۲۳ که این باگ رو داشت و رفع شد).

### تست انجام‌شده
`esbuild --jsx=automatic` روی کل `src` → بدون خطا. رفتار واقعی تست نشده (نبود `node_modules`).

### وضعیت
✅ مورد ۲۷ کامل. کار بعدی: مورد ۲۸ (اتصال واقعی `/api/state` به بک‌اند اصلی).

---

## ۱۵. مورد ۲۸ (چک‌لیست GameCenter.md) — اتصال واقعی `/api/state` — تکمیل شد

### تغییرات
- `server.ts` (بک‌اند اصلی): `GET /api/state` و `POST /api/state` اضافه شد، کنار `/api/admin/settings` موجود، با همون الگو و همون مکانیزم عمومی `getSetting`/`setSetting` از `dataProviders.ts`. کل بلوب state زیر کلید `managementAppState` ذخیره می‌شه.
- تأیید شد Management App هیچ آدرس هاردکدی نداره (همه‌ی `fetch`ها relative path هستن) — در production که هر دو از یک origin سرو می‌شن مشکلی نیست.
- **نکته‌ی کشف‌شده، خارج از اسکوپ، دست‌نخورده گذاشته شد:** `vite.config.ts` داخل `Management App/Bazino` برای dev مستقل هیچ `server.proxy` برای `/api` نداره (مثل `/api/sync/*` از قبل).
- این دو endpoint مثل `/api/admin/settings` از `requireAuth` عبور نمی‌کنن — پترن موجود رعایت شد، نکته‌ی امنیتی برای بازبینی بعدی ثبت شد.

### تست انجام‌شده
`esbuild` روی `server.ts` و کل `src` Management App → بدون خطا. رفتار واقعی تست نشده.

## وضعیت کلی: چک‌لیست ۲۸ موردی GameCenter.md کامل تکمیل شد ✅

تمام ۲۸ مورد (۳، ۵، ۹، ۱۲، ۱۴، ۱۶، ۱۷، ۱۸، ۲۳، ۲۵، ۲۶، ۲۷، ۲۸ که نیاز به رفع داشتن + بقیه که از قبل درست بودن) اکنون در وضعیت ✅ هستن. هیچ‌کدوم از تغییرات در یک مرورگر واقعی (`npm run dev`) تست نشدن، چون sandbox این نشست نه `node_modules` داشت نه دسترسی شبکه برای نصبشون — فقط چک سینتکسی با `esbuild` روی تک‌تک فایل‌ها انجام شد. **قبل از استفاده‌ی واقعی، حتماً پروژه رو لوکال بالا بیارید و هر ۲۸ مورد رو دستی تست کنید.**

---

## ۱۶. باگ صفحه‌ی خالی روی مرورگرهای قدیمی/iPhone 14 Pro (سایت اصلی) — بررسی شد و علت اصلی رفع شد

### گزارش کاربر
سایت اصلی روی مرورگرهای قدیمی و iPhone 14 Pro فقط یک صفحه‌ی خالی نشون می‌داد.

### تشخیص
`vite.config.ts` (ریشه‌ی پروژه، نه Management App) با `build.target: 'es2022'` تنظیم شده بود. `@vitejs/plugin-legacy` فقط مرورگرهای بدون پشتیبانی از `<script type="module">` رو به باندل legacy/polyfill‌شده هدایت می‌کنه؛ هر Safari/iOS که از ES modules پشتیبانی کنه (شامل خیلی از نسخه‌های نسبتاً جدید، از جمله احتمالاً دستگاه کاربر) باندل «مدرن» رو دقیقاً با سینتکس ES2022 (بدون تبدیل) می‌گیره. اگه اون نسخه از Safari از سینتکس ES2022 پشتیبانی نکنه، کل اسکریپت module اجرا نمی‌شه (یک SyntaxError کل ماژول رو متوقف می‌کنه) → صفحه‌ی خالی بدون خطای قابل‌مشاهده.

### رفع
`vite.config.ts`: `build.target` از `'es2022'` به `'es2018'` (پوشش خیلی گسترده‌تر) تغییر کرد.

### نکته‌ی جداگانه، عمداً دست‌نخورده (نیاز به تصمیم کاربر)
`src/index.css` از Tailwind v4 استفاده می‌کنه که پیش‌فرض `oklch()`/`@property` تولید می‌کنه — این‌ها فقط Safari 16.4+ رو پشتیبانی می‌کنن. این یه محدودیت CSS جداست، نه لزوماً «صفحه‌ی خالی» بلکه ظاهر بی‌استایل روی Safari زیر ۱۶.۴. رفعش (downgrade به Tailwind v3) تصمیم بزرگیه و به کاربر واگذار شد.

### تست انجام‌شده
فقط esbuild syntax check روی `vite.config.ts`. **در مرورگر واقعی/دستگاه واقعی تست نشده.**

---

## ۱۷. تکمیل رفع باگ مرورگر قدیمی: fallback رنگ oklch (هر دو پروژه)

بعد از رفع باگ JS (بخش ۱۶)، نکته‌ی جداگانه‌ی CSS هم رفع شد: Tailwind v4 پیش‌فرض `oklch()` تولید می‌کنه که Safari زیر ۱۶.۴ نمی‌فهمتش.

### تغییرات (هم در ریشه‌ی پروژه، هم `Management App/Bazino`)
- `postcss.config.js` جدید: پلاگین `@csstools/postcss-oklab-function` با `preserve: true` — قبل از هر `oklch()` یه `rgb()` fallback اضافه می‌کنه؛ مرورگر قدیمی از fallback استفاده می‌کنه، جدید از `oklch()` (کاسکید عادی CSS).
- `package.json` هر دو پروژه: `postcss` + `@csstools/postcss-oklab-function` به `devDependencies` اضافه شد.

### تست انجام‌شده
فقط esbuild syntax check روی فایل‌های config. **کاملاً تست‌نشده** — پکیج‌های جدید نصب نشدن (نبود شبکه در sandbox). **قبل از build، حتماً `npm install` رو در هر دو پوشه اجرا کنید و روی یک Safari/iOS قدیمی‌تر از ۱۶.۴ تست کنید که رنگ‌ها درست نمایش داده می‌شن.**

---

## ۱۸. حذف پوشه‌ی `server/GameNet.*` (ASP.NET) به درخواست کاربر

### تأیید قبل از حذف
با `grep` روی کل پروژه تأیید شد این پوشه هیچ نقش زنده‌ای نداره: هیچ `fetch` واقعی در `src/` به یک سرور ASP.NET اشاره نمی‌کنه، و فیچر «نمایش/دانلود کد C#» (`CsharpCodeViewer.tsx` + `src/data/csharpCode.ts`/`csharpMvcCode.ts`) محتوای کد رو به‌صورت رشته‌ی هاردکد داره، نه import از فایل واقعی — پس با حذف پوشه، این فیچر همچنان کامل کار می‌کنه.

### حذف شد
`server/GameNet.Core`, `server/GameNet.Infrastructure`, `server/GameNet.MVC`, `server/GameNet.WebAPI`, `server/GameNet.sln`. **`server/dataProviders.ts` (بک‌اند واقعی) دست‌نخورده موند.**

### پاک‌سازی جانبی
- `.vscode/tasks.json`: تسک‌های dotnet حذف شدن، فقط `run-dev` (`npm run dev`) موند.
- `.vscode/launch.json`: کانفیگ دیباگ ASP.NET حذف شد، یک کانفیگ Node.js ساده برای `server.ts` جایگزین شد.

### وضعیت
✅ انجام شد. حجم پروژه حدود ۴۰۰ کیلوبایت کد ASP.NET غیرفعال کم شد.

---

## ۱۹. بررسی اتصال Flutter App و Management App به سرور (به درخواست کاربر)

### روش
استخراج تمام مسیرهای `/api/*` که هرکدوم از سه کلاینت (Flutter، Management App، سایت اصلی) صدا می‌زنن، و مقایسه‌ی مستقیم با route های واقعی `server.ts`.

### باگ پیدا و رفع شد: `/api/sync/logs` روی سرور اصلاً وجود نداشت
`WebSyncModal.tsx` این مسیر رو صدا می‌زد (silent fail، تب لاگ‌ها همیشه خالی). رفع شد:
- `server.ts`: آرایه‌ی `syncActivityLogs` + `logSyncEvent()` اضافه شد؛ `/api/sync/webservice` و `/api/sync/reservations/update` حالا لاگ واقعی ثبت می‌کنن؛ `GET /api/sync/logs` جدید اضافه شد.
- `WebSyncModal.tsx`: باگ جانبی — badge وضعیت لاگ همیشه سبز بود صرف‌نظر از `log.status` واقعی؛ رفع شد (WARNING زرد، ERROR قرمز).

### Flutter ↔ سرور
همه‌ی مسیرهای `/api/*` که Flutter صدا می‌زنه روی سرور وجود دارن؛ شکل response لاگین/رجیستر و فرمت هدر Auth دقیقاً مچ می‌کنن. مشکلی پیدا نشد.

### نکته‌ی ثبت‌شده (نه باگ فعلی)
سرور CORS middleware نداره؛ چون Flutter فقط native هست (بدون پوشه‌ی `web/`)، فعلاً مشکلی نیست، ولی اگه در آینده Flutter Web یا کلاینت وب دیگه‌ای اضافه بشه، باید CORS تنظیم بشه.

### تست انجام‌شده
esbuild syntax check → بدون خطا. رفتار واقعی (سیکل کامل sync) تست نشده.

---

## ۲۰. راهنمای پابلیش سایت + تنظیمات دیتابیس ساخته شد

فایل جدید `PUBLISH_AND_DATABASE_GUIDE.md` در ریشه‌ی پروژه — راهنماهای موجود (`PUBLISHING_GUIDE.md`, `CLOUD_BUILD_GUIDE.md`) فقط مخصوص اپ فلاتر بودن، چیزی برای خودِ سایت (React + Express + Management App) وجود نداشت.

### محتوا (همه از کد واقعی استخراج شده)
- معماری: یک پروسه‌ی Node واحد، سایت اصلی + Management App هر دو ازش سرو می‌شن.
- جدول env varها: `JWT_SECRET` (الزامی)، `PORT`، `NODE_ENV`، `GEMINI_API_KEY` (اختیاری).
- مراحل build/start دقیق (`npm run build` → `npm start`).
- هر سه روش دیتابیس: SQLite (پیش‌فرض، بدون تنظیم)، ویزارد نصب داخل سایت، و فرمت دقیق دستی `install-config.json` برای SQL Server/MongoDB (فیلدهای `host`/`port`/`dbName`/`username`/`password` مستقیم از `server/dataProviders.ts` استخراج شد).
- نمونه‌ی Nginx reverse proxy (با تأکید روی هدرهای WebSocket برای چت زنده)، PM2 برای uptime، چک‌لیست امنیتی، جدول troubleshooting.

### رفع جانبی
`dotenv` از قبل نصب بود ولی import نمی‌شد؛ `import "dotenv/config"` به `server.ts` اضافه شد تا فایل `.env` واقعاً کار کنه (بدون این، راهنمای env var بی‌فایده می‌بود).

### تست انجام‌شده
esbuild syntax check روی `server.ts`. محتوای راهنما از کد واقعی استخراج شد، ولی هیچ مرحله‌ای روی سرور واقعی تست نشد.

---

## ۲۱. نسخه‌ی دسکتاپ مستقل Management App + اتصال واقعی به سرور سایت (فیچر بزرگ)

کاربر: Management App وب‌اپه؛ خواست نسخه‌ی دسکتاپ (بک‌اند+دیتابیس داخل خودش، کاملاً مستقل) اضافه بشه که بتونه به سرور سایت وصل بشه و رزروهای آنلاین رو بگیره/بفرسته. بعد از یک سوال روشن‌کننده (پنجره‌ی سبک وصل به سرور خارجی، یا کاملاً مستقل با سرور داخلی؟) کاربر گزینه‌ی دوم رو با قابلیت اتصال به سرور سایت خواست.

### بخش ۱: اتصال واقعی Web Sync (قبلاً کاملاً فیک بود)
- `types.ts`: `WebSyncStatus.apiKey` اضافه شد.
- `utils/syncClient.ts` (جدید): `buildSyncUrl`/`syncHeaders` — پشتیبانی از حالت relative (co-located) و absolute (سرور راه دور).
- `App.tsx`: `webSyncStatus` واقعاً persist می‌شه؛ `isConnected` دیگه همیشه `true` نیست.
- `WebSyncModal.tsx`: تب تنظیمات کامل بازنویسی — آدرس سرور + API Key واقعاً ذخیره/استفاده می‌شن؛ badge وضعیت واقعیه (قبلاً همیشه سبز/متصل بود).
- `server.ts`: CORS اضافه شد؛ middleware `requireSyncApiKey` (اختیاری، backward-compatible) برای مسیرهای `/api/sync/*`؛ `BAZINO_STATIC_ROOT` برای جدا کردن مسیر فایل‌های استاتیک از `cwd` دیتابیس.

### بخش ۲: پوشه‌ی `desktop-app/` (Electron واقعی، جدید)
`main.js` بک‌اند واقعی رو in-process با SQLite محلی اجرا می‌کنه؛ `scripts/prepare-server-bundle.js` build ریشه رو کپی + production deps نصب + `@electron/rebuild` (لازم برای native module مثل better-sqlite3) اجرا می‌کنه؛ `package.json` با کانفیگ کامل electron-builder (win/mac/linux)؛ `README.md` با تأکید صریح روی محدودیت مهم: **هر پلتفرم باید روی خود همون سیستم‌عامل build بگیره** (native module cross-compile نمی‌شه).

### بخش ۳: دانلود واقعی از داخل وب‌اپ
`server.ts`: `GET /api/desktop/availability` + `GET /api/desktop/download/:platform` (فایل واقعی از `desktop-builds/<platform>/` سرو می‌کنه، یا پیام واضح می‌ده — نه لینک خراب). `SettingsThemesModal.tsx`: بخش «نسخه‌ی دسکتاپ مستقل» با ۳ دکمه که فقط پلتفرم‌های واقعاً build‌شده رو فعال نشون می‌دن.

### تست انجام‌شده
esbuild syntax check روی همه‌ی فایل‌های تغییریافته/جدید (Management App کامل، `server.ts`، تمام فایل‌های `desktop-app/`) → بدون خطا. **⚠️ کاملاً تست‌نشده در عمل** — sandbox نه Electron داشت نه اینترنت نه node واقعی برای اجرا. مراحل تست دقیق در `desktop-app/README.md` و بخش ۴.۵ `HANDOFF_CONTINUE_HERE.md` نوشته شده.

---

## ۲۲. پاکسازی کامل پروژه — حذف فایل/پوشه‌های اضافه و تکراری

با `md5sum` (پیدا کردن کپی‌های واقعاً identical) + بررسی دستی هر مورد مشکوک با `grep` (تأیید عدم استفاده قبل از حذف) این موارد حذف شدن:

### حذف شد
- `bun.lock` (ریشه) و `Management App/Bazino/bun.lock` — lockfile قدیمی/اشتباه از اسکفولد اولیه (`"name": "react-example"`, وابستگی نادرست `sqlite3`).
- پوشه‌های خالی اسکفولد اولیه در هر دو محل پاک‌سازی شدند.
- `zip_project.py` (ریشه) — اسکریپت شخصی برای یک zip که هیچ‌جا لینک نشده.
- **`Management App/Bazino/server.ts`** — کشف مهم: یک بک‌اند mock کاملاً مرده (پیاده‌سازی fake از همون Web Sync) که هیچ‌وقت صدا زده نمی‌شد ولی هر build ریشه بی‌جهت کامپایلش می‌کرد.
- `Management App/Bazino/.env.example` و `metadata.json` — کپی/مانیفست بی‌ربط از دوران اپلت مستقل.

### اصلاح شد
`Management App/Bazino/package.json`: حذف dependencies/devDependencies مرده (`express`, `dotenv`, `@google/genai`, `esbuild`, `tsx`, `@types/express`)، اصلاح اسکریپت‌ها (`build` حالا فقط `vite build`)، تغییر نام از `"react-example"` به `"bazino-management-app"`.

### چیزهایی که بررسی شد ولی نگه داشته شد (واقعی/در-استفاده بودن)
فایل‌های presentation (۴تا)، گزارش‌های `public/*.md` در Management App، `BAZINO_PRO_Presentation.md`، `REFERENCE_IMAGE_INFO.md`، `metadata.json` ریشه.

### تست انجام‌شده
grep برای تأیید عدم reference به فایل‌های حذف‌شده در کد زنده؛ esbuild syntax sweep روی `Management App/Bazino/src` → بدون خطا. **⚠️ چون package.json عوض شد، `package-lock.json` قدیمی Management App الان out-of-sync هست — قبل از build بعدی `npm install` تازه بزنید.**

---

## ۲۳. راهنمای تصویری Management App — فیچر ناقص تکمیل شد

بررسی شد: پنل مدیریت سایت این فیچر رو از قبل کامل داشت (`VisualHelpGuide.tsx` + دکمه‌ی هدر + نوار هوشمند بخش در `AdminPanelTab.tsx`)؛ Management App هیچ‌چیزی نداشت. با تأیید کاربر، معادلش ساخته شد:

- `Management App/Bazino/src/components/VisualHelpGuide.tsx` (جدید): ۶ اسلاید (ایستگاه‌ها/بوفه/مشتریان/حسابداری/اپراتورها/تنظیمات)، navigation کامل. ساده‌تر از نسخه‌ی سایت — بدون mockup‌های hotspot‌دار (ریسک بالا/بدون امکان تست بصری).
- `Header.tsx`: دکمه‌ی «راهنما» جدید (باز کردن راهنمای کامل از ابتدا).
- `App.tsx`: نوار «راهنمای این بخش» بالای `<main>`، متن/مقصدش بر اساس `activeTab` تغییر می‌کنه.

### تست انجام‌شده
esbuild sweep روی کل `Management App/Bazino/src` → بدون خطا. ظاهر واقعی تست نشده.

---

## ۲۴. ری‌دیزاین بصری اپ فلاتر — Neon Glass Gaming (سبک PathMD)

کاربر دو تصویر مرجع فرستاد، از ظاهر «ارزون» اپ فلاتر ناراضی بود. پیشنهاد تغییر به React Native رد شد (مشکل ظاهری، نه فریم‌ورک). با تأیید کاربر، بر اساس تصویر ۱ (PathMD) ری‌دیزاین شد، فقط برای اپ فلاتر.

### تغییرات
- `theme.dart`: بازنویسی کامل + رفع باگ واقعی (`darkBackground` تعریف نشده بود، پروژه کامپایل نمی‌شد). ویجت‌های جدید: `GlassCard`, `NeonGradientButton`, `HexagonBadge`, `CircuitBackgroundPainter`.
- `main.dart`: splash بازطراحی شد.
- `auth_screen.dart`: پوسته عوض شد، منطق فرم دست‌نخورده.
- `hub_screen.dart`: bottom nav (glass)، FAB (هگزاگون)، AppBar (glass)، گرادیان پس‌زمینه‌ی کل.
- `REDESIGN_NOTES.md` (جدید): مستند کامل چی عوض شد/نشد.

### تست
فقط بررسی دستی + بالانس پرانتز با پایتون (نه Flutter analyze واقعی، چون SDK در دسترس نبود). **باید حتماً `flutter analyze` + `flutter run` روی دستگاه واقعی اجرا بشه.**

### باقی‌مانده (اختیاری، اگه کاربر خواست)
۹ صفحه‌ی دیگه هنوز glassmorphism نگرفتن (فقط رنگ‌بندیشون خودکار به‌روز شد چون از theme.dart می‌خونن).

---

## ۲۵. رفع باگ گزارش‌شده توسط کاربر: `int cartTotal` نمی‌تونست `num` بگیره

کاربر یک خطای کامپایل واقعی از IDE‌اش فرستاد (`A value of type 'num' can't be assigned to a variable of type 'int'`) و پرسید مربوط به تغییرات این نشسته یا نه.

### بررسی
`grep` تأیید کرد این باگ فقط در `lib/screens/cafe_screen.dart` (خط ۲۸، `int cartTotal = 0;` + `cartTotal += item.price * qty;`) هست — فایلی که **این نشست اصلاً دست نخورده بود** (فقط `theme.dart`, `main.dart`, `auth_screen.dart`, `hub_screen.dart` تغییر کردن). چون `item.price` در `models.dart` نوعش `num` تعریف شده (نه `int`)، این یک باگ از قبل موجود در پروژه بود.

### رفع
`int cartTotal = 0;` → `num cartTotal = 0;` (سازگار با `toLocaleString()` که extension روی `num` تعریف شده).

### نکته برای کاربر
بررسی شد این تنها نمونه‌ی این الگو در کل پروژه‌ست — «۱۶ خطا»یی که کاربر گزارش داد احتمالاً همه از همین یک ریشه (یا چند وقوع تکراری‌ش در گزارش IDE) میان، ولی چون Dart analyzer واقعی در دسترس نبود، از کاربر خواسته شد اگه بعد از این رفع، خطای دیگه‌ای موند، کل لیست رو بفرسته.

---

## ۲۶. رفع ۷ خطای دیگر گزارش‌شده توسط کاربر (Dart Analyzer) — همه از قبل موجود بودن

کاربر خروجی کامل analyzer رو فرستاد (۵ خطا از ۲ فایل). همه بررسی و رفع شدن، و در همون بررسی ۲ باگ مشابه‌ی دیگه هم پیدا شد که کاربر گزارش نکرده بود ولی همون الگو رو داشتن:

1. `reservation_screen.dart:25` — `int totalRate` نمی‌تونست `num` (`hourlyRate`) بگیره → به `num` تغییر کرد.
2. `messages_screen.dart:58` و `147` — `RoundedRectangleBorder.side` یک `Border.all(...)` می‌گرفت (باید `BorderSide` باشه) → به `BorderSide(...)` تغییر کرد.
3. `messages_screen.dart:297` — `Colors.white80` وجود نداره (تایپوی احتمالی) → به `Colors.white.withOpacity(0.8)` تغییر کرد (و `const` از اون `TextStyle` برداشته شد چون دیگه constant نیست).
4. **پیدا شد اضافه (کاربر گزارش نکرده بود):** `home_screen.dart:57` همون باگ `Colors.white80` رو داشت — رفع شد (با اینکه این فایل جایی import نمی‌شه، analyzer همه‌ی فایل‌های `lib/` رو چک می‌کنه پس همچنان تو گزارش IDE ظاهر می‌شد).
5. **پیدا شد اضافه:** `shop_screen.dart:144` — `Colors.white15` هم وجود نداره → به `Colors.white.withOpacity(0.15)` تغییر کرد.

با `grep` روی کل `lib/` تأیید شد این‌ها تنها نمونه‌های این الگوها بودن (نه `Border.all` اشتباه دیگه‌ای مونده، نه `Colors.whiteNN`/`blackNN` نامعتبر دیگه‌ای).

### تست
فقط بالانس پرانتز با پایتون (نه Flutter analyze واقعی). **همچنان باید کاربر خودش `flutter analyze` رو اجرا کنه تا مطمئن بشیم لیست کامله.**

---

## ۲۷. رفع دسته‌جمعی اخطار deprecated_member_use (`withOpacity` → `withValues`)

کاربر گزارش داد ۱۰۲ اخطار (نه خطا) از نوع `deprecated_member_use` می‌گیره؛ نمونه‌ی فرستاده‌شده `withOpacity` بود (Flutter جدید `withValues(alpha:)` رو جایگزینش کرده، برای دقت رنگی بهتر با wide-gamut).

### رفع
با یک اسکریپت پایتون (regex `\.withOpacity\(([^()]*)\)` → `.withValues(alpha: \1)`)، تمام ۸۶ نمونه در کل `lib/` (۱۲ فایل) جایگزین شد. قبلش تأیید شد هیچ‌کدوم آرگومان تودرتو (پرانتز داخل پرانتز) نداشتن، پس این جایگزینی مکانیکی امن بود.

فایل‌های تغییریافته: `theme.dart` (۸)، `hub_screen.dart` (۱۶)، `jarvis_assistant.dart` (۱۹)، `chat_screen.dart` (۱۴)، `messages_screen.dart` (۱۰)، `home_screen.dart` (۵)، `auth_screen.dart` (۴)، `loyalty_screen.dart` (۳)، `reservation_screen.dart` (۳)، `tournament_screen.dart` (۲)، `shop_screen.dart`/`blog_screen.dart` (۱ هرکدوم).

همچنین چک شد چند الگوی رایج دیگر deprecation (`MaterialStateProperty`، `textScaleFactor`، `RaisedButton`/`FlatButton`، `WillPopScope`) — هیچ‌کدوم در پروژه پیدا نشد.

### تست
بالانس پرانتز با پایتون روی کل `lib/` → بدون مشکل. **۸۶ از ۱۰۲ اخطار پوشش داده شد** — اگه ۱۶ تای باقی‌مونده از نوع دیگه‌ای هستن، کاربر باید لیست کاملشون رو بفرسته.

---

## ۲۸. رفع خطای `notifyListeners()` نامعتبر (protected member) در messages_screen.dart

کاربر گزارش داد `messages_screen.dart:77` مستقیم `appState.notifyListeners()` رو از بیرون کلاس `AppState` صدا می‌زد — این متد `@protected`/`@visibleForTesting` روی `ChangeNotifier` هست و فقط از داخل خودِ کلاس قابل‌فراخوانیه.

### رفع
- `models.dart`: متد عمومی جدید `removeNotificationAt(int index)` به `AppState` اضافه شد که خودش داخل کلاس `notifyListeners()` رو صدا می‌زنه.
- `messages_screen.dart`: محل فراخوانی به `appState.removeNotificationAt(index)` تغییر کرد.
- با `grep` تأیید شد این تنها نمونه‌ی این الگو (`notifyListeners()` از بیرون `AppState`) در کل پروژه بود.

### تست
بالانس پرانتز روی هر دو فایل → بدون مشکل.

---

## ۲۹. رفع ۷ لینت باقی‌مونده (غیر از withOpacity، که قبلاً رفع شده بود)

کاربر یک دامپ کامل ۳۰ تشخیص فرستاد. **۱۹ موردش (`withOpacity` در `jarvis_assistant.dart`) قبلاً در همین نشست رفع شده بودن** — با `grep` تأیید شد نسخه‌ی فعلی صفر `withOpacity` داره؛ یعنی اون گزارش IDE کاربر مربوط به نسخه‌ی قدیمی/قبل از دانلود آخرین زیپ بوده، نه باگ جدید.

### ۷ مورد واقعاً جدید رفع شدن
1. `models.dart:83-87` (`curly_braces_in_flow_control_structures`): زنجیره‌ی `if/else if` بدون `{}` در `ChatRoom.fromName` — همه با بلاک درست پیچیده شدن.
2. `main.dart:68,84` (`prefer_const_constructors`): دو `TextStyle` که `const` می‌تونستن باشن (خودِ `Text` نمی‌تونه چون متنش داینامیکه، ولی `style`ش می‌تونه).
3. `jarvis_assistant.dart:54` (`prefer_final_fields`): `_currentResponseText` هیچ‌وقت reassign نمی‌شد → `final`.
4. `jarvis_assistant.dart:137` (`deprecated_member_use`): پارامتر `localeId` مستقیم روی `_speech.listen()` در نسخه‌ی ۷ پکیج `speech_to_text` منسوخ شده؛ به `listenOptions: SpeechListenOptions(localeId: ...)` تغییر کرد.
5. `jarvis_assistant.dart:496` (`prefer_const_declarations`): `final barCount = 12` → `const barCount = 12`.
6. `jarvis_assistant.dart:669,784-788` (`prefer_const_constructors`): دو `TextStyle` دیگه `const` شدن.

### تست
بالانس پرانتز روی هر ۳ فایل تغییریافته → بدون مشکل.

**نکته‌ی مهم برای کاربر:** قبل از فرستادن گزارش خطای بعدی، حتماً اول فایل‌های داخل زیپ جدید رو جایگزین نسخه‌ی محلی‌تون کنید (یا VS Code رو reload کنید) تا گزارش تکراری/قدیمی نفرستید.

---

## ۳۰. جایگزینی کامل flutter_app با نسخه‌ی محلی واقعی کاربر

کاربر یک zip از پروژه‌ی کامل فلاتر محلی‌اش (`flutter_app_final.zip`) فرستاد — شامل `.git`, `.dart_tool` (۱۰۸ مگ)، `build/` (۱۰۴ مگ)، `.idea`، لاگ‌ها، و پوشه‌های پلتفرم کامل (android/ios/web/windows/macos/linux/test) که قبلاً در sandbox ما نبودن.

### کاری که انجام شد
جایگزینی هوشمند (نه کپی کورکورانه‌ی همه‌چیز):
- **جایگزین شد:** `lib/`, `android/`, `ios/`, `pubspec.yaml`/`pubspec.lock`, `analysis_options.yaml`, `.gitignore`, `.metadata`, `README.md`, `bazino_app.iml`, و پوشه‌های پلتفرم جدید (`web/`, `windows/`, `macos/`, `linux/`, `test/`, `.vscode/`, `.github/`).
- **عمداً کپی نشد** (junk محلی/generated): `.git/`, `.dart_tool/` (۱۰۸M)، `build/` (۱۰۴M)، `.idea/`، فایل‌های `flutter_0*.log`، `.flutter-plugins-dependencies`.
- **بعد از کپی، پاک‌سازی جانبی:** `android/.gradle/` (کش محلی Gradle) و `android/local.properties` (شامل مسیرهای محلی ویندوز کاربر، `C:\Users\payman\...`) حذف شدن؛ پوشه‌های `ephemeral/` در `ios`/`macos`/`windows`/`linux` (شامل symlink های مخصوص ماشین کاربر) هم حذف شدن.
- `HANDOFF.md` کاربر (یادداشت شخصی‌شون از یک نشست Claude Code محلی، تأیید می‌کنه اپ وب فلاتر روی پورت ۸۰۸۱ درست اجرا شده) به `LOCAL_RUN_HANDOFF.md` تغییر نام داد تا با مستندات خودمون تداخل نداشته باشه — نگه داشته شد چون تأیید ارزشمندیه که ری‌دیزاین + رفع باگ‌های این نشست واقعاً کار می‌کنن.
- `REDESIGN_NOTES.md` (مستند خودمون) نگه داشته شد.

### نتیجه
اندازه‌ی نهایی `flutter_app/`: ۱.۵ مگابایت (به‌جای صدها مگابایت اگه همه‌چیز کورکورانه کپی می‌شد). با `grep` تأیید شد فایل‌های `lib/` همون فایل‌هایی هستن که رفع‌باگ‌های این نشست توشون اعمال شده (۱۹ `withValues`، صفر `withOpacity`، `removeNotificationAt` موجوده).

### وضعیت
منتظر دستور بعدی کاربر برای ادامه‌ی کار روی این پروژه هستیم.

---

## ۳۱. پاس دوم ری‌دیزاین فلاتر: رفع شکل «کلاسیک/بیضی‌مانند» دکمه‌ها و پنل‌ها

کاربر بعد از دیدن پاس اول گفت رنگ خوبه ولی شکل دکمه/پنل‌ها هنوز کلاسیکه. علت واقعی: ۷ صفحه‌ی باقی‌مونده هنوز `ElevatedButton`/`Card` معمولی Material داشتن؛ چون `useMaterial3: true`، هر دکمه‌ی بدون `shape` صریح پیش‌فرض گرد/pill می‌گیره.

### فایل‌های به‌روزشده
`cafe_screen.dart`, `shop_screen.dart`, `reservation_screen.dart`, `tournament_screen.dart`, `loyalty_screen.dart`, `chat_screen.dart`, `blog_screen.dart` — همه‌ی `Card` ها به `GlassCard` و دکمه‌های اصلی به `NeonGradientButton`/گرادیان تبدیل شدن.

نکته: دکمه‌ی «تایید نهایی» تو دیالوگ `tournament_screen.dart` که Dart analyzer قبلاً بدون `shape` گزارش داده بود، این‌بار هم گرادیان و هم shape گرفت.

### تست
بعد از هر ویرایش، بالانس پرانتز/آکولاد/براکت با پایتون چک شد (چون esbuild نمی‌تونه Dart رو parse کنه) — همه‌ی فایل‌ها بالانس. **هنوز flutter analyze/run واقعی اجرا نشده.**

### خارج از اسکوپ (عمداً دست‌نخورده)
`messages_screen.dart` (از قبل shape مناسب داشت)، `jarvis_assistant.dart` (بدون Card/ElevatedButton، UI با CustomPainter)، `home_screen.dart` (dead code، هیچ‌جا استفاده نمی‌شه).

---

## ۳۲. دامنه‌ی واقعی سایت ست شد: xerxes.biz

کاربر ابتدا رنج‌های IP اشتباه فرستاد (که با بررسی مستندات Render.com مشخص شد اون‌ها outbound IP های Render هستن، نه آدرس سرور — توضیح داده شد و اصلاح شد)، بعد دامنه‌ی واقعی رو داد: `https://xerxes.biz`. با `web_fetch` تأیید شد — صفحه‌ی واقعی «Bazino Game Center | Northern Cyprus' Ultimate Gaming Hub» رو نشون می‌ده.

### تغییرات
- `flutter_app/lib/api_config.dart`: مقدار پیش‌فرض `kApiBaseUrl` از `http://localhost:3000` به `https://xerxes.biz` تغییر کرد.
- `Management App/Bazino/src/components/WebSyncModal.tsx`, `utils/syncClient.ts`, `types.ts`: مثال‌های قدیمی `bazino.runasp.net` (دامنه‌ی قدیمی‌تر) به `xerxes.biz` به‌روزرسانی شدن.
- `PUBLISH_AND_DATABASE_GUIDE.md`: نمونه‌ی Nginx از placeholder ژنریک به دامنه‌ی واقعی تغییر کرد.
- `ARCHITECTURE.md`: یادداشت تاریخی اضافه شد (نه حذف) که دامنه از `bazino.runasp.net` به `xerxes.biz` تغییر کرده.

### تست
esbuild syntax check روی فایل‌های TS تغییریافته → بدون خطا. Dart فایل با بالانس پرانتز چک شد.

---

## ۳۳. بازطراحی سیستم قالب‌بندی (Theming) — هر قالب فایل CSS مجزا + پوشش تمام صفحات

### مشکل قبلی
همه‌ی CSS قالب‌ها در یک فایل `index.css` بود، قالب `console-grid` هیچ CSS نداشت، قالب‌های سفارشی ادمین فقط اسم/رنگ داشتند (بدون استایل واقعی)، و تغییر قالب فقط متغیرهای رنگی را عوض می‌کرد (بقیه صفحات دست‌نخورده می‌ماندند).

### تغییرات
- **`src/themes/`** — موتور جدید قالب‌بندی:
  - هر قالب فایل CSS مستقل خودش را دارد: `dark-gold.css`, `cyberpunk-cyan.css`, `geco-purple.css`, `gaming-amp.css`, `console-grid.css` (+ `gaming-hub.css` legacy)
  - هر فایل «تمام صفحات» را پوشش می‌دهد: متغیرهای رنگی + `.theme-<id> .site-header`, `.bg-dark-card`, `.btn`, input ها, مودال‌ها, اسکرول‌بارها, `::selection`, پس‌زمینه اختصاصی
  - `index.ts` — موتور: فقط CSS قالب فعال در یک `<style>` تزریق می‌شود (تگ قبلی حذف می‌شود)؛ قالب پیش‌فرض ایستا (بدون flash)
  - `themeZipCore.ts` — هسته‌ی مشترک خالص (بدون وابستگی Vite) برای پارس/ساخت ZIP؛ هم کلاینت و هم سرور استفاده می‌کنند
  - `zip.ts` — لایه‌ی کلاینت: `parseThemeZip`, `buildThemeZip`, `buildSampleThemeZip`, `downloadZip`
- **`src/themeSdk/sdk.ts`** — SDK کامپوننت قالب: قالب ZIP می‌تواند `theme.js` داشته باشد که با `window.BazinoThemeSDK.registerComponent('home', factory)` یک صفحه‌ی اصلی اختصاصی ثبت می‌کند (همان قرارداد داده‌ی GecoPurpleHome/GamingAmpHome)
- **`server/themeStore.ts`** — ذخیره‌ساز قالب‌ها روی سرور: هر قالب پوشه‌ی اختصاصی `themes/<id>/` دارد (theme.json + theme.css + theme.js + assets/)؛ حذف قالب = حذف کامل پوشه
- **API های جدید:** `POST /api/admin/themes/install` (آپلود خام ZIP), `GET /api/themes/:id/theme.css`, `GET /api/themes/:id/theme.js`, `GET /api/themes/:id/assets/*`, `GET /api/themes/:id/export`, `DELETE /api/admin/themes/:id`
- **فرمت ZIP جدید:** `theme.json` (اختیاری) + `theme.css` (اجباری) + `assets/` (اختیاری — تصویر/ویدئو/فونت) + `theme.js` (اختیاری — کامپوننت)
- مسیرهای نسبی assets در CSS هنگام سرو بازنویسی می‌شوند: `url('assets/banner.jpg')` ← `/api/themes/<id>/assets/banner.jpg`
- پنل ادمین: نصب از ZIP با پیش‌نمایش متادیتا، «ساخت سریع با رنگ»، دانلود قالب نمونه، خروجی ZIP از هر قالب، نشان تعداد assets

### تست
`scripts/verify-themes.ts`, `scripts/test-theme-store.mts`, `scripts/test-theme-http.mts`, `scripts/test-theme-sdk.mts` — همه پاس. (نیاز به سرور running برای دو تست آخر)

---

## ۳۴. منبع داده نمونه/دیتابیس (Sample ⇄ Database) — پیش‌فرض: نمونه

### تغییرات
- **`server/sampleData.ts`** — منبع واحد داده‌های نمونه (۴-۵ مورد برای هر بخش): سیستم‌ها، کافه، فروشگاه، تورنمنت‌ها، مقالات، اسلایدرها، کدهای تخفیف، تراکنش‌ها، اتاق‌های چت، رزروها، تنظیمات کلوپ
- `server/dataProviders.ts` — seed داده‌ها از همین فایل خوانده می‌شود (یک منبع واحد)
- **حالت نمونه (پیش‌فرض):** همه‌ی لیست‌ها از داده‌های نمونه می‌آیند؛ **حالت دیتابیس:** از دیتابیس، و جدول خالی → فال‌بک خودکار به نمونه
- جستجوی by-id (رزرو/سفارش/ثبت‌نام/نظر/کد تخفیف) در حالت نمونه هم کار می‌کند
- API ها: `GET /api/data-source` (وضعیت + شمارنده‌ها), `POST /api/admin/data-source` (سوییچ)
- پنل ادمین ← سفارشی‌سازی کلوپ ← «منبع داده سایت و اپلیکیشن»: دو کارت قابل‌انتخاب با شمارنده‌ی زنده

---

## ۳۵. قانون لوگوی سایت مادر + پوشه‌ی assets در فرمت قالب

- **لوگو متعلق به سایت مادر است** — هیچ قالبی حق تغییر/مخفی‌کردن آن را ندارد:
  - `public/logo.png` → آدرس استاندارد `/logo.png` (پایدار در dev و production)
  - متغیر CSS سراسری `--brand-logo-url`
  - کلاس `brand-logo-guard` روی لوگوی هدر + قانون CSS که برای هر `[class*='theme-']` نمایش را اجباری می‌کند
  - مستندسازی کامل در `src/themes/README.md` (راهنمای قالب)
- **پوشه‌ی `assets/`** به فرمت ZIP اضافه شد (تصویر/ویدئو/فونت) — استخراج به پوشه‌ی قالب روی سرور، سرو از `/api/themes/<id>/assets/*`
- **کامپوننت قالب (`theme.js`)** — قالب‌های ZIP می‌توانند صفحه‌ی اصلی اختصاصی با SDK داشته باشند (مثل GecoPurpleHome)

---

## ۳۶. حذف برندینگ Google AI Studio از کل پروژه

جستجوی سراسری `google ai studio | aistudio | AI Studio` → صفر نتیجه. فایل‌های اصلاح‌شده: `index.html` (تایتل → «Bazino Pro | بازینو پرو — گیم‌نت و کلوپ گیمینگ»), README ها, `.env.example`, `server.ts` (User-Agent → bazino-pro-server), `AdminPanelTab.tsx` (راهنمای Gemini API → محیط میزبانی + لینک console.cloud.google.com), `PresentationTab.tsx`, `vite.config.ts`, `package.json` (نام → bazino-pro).

---

## ۳۷. CI — ورک‌فلو بیلد و تست بک‌اند + فرانت‌اند

- `.github/workflows/build-test.yml` (فعال‌سازی دستی از `ci/build-test.workflow.yml` — GitHub App permission «workflows» ندارد):
  - `typecheck-and-tests`: tsc + تست‌های موتور/ذخیره‌ساز قالب
  - `frontend-build`: vite build + بررسی dist
  - `backend-build`: rebuild بهتر-sqlite3 + esbuild + بوت production + smoke تست ۹ endpoint
- رفع باگ: `createRequire` در `server/dataProviders.ts` fallback به مسیر مطلق (باندل CJS قبلاً boot نمی‌شد)
- رفع باگ CI: `flutter_app` به exclude تایپ‌چک اضافه شد؛ job بک‌اند public را در dist کپی می‌کند (لوگو 404 → 200)

---

## ۳۸. بهینه‌سازی عملکرد بر اساس گزارش GTmetrix (Performance 75 → هدف 90+)

گزارش واقعی GTmetrix (۱۰ آگوست): Performance 75 | Structure 92 | LCP 1.7s | TBT 196ms | CLS 0.05 | حجم کل 2.08MB (۱.۶۷MB تصویر!)

### رفع‌شده
- **تصاویر:** لوگو ۸۴۰KB→۱۵KB (WebP 256px)، پس‌زمینه ۳.۵MB کرپت→۱۸KB JPG، bg.png کرپت→۲۷۶KB JPG
- **کد اسپلیتینگ:** React.lazy برای همه‌ی تب‌ها/مودال‌های غیر landing — باندل اصلی 1.1MB→345KB (gzip 298→109KB)
- **کش:** assets هش‌شده immutable یک‌ساله، بقیه ۷ روز + ETag
- **فونت‌ها:** از `@import` به `<link>` در HTML + کاهش وزن‌ها + preconnect (fonts, gstatic, unsplash, dicebear)
- **Lazy loading:** ۳۷ تصویر خارجی + فقط اسلاید فعال hero eager
- **width/height صریح** روی لوگوها (کاهش CLS)

### باقی‌مانده (پیکربندی هاست، نه کد)
TTFB 240ms و ریدایرکت 307 → تنظیمات Cloudflare؛ بیکن Cloudflare Analytics؛ انیمیشن‌های غیرکامپوزیت کوچک.

---

## ۳۹. وضعیت نهایی اسناد

- `ARCHITECTURE.md` — چک‌لیست وضعیت به‌روز (همه موارد اصلی ✅)، بخش هاستینگ (bazino.pro + Cloudflare)، تاریخچه
- `README.md` — بازنویسی کامل با معرفی بازینو پرو، امکانات، اجرا، تست‌ها، تنظیمات، ساختار
- `src/themes/README.md` — راهنمای کامل موتور قالب‌بندی (فرمت ZIP، assets، لوگو، کامپوننت، API ها)
- `ci/README.md` — راهنمای فعال‌سازی ورک‌فلو CI

---

## ۴۰. دامنه‌ی سرور اپ فلاتر: xerxes.biz → bazino.pro

کاربر اعلام کرد سرور اپ فلاتر باید `https://bazino.pro` باشد (دامنه‌ی فعلی سایت، پشت Cloudflare).

### تغییرات
- `flutter_app/lib/api_config.dart`: مقدار پیش‌فرض `kApiBaseUrl` از `https://xerxes.biz` به `https://bazino.pro` تغییر کرد. (WebSocket چت به‌صورت خودکار `wss://bazino.pro/api/chat/ws` می‌شود — نیازی به تغییر جداگانه نیست.)
- `Management App/Bazino` (و کپی داخل `flutter_app/`): متن راهنما و placeholder مودال Web Sync + کامنت‌های `types.ts` / `syncClient.ts` از `xerxes.biz` (و `bazino.runasp.net` در نسخه‌ی قدیمی‌تر) به `bazino.pro` به‌روزرسانی شدند.
- `ARCHITECTURE.md` (و کپی داخل `flutter_app/`): یادداشت دامنه‌ی فعلی `bazino.pro` + تاریخچه‌ی دامنه (runasp.net → xerxes.biz → bazino.pro).
- `PUBLISH_AND_DATABASE_GUIDE.md`: مثال Nginx (`server_name` و `certbot -d`) به `bazino.pro` به‌روزرسانی شد.

### نکته
- از sandbox نمی‌توان به `bazino.pro` دسترسی گرفت (HTTP 000 — محدودیت شبکه‌ی sandbox)؛ تست زنده روی خود دامنه انجام شود.
- مقدار پیش‌فرض طبق درخواست کاربر با اسلش انتهایی ثبت شد (`https://bazino.pro/`) اما `api_config.dart` یک نرمال‌سازی دارد: اسلش(های) انتهایی حذف می‌شوند (`_kApiBaseUrlConfigured` → `kApiBaseUrl`) تا الحاق رشته‌ای `'$kApiBaseUrl/api/...'` هرگز `//api` نسازد. پس هر دو حالت (با یا بدون اسلش، چه در `--dart-define` چه در پیش‌فرض) امن است.

---

## ۴۱. بررسی کامل گزارش GTmetrix (bazino.pro — ۱۱ آگوست ۲۰۲۶) + رفع LCP lazy + تایپ‌چک واقعی هر دو پروژه

### ۴۱.۱ تحلیل گزارش GTmetrix (`GTmetrix-report-bazino.pro-20260811T132722-56nZVqpM.pdf`)

گزارش ۵ صفحه‌ای از `https://bazino.pro/` — تست از Seattle (Chrome 142, Lighthouse 12.6.1):

| معیار | مقدار | حد خوب | وضعیت |
|---|---|---|---|
| Performance | 54% | — | ❌ ضعیف |
| Structure | 93% | — | ✅ خوب |
| FCP | 582ms | ≤0.9s | ✅ |
| LCP | 2.0s | ≤2.5s | ✅ |
| TTI | 2.0s | ≤2.5s | ✅ |
| Speed Index | 4.4s | ≤1.3s | ❌ |
| TBT | 729ms | ≤150ms | ❌ عامل اصلی افت امتیاز |
| CLS | 0.07 | ≤0.1 | ✅ |
| TTFB | 329ms (Backend 293ms) | ≤600ms | ✅ |
| Onload / Fully Loaded | 484ms / 2.0s | — | ✅ |
| حجم | 1.19MB / 50 req | — | IMG 808KB، JS 189KB، Font 121KB |

**Top Issues:** ① Med: «LCP was lazy loaded» ② Med-Low: cache policy (فقط 4.52KB) ③ Low: 8 long task (266ms JS, main-thread 1.9s) ④ Low: 2 layout shift ⑤ Low: properly size images (149KB) + کاهش unused JS (74.5KB) + 18 critical chain.

**تطبیق با کد:** هش‌های باندل در آبشار (`index-L-i_-Z0o.js`, `VisualHelpGuide-6rWFBpdW.js` و...) دقیقاً برابر خروجی `vite build` محلی هستند → کد ریپو = نسخه‌ی دیپلوی‌شده در زمان تست. Cache استاتیک از قبل درست است (`server.ts` L2778: `public,max-age=31536000,immutable` + etag). کش 4.52KB باقی‌مانده مربوط به unsplash/API است نه سایت.

### ۴۱.۲ ریشه‌ی LCP lazy و رفع

- تم پیش‌فرض dark-gold در مسیر اصلی `HomeTab.tsx` از قبل `loading="eager"` + `fetchpriority="high"` دارد (اسلاید فعال — اصلاح قبلی ✅).
- **مشکل واقعی:** `GamingAmpHome.tsx:72` — hero این تم با `loading="lazy"` بود؛ اگر مدیر سایت تم gaming-amp را فعال کند، LCP lazy می‌شود (و همین در تست GTmetrix دیده شده).
- `DarkGoldHome.tsx` دو hero با `loading="lazy"` داشت (فایل در حال حاضر import نمی‌شود ولی برای آینده اصلاح شد).
- `GecoPurpleHome.tsx` از CSS `bg-[url(...)]` استفاده می‌کند (نه `<img lazy>`) — بدون مشکل LCP.

**رفع:** هر ۳ hero → `loading="eager"` + `fetchpriority="high"` + کاهش `w=1920→1600` (کمک به Properly size images).

### ۴۱.۳ یافته‌ی مهم: `@types/react` در devDependencies Management App وجود نداشت!

با نصب `node_modules` و اجرای واقعی `tsc --noEmit` مشخص شد:
- `@types/react`/`@types/react-dom` در `Management App/Bazino/package.json` نبود → `npm run lint` (= `tsc --noEmit`) در هر سیستم تمیزی ۳ خطای مجازی در `ErrorBoundary.tsx` می‌داد (فقط چون تایپ react resolve نمی‌شد، نه باگ واقعی کد).
- بعد از `npm install -D @types/react @types/react-dom`: خطاهای ErrorBoundary رفت و **۲ خطای تایپی واقعی پنهان نمایان شد** در `App.tsx` (تایمر مورد ۳):
  - `let nextStatus = st.status;` — TypeScript نوع را از باریک‌سازی به literal `'PLAYING'` محدود می‌کرد → تعیین تایپ صریح `let nextStatus: StationStatus = st.status;` + import `StationStatus`.
- نتیجه: `tsc --noEmit` هر دو پروژه (ریشه + Management App) → **صفر خطا** ✅
- بیلد production هر دو پروژه → **موفق** ✅

### ۴۱.۴ فایل‌های تغییر یافته

- `src/components/GamingAmpHome.tsx` — hero eager + fetchpriority + w=1600
- `src/components/DarkGoldHome.tsx` — دو hero همین‌طور
- `Management App/Bazino/package.json` + `package-lock.json` — @types/react و @types/react-dom اضافه شد
- `Management App/Bazino/src/App.tsx` — رفع ۲ خطای تایپی nextStatus

### ۴۱.۵ پیشنهاد برای ادامه

1. **TBT 729ms بزرگ‌ترین فرصت** است (نه LCP): 8 long task از رندر اولیه + fetchهای موازی ۱۱ API + انیمیشن‌ها. گزینه: `defer`/batch کردن fetchها یا اسپلیت بیشتر باندل اصلی (345KB گزاف نیست ولی recharts/مابقی بررسی شود).
2. Speed Index 4.4s با Onload 484ms تناقض دارد → محتوای بعد از onload (تصاویر پایین‌تر + فونت) دیر paint می‌شود؛ کاهش حجم unsplash ها کمک می‌کند.
3. بعد از دیپلوی، دوباره GTmetrix بگیرید. تست از Seattle انجام شده؛ برای ایران/ترکیه تست از Frankfurt/Amsterdam واقعی‌تر است.
4. **اختیاری:** `HANDOFF_CONTINUE_HERE.md` هنوز قدیمی است (۲۸ مورد را ناتمام نشان می‌دهد) — با وضعیت واقعی (همه ✅) هماهنگ شود.

---

## ۴۲. بهینه‌سازی TBT بر اساس گزارش GTmetrix (پاس دوم) — مودال‌های lazy، vendor splitting، انیمیشن‌ها

پس از تحلیل گزارش GTmetrix (بخش ۴۱)، به‌درخواست کاربر هر سه مورد اجرا شد: بهینه‌سازی TBT + هماهنگ‌سازی سند Handoff + مستندسازی برای تست GTmetrix مجدد.

### ۴۲.۱ شرطی کردن mount مودال‌های lazy — بزرگ‌ترین برد (App.tsx)

**مشکل:** `VisualHelpGuide`, `AuthModal`, `ThemeSelectorModal` lazy بودند ولی بدون شرط داخل `<Suspense>` mount می‌شدند. React به‌محض رندر شدن کامپوننت lazy، چانکش را **در startup** دانلود/اجرا می‌کند — دقیقاً همان چیزی که در Waterfall گزارش دیده می‌شد (`ThemeSelectorModal-…` 135KB + `ThemeScreenshot` + `AuthModal` + `VisualHelpGuide` + `motion` که فقط داخل ThemeSelectorModal است). این سهم بزرگی در TBT 729ms داشت.

**رفع:** `{isHelpOpen && ...}` / `{isAuthModalOpen && ...}` / `{isThemeModalOpen && ...}` — هر سه مودال داخلاً `return null` می‌کنند وقتی بسته‌اند، پس بدون تغییر رفتار، چانک‌ها فقط با اولین باز شدن دانلود می‌شوند.

### ۴۲.۲ Defer کردن fetchهای غیربحرانی (App.tsx)

`fetchData` قبلاً ۸ درخواست را با `Promise.all` هم‌زمان می‌فرستاد. حالا:
- **بحرانی** (systems/cafe/accessories/tournaments — محتوای LCP) همان‌جا می‌ماند.
- **غیربحرانی** (articles/transactions/coupons/user) با helper جدید `scheduleIdle` (requestIdleCallback با fallback setTimeout 2000ms) بعد از اولین paint اجرا می‌شود — پردازش پاسخ‌ها (JSON.parse + setState + رندر) از پنجره‌ی بحرانی TBT خارج شد.

### ۴۲.۳ Vendor splitting (vite.config.ts)

`manualChunks: { 'vendor-react': ['react','react-dom','react-dom/client','react-dom/server','scheduler'] }` →
**باندل اصلی index: 333.6KB → 151.6KB (gzip 106.3→49.7KB)** + `vendor-react` 193KB جدا (کش دائمی). نکته: فرم object با فقط `['react','react-dom']` کار نکرد (react-dom جدا نمی‌شد) — اضافه‌کردن `react-dom/client` و `scheduler` صریح لازم بود.

### ۴۲.۴ انیمیشن‌های non-composited (گزارش: «4 animated elements»)

`transition-all` همراه `group-hover:scale` → `transition-transform` / `transition-[transform,opacity]` در: `HomeTab.tsx` (hero + کارت ژانر/سالن/بازی + فلش‌ها)، `DarkGoldHome.tsx` (۳ تصویر)، `GecoPurpleHome.tsx` (۳ دکمه)، `ShopTab.tsx`، `CafeTab.tsx`. `transition-all` مرورگر را مجبور به ترنزیشن همه‌ی properties می‌کرد (هزینه‌ی layout/paint)؛ `transform`/`opacity` کامپوزیت‌شونده و ارزان هستند.

### ۴۲.۵ تست و تأیید

- `tsc --noEmit` ریشه → صفر خطا ✅
- `vite build` ریشه → موفق ✅ (index 151.6KB + vendor-react 193KB؛ HTML فقط index+vendor را در startup دارد)
- `tsc --noEmit` Management App → صفر خطا ✅ (بعد از اضافه‌کردن @types/react و رفع nextStatus در بخش ۴۱)
- `vite build` Management App → موفق ✅
- Lighthouse/Chrome محلی ممکن نشد (دانلود Chromium از sandbox مسدود) — تست GTmetrix واقعی باید بعد از دیپلوی روی bazino.pro انجام شود. پیش‌بینی: TBT 729→~250-350ms، حذف «LCP lazy loaded»، Performance 54→~70+.

### ۴۲.۶ فایل‌های تغییر یافته (این batch)

- `src/App.tsx` — مودال‌های شرطی + scheduleIdle + fetchهای غیربحرانی defer
- `vite.config.ts` — manualChunks vendor-react
- `src/components/HomeTab.tsx`, `DarkGoldHome.tsx`, `GecoPurpleHome.tsx`, `ShopTab.tsx`, `CafeTab.tsx` — transition محدود به transform/opacity
- `HANDOFF_CONTINUE_HERE.md` — بازنویسی کامل به وضعیت واقعی (۲۸ مورد ✅ + کارهای اخیر + کار بعدی)

---

## ۴۳. پاکسازی کامل پروژه — حذف فایل‌های زائد و تکراری

به درخواست کاربر، یک پاکسازی کامل روی ریپو انجام شد: **۱۱۰۸ → ۲۹۵ فایل** (۸۱۳ فایل حذف از git).

### حذف‌شده‌ها

**۱. پوشه‌های اسکیل دستیاران AI — ۴ نسخه‌ی تکراری از یک محتوا (~۱۱.۶MB):**
- `.claude/` (۱۵۵ فایل)، `.cursor/` (۱۵۴)، `.gemini/` (۱۵۵)، `.windsurf/` (۱۵۲)، `.claude-plugin/` (۲) — هر ۴ پوشه تقریباً محتوای یکسان (`ui-ux-pro-max`, `design-system`, `brand`, `design`, `slides`, `banner-design`, `bazino-*`...) بودند که از طریق CLI نصب شده‌اند. این‌ها کانفیگ ابزار شخصی هستند نه بخشی از پروژه → همه حذف شدند.

**۲. کپی‌های قدیمی/تکراری داخل `flutter_app/` (فقط نسخه‌ی واقعی Flutter باقی ماند):**
- `flutter_app/src/` (۳۸ فایل) — کپی قدیمی سایت → حذف
- `flutter_app/server.ts` + `flutter_app/server/` — کپی قدیمی بک‌اند → حذف
- `flutter_app/Management App/` (۳۹ فایل) — کپی نرم‌افزار مدیریت → حذف
- `flutter_app/public/` (۷ فایل) — کپی یکسان از public ریشه → حذف
- `flutter_app/desktop-app/` (۷) + `flutter_app/desktop-builds/` — کپی یکسان از ریشه → حذف
- `flutter_app/index.html`, `package.json`, `package-lock.json`, `postcss.config.js`, `tsconfig.json`, `vite.config.ts` — کپی کانفیگ‌های ریشه → حذف
- `flutter_app/metadata.json`, `.env.example`, `assets/theme-design-guide.md` — کپی‌های یکسان از ریشه → حذف
- `flutter_app/*.md` — ۹ مستند که نسخه‌ی canonical آن‌ها در ریشه است (ARCHITECTURE, CLOUD_BUILD_GUIDE, HANDOFF_CONTINUE_HERE, ISSUES_FOUND, PUBLISHING_GUIDE, PUBLISH_AND_DATABASE_GUIDE, REFERENCE_IMAGE_INFO, SESSION_SUMMARY, BAZINO_PRO_Presentation) → حذف
- `flutter_app/Bazino_Pro_Presentation.pdf/html` + `Bazino_Pro_Mobile_Presentation.pdf/html` — کپی پرزنتیشن‌های ریشه → حذف
- `flutter_app/.git_flutter_backup_20260802/` (۷۶ فایل) — یک مخزن git کامل داخل پروژه → حذف

**باقی‌مانده در flutter_app (پروژه‌ی واقعی):** `lib/` (۱۶ فایل Dart)، `pubspec.yaml/lock`، `analysis_options.yaml`، `.metadata`، `.gitignore`، `.vscode/`، پلتفرم‌های `android/ios/macos/windows/linux/web/test` و مستندات مخصوص خود Flutter (`README.md`, `HANDOFF.md`, `LOCAL_RUN_HANDOFF.md`, `REDESIGN_NOTES.md`).

**۳. فایل‌های ریشه:**
- `greeting.zip` (۳MB) — یک پروژه‌ی جدا (greeting card) که به این ریپو آپلود شده بود → حذف
- `PROMPT_SKILL_INSTALL_AND_UI_IMPROVE.md` — پرامت نصب اسکیل که با حذف اسکیل‌ها منسوخ شد → حذف
- `bazino-pro-latest.zip` — آرشیو قدیمی → حذف (نسخه‌ی تازه ساخته شد)

### .gitignore به‌روزرسانی شد
- `*.zip` — آرشیوهای خروجی دیگر commit نشوند
- `.claude/`, `.cursor/`, `.gemini/`, `.windsurf/`, `.claude-plugin/` — پوشه‌های اسکیل AI دیگر وارد git نشوند

### تأیید صحت بعد از پاکسازی
- `tsc --noEmit` ریشه → صفر خطا ✅
- `vite build` ریشه → موفق ✅ (vendor-react + index همان ۱۵۱KB)
- flutter_app ساختار سالم (pubspec + lib + پلتفرم‌ها) ✅
- هیچ ارجاعی در کد به مسیرهای حذف‌شده وجود ندارد ✅

## ۱۴۰۵/۰۶/۱۲ — ترجمه‌ی کامل سایت به fa/en/ru/tr (+ CSP، تم، منوی زبان)

**کامیت‌ها (شاخه `arena/01a067ac-bazino-gamenet-portal`):** `ad6414f` CSP → `3a7ebe3` پایداری تم → `4e78eb4` منوی زبان + GeoIP → `3239806` بچ A/B/D → `006ff6f` بچ C → `afe1046` بچ E → بچ F (تست‌ها/مستندات).

**الگو:** به‌جای کلیدهای انتزاعی، هر متن به‌صورت inline با `L(language, { fa, en, ru, tr })` (در `src/utils/i18n.ts`) نوشته شده تا diff قابل بازبینی بماند. RTL فقط برای fa؛ اعداد با `localeOf(language)`؛ واحد پول «تومان» و نام‌های برند حفظ شدند.

**ابزار:** `scripts/i18n-extract.mts` (extract / apply / audit روی AST) — ~۹۰۰ سه‌تایی را استخراج و پس از ترجمه جای‌گذاری کرد؛ موارد تودرتو دستی اصلاح شدند.

**سرور:** `server/apiMessages.ts` دیکشنری ۴۹ پیام × ۴ زبان؛ `server.ts` همه‌ی خطاهای کاربرپسند را با `apiError(req, KEY)` برمی‌گرداند (متن ترجمه‌شده + `code`). کلاینت زبان انتخابی را با هدر `X-Lang` می‌فرستد (اینترسپتور `src/services/authToken.ts`)؛ در نبود آن `Accept-Language`.

**تست‌ها:** `npm test` = ۲۴۶/۲۴۶ (۳ تست جدید: کامل‌بودن دیکشنری API، نبودن متن خام فارسی در server.ts، نبودن سه‌تایی fa/en در کامپوننت‌ها). `tsc` و `vite build` سبز.

**بررسی بصری (Chromium واقعی، production build):** صفحه‌ی اصلی و رزرو در هر ۴ زبان (`final-{lang}-home|reserve.png`) و ۱۶ بخش پنل مدیریت در ru/tr (`i18n-admin-{lang}-NN.png`): صفر خطای کنسول؛ تنها متن فارسی باقی‌مانده در ru/tr داده‌های نمونه‌ی دیتابیس است (E.42).

**خارج از محدوده:** اپ Flutter، Management App، theme.js شخص ثالث، ترجمه‌ی sampleData.

## ۱۴۰۵/۰۶/۱۲ — سیستم قالب: ماندگاری (Volume/Mongo)، نصب اتمیک، حذف امن، فعال‌سازی سراسری

- `server/paths.ts` جدید؛ `BAZINO_DATA_DIR` و `MONGO_URL` (README به‌روز شد).
- themeStore: نصب اتمیک با replace، cleanup پوشه‌های نیمه‌کاره، `installedAt` در فهرست.
- server.ts: نصب = فعال‌سازی سراسری، DELETE ریست activeThemeId، `GET /api/admin/storage-status`.
- کلاینت: بدون fallback بی‌صدا، کش نسخه‌دار، دکمه‌ی «انتخاب به‌عنوان قالب سایت» سراسری، کارت زیرساخت و «حالت به‌روزرسانی» در پنل.
- تست: ۲۵۱/۲۵۱ + فلو UI در Chromium واقعی (`browser-test/t2-*.png`). Railway واقعی تست‌نشده.

## ۱۴۰۵/۰۶/۱۲ — سیستم قالب v2: بخش‌ها (regions)، توکن‌ها، رشته‌های ۴زبانه، اسلایدر ۴زبانه

- `src/themeSdk/sdk.ts` v2 (`THEME_REGIONS`, `ThemeComponentProps` با `ts/tokens/slides/region`), `ThemeRegion.tsx` (fallback به پیش‌فرض)، `App.tsx`/`HomeTab.tsx` بخش‌بندی شدند؛ قالب `home` نسخه ۱ بدون تغییر کار می‌کند.
- `server/themeStore.ts`: `detectRegisteredRegions`, `KNOWN_REGIONS`, strings/tokens/regions در `/api/themes`؛ `theme.js` اختیاری؛ region ناشناخته → 400.
- `src/index.css`: پل توکن `body[data-theme]` → کلاس‌های Tailwind (E.51 — دلیل واقعی «هدر عوض نمی‌شد»).
- پنل ادمین: توضیح اسلاید در ۴ زبان، بج regions/CSS-only روی کارت قالب، پیش‌نمایش ZIP.
- قالب نمونه (`themeZipCore.ts`): SDK v2 با hero+footer، strings ۴زبانه، tokens.
- مستندات: `src/themes/README.md` بازنویسی (regions، توکن‌ها، قرارداد RTL/فونت، strings)، بخش قالب در README.
- تأیید بصری در Chromium واقعی (`r1…r7`, `b-*.png`)؛ تست‌ها ۲۵۸/۲۵۸.

## ۱۴۰۵/۰۶/۱۳ — روتینگ آدرس، عنوان/جستجوی پنل، آدرس و نقشه‌ی واقعی
- `src/utils/routes.ts` + `App.tsx`/`AdminPanelTab.tsx`: آدرس مرورگر = صفحه‌ی فعلی؛ رفرش/Back درست؛ `<base href="/">`.
- پنل: هدر بخش (نام + مسیر)، `document.title`، جستجوی سریع بخش‌ها با نتایج لینکی.
- آدرس/تلفن/نقشه‌ی واقعی İskele (Vista Mare) + فیلدهای Google Maps/Lat/Lng در سفارشی‌سازی.
- `public/images/real/` برای تصاویر واقعی. تست‌ها ۲۵۹/۲۵۹.

## ۱۴۰۵/۰۶/۱۳ — تصاویر واقعی + تحقیق درگاه PayTR
- تصاویر نمونه‌ی هرو/ژانر/سالن/اسلاید موبایل با عکس‌های واقعی جایگزین شد (`public/images/real/`, `sharp`، همه‌ی سایزها). کافه/فروشگاه هنوز نمونه.
- تحقیق PayTR و ذخیره در `docs/payments/` (چک‌لیست الزامات سایت + مرجع API). هنوز هیچ کدی برای پرداخت نوشته نشده؛ برنامه‌ی ۵ مرحله‌ای در `PAYTR.md §۴`.

## ۱۴۰۵/۰۶/۱۳ — PayTR + صفحات قانونی مستقل از قالب + TL

- راهنمای گام‌به‌گام دریافت درگاه: `docs/payments/PAYTR-BASVURU-REHBERI.md` (مدارک، ثبت‌نام در سایت، دریافت merchant_id/key/salt، تنظیم Bildirim URL، حالت تست → زنده، ریسک KKTC).
- بک‌اند: `server/payments/paytr.ts` (امضاها/HTTP) و `server/payments/routes.ts` (config/create/callback/orders/mock/admin list/refund)؛ جدول `payment_orders` در هر سه پرووایدر؛ env: `PAYTR_MERCHANT_ID/KEY/SALT`, `PAYTR_TEST_MODE` (پیش‌فرض ۱), `PAYTR_MOCK`, `PUBLIC_URL`.
- فرانت مستقل از قالب: `src/legal/` (LegalShell, LegalPage, ContactPage, LegalFooter, PaymentBadges, PaymentCheckout, PaymentResultPage, LegalAdminSection, legalContent). مسیرها در `routes.ts` (`standalonePageFromPath`)، رندر قبل از ThemeRegionProvider در `App.tsx`.
- خریدها (کافه/فروشگاه/رزرو/تورنمنت): اگر `/api/payments/config.enabled` → مودال PayTR؛ وگرنه مسیر قدیمی بدون پرداخت (سازگاری عقب‌رو).
- واحد پول TL در کل سایت؛ امتیاز = مبلغ/10.
- تست‌ها: ۲۸۳/۲۸۳ (۲۴ تست جدید: امضاهای PayTR، محتوای قانونی، استقلال از قالب، جریان create→mock→callback، idempotency، هش نامعتبر، رزرو/هم‌پوشانی، فهرست ادمین، تنظیمات).
- تست‌نشده: درگاه واقعی PayTR، رندر فونت فارسی در sandbox، Railway.

## ۱۴۰۵/۰۶/۱۳ — انتخاب قالب فقط توسط ادمین، رفع باگ SDK هدر، `SDK.LocationFrame`

- هدر: دکمه‌ی انتخاب قالب و `ThemeSelectorModal` حذف؛ قالب سایت همیشه = انتخاب ادمین.
- علت «قالب جدید هدر را تغییر نمی‌دهد»: باگ سیستم قالب (خروجی DOM خام از `render()` نادیده گرفته می‌شد). رفع در `src/themeSdk/sdk.ts` (`normalizeRenderOutput`).
- `src/themeSdk/LocationFrame.tsx`: فریم آماده‌ی لوکیشن (card/map/inline) + `locationFrom`؛ روی `window.BazinoThemeSDK` در دسترس theme.js؛ مستند در README قالب‌ها (+ سه شکل مجاز خروجی render).
- تست‌ها ۲۸۹/۲۸۹ (۶ جدید).

## ۱۴۰۵/۰۶/۱۳ — ورود با کد پیامکی (OTP)، پروفایل کاربر، تیکت پشتیبانی

- احراز هویت: ثبت‌نام فرم‌محور در وب حذف شد؛ ورود/عضویت با شماره‌ی موبایل + کد پیامکی (`/api/auth/otp/request|verify`)، کد هش‌شده در جدول `otp_codes`، محدودیت‌ها سمت سرور روی شماره و IP هم‌زمان (`429 + retryAfter`). ورود با رمز برای ادمین و کاربرانی که رمز دائمی گذاشته‌اند باقی است.
- لایه‌ی SMS: `server/sms/` (smsto اصلی، easysendsms، mock) + `docs/sms/SMS-PROVIDERS.md`؛ env: `SMS_PROVIDER`, `SMSTO_API_KEY`, `SMS_SENDER_ID`.
- پروفایل `/profile[/tab]` مستقل از قالب: ویرایش مشخصات، آواتار WebP، امتیازها، رزروها، سفارش‌ها (ستون `username` روی سفارش‌ها)، تورنمنت‌ها، پشتیبانی، امنیت (شماره‌ی تأییدشده + رمز دائمی اختیاری).
- تیکت پشتیبانی: کاربر (ایجاد/پاسخ/بستن) + ادمین `/admin/tickets`؛ نشان «پاسخ جدید» در هدر و تب پشتیبانی.
- رفع باگ‌های جانبی: مودال ورود زیر ویجت دانلود اپ (E.82)، شکست نصب پس از بوت با ادمین fallback (E.83).
- تست‌ها ۳۲۶/۳۲۶. تست‌نشده: ارسال واقعی پیامک با SMS.to (کلید ندارید)، چیدمان RTL فارسی در Chromium (فونت)، Railway.
- اصلاح وضعیت تیکت‌ها طبق درخواست: «در حال بررسی» → «پاسخ داده شده» → «بسته شده» خودکار پس از ۴۸ ساعت بی‌پاسخی کاربر (E.85).
- رفع «هدر جایگزین نمی‌شود / فلش اسلایدر قبل از هروی قالب» (E.86): ZIP با پوشه‌ی ریشه، bootstrap قالب فعال در HTML، جای‌نگهدار hero وابسته به قالب.

## ۱۴۰۵/۰۶/۱۳ — تسک ۱۳: کیف پول + پرداخت در محل، PayTR خاموش (نه حذف)

- درگاه PayTR با `PAYMENT_ONLINE_ENABLED` (پیش‌فرض خاموش) گیت شد؛ کد/callback/docs باقی است؛ ادمین «موقتاً غیرفعال» نشان می‌دهد.
- بک‌اند: `server/wallet/routes.ts`؛ جدول‌های `wallet_transactions` (دفتر کل، `balanceAfter`، `idempotencyKey` یکتا) و `onsite_orders` در SQLite/SQL Server/Mongo. موجودی هرگز منفی نمی‌شود.
- قوانین: رزرو ایستگاه → کیف پول یا در محل تا ۱۰ دقیقه قبل از سانس؛ تورنمنت → کیف پول یا در محل تا ۴۸ ساعت قبل از شروع؛ گذشت مهلت → ابطال خودکار (`expireOnsiteOrders` هر دقیقه). بوفه/فروشگاه فقط در محل؛ موجودی انبار و امتیاز فقط بعد از تأیید پرداخت. لغو کاربر قبل از مهلت → آزادسازی جا / بازگشت وجه.
- فرانت مستقل از قالب: `CheckoutModal` (انتخاب روش + متن قانون + تیک پذیرش + مهلت دقیق در نوتیفیکیشن)، `/profile/wallet` (موجودی، گردش، سفارش‌های در انتظار + لغو)، `/admin/wallet` (تأیید نقدی/کارت/کیف پول، لغو، جست‌وجو/اصلاح کیف پول، تراکنش‌ها). فوتر بدون نشان کارت وقتی درگاه خاموش است.
- اپ مدیریت: `walletSync.ts` (صف آفلاین با idempotency، flush خودکار) + `WebWalletPanel` در تب «اعضا و کیف پول»؛ آزمایش زنده روی سرور محلی.
- API sync برای اپ: `/api/sync/wallet/topup|charge|:phone`, `/api/sync/onsite-orders[…/settle|cancel]`.
- سند: `docs/payments/WALLET.md`. ISSUES E.87–E.92.
- تست‌ها: API ۱۶۳/۱۶۳ (۱۳ جدید)، Unit ۸۷/۸۷ (۵ جدید)، UI ۴۲/۴۲ (۵ جدید)، DB ۳۶/۳۶، Providers ۲۶/۲۶ → **۳۵۴/۳۵۴**. Chromium: مودال رزرو (EN دسکتاپ)، تورنمنت در محل (EN)، بوفه (TR موبایل)، پروفایل کیف پول (EN دسکتاپ+موبایل)، ادمین (TR)، اپ مدیریت (FA).
- تست‌نشده: اجرای واقعی روی SQL Server/Mongo؛ فونت فارسی در Chromium sandbox؛ Railway.

## ۱۴۰۵/۰۶/۱۴ — Affiliate Marketing (batch کامل)

- اعداد طرح به‌صورت ردیف واقعی `settings` seed می‌شوند (`affiliate_new_pct=10`, `return=5`, `tournament=10`, `override=0`, `window_days=30`, `wallet_cashout_min_tl=0`, `excluded_roles=admin`, `program_open=1`) و بازنویسی نمی‌شوند.
- موتور: کلیک/claim/کمیسیون pending تا مهلت لغو، تأیید+شارژ کیف پول، برگشت، حضور، گزارش.
- هوک: checkout/settle → `onOrderPaid`؛ cancel/expire → `onOrderReversed`؛ checkin → `onReservationAttended`.
- UI: ادمین، پروفایل، `?ref=`، فیلد کد معرفی رزرو/تورنمنت، متن قانونی، نقد حضوری اپ مدیریت.
- سند: `docs/payments/AFFILIATE.md`.
- تست: unit seed/normalize/engine، SQLite جداول+CRUD، Mongo round-trip، API click/checkout/cashout.
- تست (اجرا شد): unit 95/95، database 37/37، providers 27/27، ui 42/42، api 167/167 (سوئیت ۳۶ افیلیت سبز). Chromium این batch اجرا نشد.
- هوک تسویهٔ حضوری `settleOnsite` هم `onOrderPaid` را صدا می‌زند.

## ۱۴۰۵/۰۶/۱۴ — تحقیق وب اتصال POS، بدون تغییر کد

- گزارش منابع عمومی و نتیجهٔ بررسی: `docs/payments/POS_RESEARCH.md`؛ شامل مستند GMP-3 v5.0، راهنمای USB/TCP-IP و جفت‌سازی DİA، مستندات رسمی Ingenico/Worldline/iKasa و İş Bankası.
- مرجع محلی در فهرست رسمی KKTC تأیید شد: Microvise Innovation Ltd. برای iWE280/iDE280/MOVE5000F. مقررات محلی (ابلاغیهٔ ۱۴، اصلاحیهٔ فوریهٔ ۲۰۲۶) و جریان ÖKC → EFT-POS → رسید مالی بررسی شدند؛ شمول بازینو و نحوهٔ ثبت کیف پول باید با حسابدار/تأمین‌کننده تأیید شود.
- مدل از `maximum 25.yıl` و شناسهٔ ترمینال مشخص نشد. عکس MODEL، پایه/پورت‌ها، نام بانک/نصاب و بعد SDK/پروتکل/مجوز ECR متناسب با همین دستگاه لازم است. متن آمادهٔ درخواست فنی ترکی در گزارش ذخیره شد؛ ارسال نشده است.
- نقاط فعلی پرداخت مدیریت و پوستهٔ Electron فقط ایستا خوانده شدند؛ انتخاب POS فعلی اتصال بانکی نیست. طراحی نهایی/کدنویسی هنوز شروع نشده است.
- E.101 و اسناد هندآف به‌روز شدند. شاخهٔ نشست `arena/01a070af-bazino-gamenet-portal`، پایه `daf4791` (مرج PR #17)؛ فرمان‌های مخرب/شاخه‌های قدیمی هندآف اجرا نشدند.
- هیچ کد محصولی، تنظیم دستگاه یا سیاست پرداخت عوض نشد. هیچ حساب SDK، خرید، تراکنش بانکی، تماس خارجی یا تست نرم‌افزاری/مرورگری انجام نشد؛ اتصال عملی تأیید نشده است.

## ۱۴۰۵/۰۶/۱۴ — پلن توسعهٔ مدیریت (بدون شروع پیاده‌سازی)

- درخواست جدید کاربر بررسی و پلن در `docs/management/EXPANSION_PLAN.md` نوشته شد: رزرو روی کارت ایستگاه، سفارش کافه/فروشگاه، افیلیت و کیف پول، کوپن/ساعات ویژه، تورنمنت و محتوا/انتشار با Manus.
- سه تصمیم از کاربر با سؤال صریح گرفته شد: **دریافت‌های حضوری فقط نقدی/POS** و حفظ کیف پول سایت در موارد موجود؛ **تسویهٔ کیف پول یعنی نقدکردن موجودی مشتری/همکار**؛ **Instagram + Telegram** همراه بلاگ. محتوا و کوپن/ساعات ویژه در هر دو پنل.
- کد مدیریت، APIهای رزرو/سفارش/پرداخت و providerها بازبینی ایستا شدند؛ E.102 (sync/حضور)، E.103 (پلن توسعه) و E.104 (ارتباط نتیجهٔ رزرو بعد از settle) ثبت شدند.
- مستندات رسمی Manus v2 (Task/Structured Output/Webhook/RSA-SHA256/Connector) و نشر Zernio بررسی شد. استفاده از Zernio برای نشر دو شبکه **پیشنهاد پلن** است، نه اتصال موجود یا تست‌شده؛ ماژول PR/DM قبلی نشر پست انجام نمی‌دهد.
- ۹ batch با معیار پذیرش مالی/همگام‌سازی/بصری پیشنهاد شد. کنترل خودکار POS همچنان منتظر مدل/SDK و خارج از این بسته است.
- فقط اسناد تغییر کردند. نصب dependency، تغییر کد محصول، اجرای سرور/تست/Chromium، ایجاد حساب یا ارسال/انتشار واقعی انجام نشد؛ «شروع کن» هنوز صادر نشده است.

## ۱۴۰۵/۰۶/۱۴ — تأیید پلن و بررسی تصاویر POS، قبل از شروع

- کاربر پلن مدیریت را تأیید کرد ولی صریحاً بررسی هشت تصویر POS را **پیش از شروع** خواست. «شروع کن» صادر نشده؛ کد محصول دست نخورده است.
- تصاویر در خود پیام بصری بررسی شدند: تطابق بسیار قوی با **Ingenico Desk/2600**، نوشتهٔ İş Bankası روی بدنه، MagicBox با PSU/POS و Ethernet متصل. نوشتهٔ MODEL/SKU و نسخهٔ برنامه هنوز خوانده/تأیید نشده‌اند.
- راهنمای رسمی Desk/2600 به‌طور کامل از ابزار وب خوانده شد؛ **USB-B Slave برای اتصال cash register در §۴٫۲٫۳، صفحهٔ ۱۲** مستند است. فعال‌سازی/پروتکل ECR و SDK همین نصب KKTC همچنان به بانک/نصاب وابسته‌اند.
- گزارش `docs/payments/POS_DEVICE_REVIEW.md` نوشته شد؛ تحقیق POS، وضعیت پلن، هندآف‌ها و E.101/E.103 به‌روز شدند. اسناد GMP3 مدل‌های مالی و iCT250 به Desk/2600 تعمیم داده نشوند.
- مسیرهای اعلام‌شدهٔ فایل آپلود در workspace موجود نبودند؛ read_file و جست‌وجوی محلی ناموفق بود، پس crop/OCR فایل اصلی انجام نشد. تصاویر مقایسه‌ای وب با read_file باز شدند؛ عکس‌ها/شناسه‌های مالی رسید کاربر به Git اضافه نشدند.
- فقط تحقیق/مستندسازی: بدون نصب وابستگی یا SDK، تغییر دستگاه/شبکه، تراکنش/تماس واقعی، تست نرم‌افزار/Chromium، commit/push یا ZIP.


## ۱۴۰۵/۰۶/۱۴ — درخواست کد تست و روشن‌سازی شکاف پروتکل

- کاربر نوشت آیا می‌توان با مستندات کد نوشت و تست کرد. یک تستر مستقلِ read-only USB/COM و شبیه‌سازِ بدون اثر مالی پیشنهاد شد، اما کاربر آن را تأیید نکرد و پرسید چرا پروتکل با تحقیق وب به دست نیامده است.
- **Windows 10/11** به‌عنوان سیستم صندوق توسط کاربر انتخاب شد. نسخهٔ دقیق/معماری هنوز معلوم نیست.
- بررسی واقعی `desktop-app` و جست‌وجوی تکمیلی وب ECR/SDK/ZVT انجام شد. نتیجه عوض نشد: رابط USB-B مستند است، اما دستور فروش/استعلام و SDK برنامهٔ İş Bankası همین نصب KKTC در دست نیست. GMP3 مدل‌های دیگر یا شبیه‌ساز جای آن نیست.
- وضعیت در `POS_DEVICE_REVIEW.md §۸` و هندآف §۱۳ ثبت شد. هیچ تستر/کد محصول نوشته، تست/تراکنش اجرا یا SDK نصب نشده است؛ شروع تأیید نشده است.


## اجرای پلن مدیریت — batch 1

کاربر اجرای پلن اصلی و کنارگذاشتن POS را خواست؛ اجرا آغاز شد. زیربنای رکورد/تراکنش، هویت/مجوز، نگاشت ایستگاه و اصلاح تفکیک وضعیت رزرو اضافه شدند. ۸/۸ تست SQLite/core، TypeScript و build مدیریت موفق؛ ورود و نگاشت در Chromium و تصاویر واقعی بازبینی شدند. جزئیات/موارد باقی‌مانده در `docs/management/EXECUTION_LOG.md`.


### Batch 2 مدیریت

دریافت‌های نقدی/POS، رسید، شارژ اتمیک و نقدکردن دومرحله‌ای با دفترکل سرور پیاده‌سازی شد. تست ۱۳/۱۳ و مرورگر واقعی سه سناریو/تصویر؛ جزئیات در EXECUTION_LOG. POS واقعی همچنان متوقف است.
